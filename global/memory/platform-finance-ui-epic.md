---
name: platform-finance-ui-epic
description: "ACTIVE epic — production finance-BC UI on @acme/ui (GH #1453, tasks #1454-1465), PR #1533 MERGED to main (70f0b5ad, 2026-07-01). Backend done (#1462/1464/1465); FE: #1456 shell (c96c4e57), #1457 Accounting (8af63625), #1458 Commission (f13831cb), #1459 RefData (5820b64d), #1460 Communication (b9d78554), #1461 kit-gaps (26fd837f) DONE; #1455 CLOSED. GitHub CLOSED=#1454/1455/1459/1460/1461/1462/1464/1465; OPEN-but-impl=#1456/1457/1458 (need reconcile-close); OPEN-remaining=#1463 (deploy+prod-verify, HUMAN-gated, SWA framing STALE→same-origin AKS #1466). main MERGED 2026-06-30 (cdd64094, 0 behind). CI: pact-contracts flake FIXED — reporters:['default'] unblindfold (6cbc24b9) + pact-only CI retry:2 (e46a82a4). Follow-ups (2026-07-02): #1491 MERGED via #1554 (permission-based) + #1457 CLOSED + #1546 CLOSED via #1555. Verified & staged, awaiting quiet CI window (strict-up-to-date merge race): #1498→PR#1556 (re-audited MERGE), #1511→PR#1558(BE)+#1582(FE), #1472→PR#1584, #1499→PR#1585 (stacked on #1556). Security P0s: #1587 fix COMPLETE incl. audit-attribution dd50bc0d (PR #1601 CI-baking, then merge w/ #1585/#1584/#1582); network-policy hardening follow-up filed. RabbitMQ re-affirmed (OD-4; stale-docs issue #1615). Finance UX design rounds live: R1 invoices vendored PR #1616 (⚠ lineage diverged from #1612 — reconcile before R2). Requirements synthesis + enterprise-audit workflow (wf_daaef123-735) → hardening plan pending. #1463 deploy LAST (human-gated). See 2026-07-02 body bullets."
metadata:
  node_type: memory
  type: project
  originSessionId: 00000000-0000-0000-0000-000000000006
---

**Epic `platform-finance-ui`** (branch `feat/platform-finance`) — port the approved "Platform v2" design into the real finance UI, full-stack-wired, no mock data.

- **Two sources of truth (user-set):** _design_ = the **live Claude Design project** `00000000-0000-0000-0000-000000000053` ("Platform v2", owner Milos), also vendored at `docs/platform/design/platform-v2-prototype/` (#1264). _Requirements/domain_ = `docs/platform/` (doc-site is READ-ONLY business spec). Design governs appearance; requirements govern behaviour. Surface docs↔design↔code conflicts, never silently resolve.
- **Scope:** finance modules — Accounting/Invoices (+ERP), Commission, Reference data (FX/customers/products/accounting-months), Communication (document+notification). Out: trading, inventory, dashboard, platform, auth.
- **Build on the existing kit:** `@acme/ui` (Nx lib `platform-ui`, `libs/platform/ui`; imports `@acme/ui` + `@acme/ui/domain`) already ships primitives, DataTable, CommandPalette, ThemeProvider, and finance domain bits (TypeBadge, LifecycleStepper, fmtGBP, charts incl. Waterfall/ErpStatusPie/FailureTrend/CommissionStackedBar/AgeingBar). See [[platform-design-system-kit]]. **Policy AD-7 (user-mandated): use the library; any missing component is created in `libs/platform/ui` FIRST (impl+story+test+barrel), then consumed — no bespoke/`@mui` in finance pages.** `apps/platform-design-demo` = reference. MUI still in `platform-frontend` (AppLayout/SearchOverlay) → migrate off.
- **Backend mostly exists** (accounting 3010, commission 3012, document 3021, notification 3020; gateway routes `/invoices`,`/commissions`). Gap-fills FR-G1..G6: wire invoice generation to real Trading line items (it was stubbed), ERP posting summary+trend endpoint, commission-rule write, period open-new, new gateway routes, audit failure-reason field.
- **PM ceremony status: COMPLETE through epic-sync; PAUSED before issue-start (2026-06-25).** PRD ✅ · epic ✅ · arch-create ✅ (`architecture.md` + **[ADR-0058](docs/adr/0058-invoice-generation-event-carried-line-items.md)**) · epic-decompose ✅ (**12 tasks**, 004/005 split → +011/012) · tests-generate ✅ (RED suite, manifest) · readiness-check ✅ **NEEDS_WORK / 0 CRITICAL / hard-gate PASS** (residual MAJORs = advisory RED-phase todos; `readiness-report.md`) · **epic-sync ✅**. Architecture forks: FR-G1 = event-carried snapshot (ADR-0058); FR-G2 = on-the-fly ERP aggregate. ADR renumber on the 2026-06-29 main merge: main had consumed 0058-0062, so the invoice-gen ADR was renumbered **0058→0063** (file rename + README + accounting/trading code refs; main's CORS ADR-0058 left as-is); next free = **0064**.
- **GitHub: epic [#1453](https://github.com/initech-trading-platform/acme/issues/1453); 12 task sub-issues #1454–#1465** (001→1454 … 012→1465; `.claude/epics/platform-finance-ui/github-mapping.md`). Task files renamed to issue#, deps remapped.
- **IMPLEMENTATION (on `feat/platform-finance`, GREEN where verifiable):** main MERGED (commit `9338fed0`; brought adoption-epic kit-align #1423 + SWA scaffold #1440 — kit now has NavItem/Segmented/Metric/StatusStepper/Switch/AmountCell + platform-frontend SWA infra exists). Commits: `766326b9` docs/DDD · `a5a52371` event-contracts · `3d967f62` RED suite · `1c4ec09e` **#1462** cross-cutting backend (gateway 6 finance routes + ADR-0058 snapshot trading-emit+accounting-consume+Migration_004 + ErpPostingAudit.failureReason) · `724ac548` **#1464** accounting backend (generate-from-eligibility, posting summary, period open/close, invoice DTO) · `40c97eaa` **#1465** commission backend (endpoint rename + versioned-update 409 + feature-flag gate + `/reports?scope=` + CSV/PDF export) · `c96c4e57` **#1456** finance app shell (financePermissions canPerform/getNavItems + AppLayout RBAC finance nav + kit CommandPalette launcher + TenantSwitcher FR-S3 + AppShell/FinanceErrorBoundary/DirtyFormGuard FR-S6 + RBAC finance routes /accounting,/commission,/refdata w/ placeholder pages pending #1457-1459 + types/auth tenants/tenantName; FR-S6 shell-wiring RED→GREEN; 66 specs green; code-reviewer APPROVE). · `8af63625` **#1457** accounting vertical (InvoicesPage FR-A1 server-filtered DataTable + InvoiceDetailPage FR-A2/A4 — **only the actions the backend actually exposes** (the extra lifecycle buttons in the prototype have no route and no status behind them) + BankAccountsPage FR-A6 ADMIN-gated + `/api/v1/bank-accounts` gateway route + api/invoices+bank-accounts hooks; FR-D3 Task-004 RED→GREEN incl. 401-unwrap fix; 350 FE specs green; maker≠checker APPROVE after 3 fixes [duplicate query keys, 422→400 on a server-rejected write, RBAC comment honesty]; **follow-up #1491 = server-side accounting RBAC**). · `f13831cb` **#1458** commission vertical (CommissionPeriodsPage FR-C1 + CommissionPeriodDetailPage FR-C2 via **role-scoped** `/commissions/reports?scope=all` (server downgrades TRADER→own) + PayoutsPage FR-C4 w/ useInfiniteQuery pagination + reactive FR-C6 403 FEATURE_DISABLED + api/commission hooks; FR-D3 Task-005 RED→GREEN; **payouts list leaked cross-trader → /commission/payouts now ADMIN/FINANCE route-gated** + defensive report-unwrap + Big.js sums; maker≠checker first-pass found CRITICAL leak + 3 majors, **re-review APPROVE 0 issues**; follow-ups **#1498** server RBAC/scoping + **#1499** pact-vs-backend contract; 392 FE specs green). · `f07ae8d6`(checkpoint)+`42111968`(main merge)+`5820b64d` **#1459** reference-data vertical: `api/reference-data.ts` (FR-D3 referenceDataKeys+hooks) + RefDataLayout tabbed `/refdata` + ExchangeRates (FX history + manual-override w/ **audit reason**, rate rendered RAW string) / Customers (+detail Contacts/Sites tabs, email-missing warn, derived role display) / Products (+product-groups CRUD, ERP-nominal on GROUP) / AccountingMonths (open/close — the ordering constraints are enforced server-side, never in the UI) pages; backend: exchange-rate `reason` column + Migration_005, `/api/v1/product-groups` gateway route (+registry tests). 4 pages agent-built in parallel then code-reviewed (agent-skills:code-reviewer) → fixed [CRITICAL `fmtNum` 0-dp rounded sub-1 rates; **DataTable column-order state leaks across tab panels → distinct `key` per table** (real bug: hid groups' ERP-nominal col); AccountingMonths always-shown rowActions silent no-ops → status-conditional action column; Customers/CustomerDetail error states → message+Retry]; typecheck+lint clean, 434 FE specs green, backend exchange-rate 19 + service-registry 38. See [[platform-design-system-kit]] kit-usage gotchas. **Done: #1454 #1456 #1457 #1458 #1459 #1462 #1464 #1465 (8/12, epic 67%).** Each verified typecheck + test:unit + lint/gitleaks; Testcontainers .tc verify in CI (Docker, not run locally).
- **BACKEND COMPLETE; FRONTEND IN PROGRESS.** #1383 MERGED (`fc08f8a5`, MUI gone) → **#1455 CLOSED** (satisfied by #1383) and **#1456 DONE** (`c96c4e57`, shell on migrated @acme/ui). #1457 Accounting (8af63625) + #1458 Commission (f13831cb) DONE. **NEXT = #1459 (Reference data) / #1460 (Communication)** — they replace the #1456 placeholder route bodies. Pattern (set by #1457/#1458): thin `api/<domain>.ts` (typed client + array query-key factory + TanStack hooks exported FROM the module per FR-D3) + pages composing kit DataTable/Badge/StatusStepper/Modal; route under the existing RequireRole block; `type` (not interface) for DataTable row models (Record<string,unknown> constraint); getByRole('grid') not 'table' in specs; required Field labels append a "\*" (match loosely + within(dialog)); reactive feature-gating = catch `403 errorCode==='FEATURE_DISABLED'` → disabled EmptyState; **a tenant-wide endpoint with NO server-side scoping must be route-gated to the privileged role + a server-scoping follow-up filed — never rely on client filtering for a data-leak boundary** ([[feedback_prefer_long_term_no_rush]]); defensive-unwrap report `{scope,period,data}` envelopes; Big.js for money sums. Then FE halves of #1464/1465, #1461 kit-gaps, #1463 deploy+prod-verify. DirtyFormGuard SPA-interception deferred (needs data-router; app is component-mode BrowserRouter). **Follow-ups filed off this vertical (all fixed later — see the 07-01/07-02 entries):** [#1472](https://github.com/initech-trading-platform/acme/issues/1472) a feature gate that was wired but FAILED OPEN — no tenant-feature source was bound, so the flag never actually gated anything. [#1491](https://github.com/initech-trading-platform/acme/issues/1491) write endpoints that were authenticated but never authorised, with the FE `canPerform` check as the only gate — HIGH (security), surfaced by the #1457 review; fix = adopt the identity + permission guards the sibling service already ran. **#1498**: a tenant-wide list endpoint with no role decorator, and reports that were not caller-scoped (mitigated meanwhile by route-gating the page to the privileged roles). **#1499**: consumer pact ↔ implemented backend divergence (the pact expected per-row detail the backend only served aggregated) — reconcile. Also: source-driven-dev gate blocks framework edits until USER touches `.claude/.source-driven-dev/{nestjs,mikroorm,fastify}.fetched` (see [[feedback_source_driven_dev_breadcrumb_cwd]]).
- **2026-06-30 STATUS + CI de-flake (PR #1533).** All FE verticals landed: #1460 Communication (`b9d78554`) + #1461 kit-gaps DescriptionList/DescriptionItem (`26fd837f`) now DONE. main merged clean (`cdd64094`, 0 behind; NO symlink dance needed this time — main touched 0 tracked `.claude` paths). **PR #1533 was red on `ci / pact-contracts`** — misleadingly attributed to `platform-accounting-service:test:pact` by the `@nx/vite:test` "blindfold" (no `reporters` ⇒ zero vitest output; the failing task name is just whichever Nx task exits non-zero). ROOT CAUSE: `pact-contracts` runs `nx affected -t test:pact` on a 2-core `ubuntu-latest`; each pact `executeTest` spawns a pact-core native HTTP mock server, and the spawn/port/ready handshake loses a timing race under load ("The following request was expected but not received"). **The victim suite VARIES run-to-run** (accounting one run, auth-service `auth-user-contract.pact.spec.ts` the next) and EVERY pact suite is deterministically GREEN locally (`vitest run --config <svc>/vitest.pact.config.ts` — must run vitest DIRECTLY; `nx run <svc>:test:pact` is blindfolded). FIX (2 commits): (1) `6cbc24b9` reporters:['default'] to un-blindfold — a concurrent session pushed this; (2) `e46a82a4` **pact-only + CI-only `retry: 2`** in `libs/platform/service-bootstrap/src/testing/vitest-config.ts` (unit/integration keep 0 — real failures stay immediate & visible). Deeper follow-up: move `pact-contracts` to the self-hosted 8-core runner. **⚠️ A CONCURRENT session actively pushes to feat/platform-finance — always `git fetch` + guard before pushing.** NEXT WORK (prioritized, security-first): reconcile-close #1456/1457/1458 → **#1491 accounting server-side RBAC (HIGH, zero tolerance)** → #1498 commission RBAC → #1472 fail-open → #1499 pact → LAST re-scope+execute #1463 deploy (needs #1491/1498 first + platform-owner + release sign-off). Untracked `apps/platform/accounting-service/test/pact/pacts/platform-frontend-accounting-service.json` should be COMMITTED (convention: sibling pacts tracked; no gitignore; CI-independent).
- **2026-07-01 — #1491 accounting server-side RBAC DONE → PR [#1554](https://github.com/initech-trading-platform/acme/pull/1554)** (own branch `fix/platform-1491-accounting-server-rbac` off main, independent of the finance session). **CORRECTION to the line-19 framing:** the fix is **permission-based**, NOT "port RolesGuard" — the epic owner chose to reuse `@acme/auth-client` verbatim (the trading-service `RequireTradingPermission` pattern), i.e. a method-level composite `@RequireAccountingPermission(perm)` = `UseGuards(GatewayIdentityGuard, PermissionsGuard) + RequirePermission(perm)`. (commission-service #1533 went role-based with a service-local `RolesGuard`/`@Roles` — the two finance services deliberately differ; a later consolidation could hoist a shared guard.) Guards **12** mutating endpoints across 5 controllers, each write mapped to a typed permission _(which permission gates which action is a product decision and is not recorded here)_. Guards are applied per method, so only write endpoints carry a permission gate; read endpoints and the S2S resolve path were deliberately left without a write-permission check (design choice recorded, revisit if reads become tenant-sensitive). `AccountingPermission` union admits only write-gating perms (compile-error to gate a write with `invoice:read`). TDD RED→GREEN; accounting unit suite 312/312; tc/pact suites bypass HTTP layer (unaffected — pact is a PactV4 consumer generator). Merged main (incl #1533) into the branch: resolved erp.controller import conflict + guarded #1533's NEW `POST /accounting-months` (open, FR-G4) + added `ERP_TOKEN_ENCRYPTION_KEY` test default in `test/setup.ts` (erp.controller validateConfig at module-load breaks any spec importing ErpController). Security review (agent-skills:security-auditor, 5-axis): 0 crit/high/med. **Forward-compat follow-up:** #1533's `finance-ui-consumer.pact.spec.ts` records mutating interactions with `x-user-roles:FINANCE` but NO `x-permissions` — harmless today (no provider verification), but a future provider-verify must inject `x-permissions` or those replays will 403.
- **2026-07-01 (session cont.) — follow-up fleet + serial merge drain.** After #1533 merged to main, ran parallel worktree agents for the remaining follow-ups. **MERGED: #1555** (#1546 platform-frontend CI-lint gap — added `lint` target so `nx affected -t lint` picks it up; `893ab29d`). **⚠️ #1491 DUP LESSON:** the compaction summary DROPPED knowledge that #1491 was already solved by **PR #1554 (permission-based, CANONICAL)** → a stream re-implemented it role-based as #1557 (DUPLICATE, now **CLOSED**). #1554 kept + being corrected: two of the invoice actions re-mapped to the stricter permission per the epic owner's ruling, and the doc-site table updated to match the code. **BEFORE re-implementing any follow-up, check memory + `gh pr list --search <issue#>` for an existing PR.** **Open VERIFIED PRs pending serial merge** (strict up-to-date enforced → serial; self-hosted runner end-of-day 17:00 UTC contention slows ci/ci): **#1554** (#1491 accounting RBAC, permission-based, being Option-1-corrected), **#1556** (#1498 commission RBAC + `resolveTraderScope` trader-scoping, role-based service-local RolesGuard; GatewayIdentityGuard guarantees req.user), **#1558** (#1511 email-delivery API in notification-service — GET list + POST retry, suppression-checked-before-lifecycle), **#1582** (#1511 email-delivery FE — DeliveriesPage + CommunicationLayout tabs; kit Badge +QUEUED/SENDING/DELIVERED), **#1584** (#1472 commission_enabled now FAIL-CLOSED, bound to tenant-service `GET /tenants/:id/features`; deleted the fail-open header source; did NOT add INTERNAL_API_SECRET to avoid #1292 crash-loop), **#1585** (#1499 pact reconcile — aligned the consumer pact to the aggregates the provider actually serves, dropping the per-row fields it had invented; **STACKED on #1556 → retarget base to main after #1556 merges**). All independently checker-verified. **NEXT: merge #1554→#1556→#1558 serially → close #1457+#1458 → retarget+merge #1585 → merge #1582+#1584 → #1463 deploy (human-gated).**
- **2026-07-02 — expert reviews + merges + new security P0s.** Ran expert pre-merge reviews (security-auditor on the RBAC PRs, code-reviewer on the API PR). **MERGED: #1554** (accounting RBAC, permission-based, Option-1 map) → **#1491 + #1457 CLOSED**; #1546 already CLOSED via #1555. The review gate caught real defects the maker + my own check missed: **#1556** review found CRITICAL report-scope leaks — an export path neither scoped nor gated, a root report path that never downgraded the lower-privilege roles, and an aggregate path left ungated → FIXED + independently re-audited **MERGE** (all 5 report read paths now scoped through ONE predicate rather than per-route checks; the list endpoint gated; 39/39). **#1558** review found a `createdTo=YYYY-MM-DD` off-by-one (dropped the end day) + test gaps → FIXED (inclusive end-of-day). **Follow-ups from the re-audit:** **#1587 (P0)** — the commission calculation controller had opt-in authorisation that was never opted in (reads, detail and state-transition endpoints alike); FIXED via **PR #1601** (stacked on #1556), independently re-audited → **MERGE**, plus a folded-in audit-attribution fix (state transitions record the acting user's id, not a `'system'` placeholder). LESSON: where authorisation is opt-in per controller, a deliberate omission and a forgotten one look identical — audit every controller in the service, not just the one under review. **#1536 (blast radius, tracked — do NOT dup):** an identity-wiring mismatch (service read identity from a request field the gateway does not populate; same class trading fixed in #1471) made the new guards fail CLOSED on real gateway traffic — correct but **INERT** until #1536 (not a leak); the report/payout paths scope via ALS `TenantEntityManager` + `req.user`, both populated → #1458 functional + **closeable**. Consolidated the identity-wiring, attribution, latent-500 and ungated-findAll items onto #1536. **#1586** — where downstream services trust gateway-injected unsigned identity headers, the chart's ingress default is the forgery surface; systemic Helm follow-up ([[platform-gateway-trust-unsigned-headers]]). **MERGED 2026-07-02 (runner freed ~09:45 after morning peak):** **#1556** (#1498, 09:52) + **#1558** (#1511 BE, 10:14) squash-merged serially; **#1458 CLOSED** (#1457 already). Stacked PRs **REBASED onto main + force-pushed** (squash-merge artifact fixed: `git rebase --onto origin/main <old-1556-tip>` in a scratchpad worktree — replays ONLY each PR's own commit; push needs `SKIP_HOOKS=1` since a fresh worktree has no node_modules for the pre-push `nx` checks; CI is the real gate): **#1585** (#1499 pact → d208cc0d, 4-file diff, MERGEABLE); **#1601** (#1587 P0 → fadab43b, 5-file diff). Both base=main now. **#1601 also still MISSING its audit-attribution (b) fix** — the folding agent hit the account monthly spend limit before committing (last commit = original RBAC only); redo the `req['userId']`→`req.user?.userId` edit manually. **#1584** (#1472 commission_enabled) is independent (off main), still open. **REMAINING:** rebase+merge #1585/#1601 (+finish #1601 b-fix) + #1584; #1582 (#1511 FE); **#1463 deploy = LAST, human-gated, after #1586/#1536.** **LESSON — strict-up-to-date merge race:** with require-up-to-date ON + a saturated self-hosted runner + an actively-moving `main` (morning peak), serial merges starve — each `update-branch` re-queues the ~15-min CI to the BACK of the queue and `main` re-stales it before it runs; DON'T re-sync in a tight loop, merge only in a quiet window; and the merge-guard hook BLOCKS `gh pr merge` inside a loop (must be a standalone `ALLOW_PR_MERGE=1` command).
- **2026-07-02 PM — requirements synthesis + design Round 1 + #1601 completed + audit workflow.** (1) **Requirements synthesis** written to `.claude/epics/platform-finance-ui/requirements-synthesis.md` (4 verticals, domain rules, RBAC, events, cross-doc conflicts). **RabbitMQ re-affirmed by user ("CRITICAL NO BullMQ")** — OD-4 hardened in the artifact; stale doc-site BullMQ/Redis-Streams/Prisma refs tracked in **#1615** (9 spots listed). (2) **Finance UX design rounds started in Claude Design** (detailed brief generated → user's clipboard; 5-round plan). **Round 1 (invoices module) exported + vendored via PR [#1616](https://github.com/initech-trading-platform/acme/pull/1616)** as `docs/platform/design/platform-v2-prototype/handoffs/finance-round1/` (verbatim bundle + `FINANCE-DESIGN-SYNC-REPORT.md`). **⚠ DESIGN-LINEAGE DIVERGENCE:** the finance design project does NOT contain trading r2–r4 (#1612) state (finance app.jsx = 0 trading-r2r4 refs, ~2k lines smaller) → vendored self-contained, wiring files reference-only; **reconcile lineages in Claude Design before Round 2**. Sync report: kit reconciliation (exists: DescriptionList/Checklist/LifecycleActionPanel/etc — verify parity; KIT-GAPs: AgeingStrip/Drawer/AttemptTimeline/AuditPanel/Byline/MonthGate/FinMoney/FxNote/panels; DataTable rowAction show/disabled/why upgrade; STATUS tokens — QUEUED/SENDING/DELIVERED already on #1582), real-code mapping (R1 = UPGRADE of #1457 pages; BankAccountsPage already exists; generate-flow + ERP dashboard = the 2 new screens), 6 API-GAPs reality-checked (missing: ERP attempt-history endpoint), **AUDITOR persona = design-ahead, needs product decision**. (3) **#1601 (b) audit-attribution fix DONE**: `dd50bc0d` — approve/pay/close record `req.user?.userId` (gateway-guaranteed) not `'system'`; 3 discriminating tests; verified locally via direct vitest in scratchpad worktree w/ symlinked node_modules (40/40); PR commented + update-branch run, CI baking. source-driven-dev breadcrumbs were wiped AGAIN → re-synced from ~/.claude (see [[feedback_source_driven_dev_breadcrumb_cwd]]). (4) **finance-bc-enterprise-audit Workflow launched** (run `wf_daaef123-735`: 4 readers → 3 lenses → adversarial verify vs 13-item enterprise checklist) — results pending → prioritized hardening plan → tracked issues under #1453 before implementation.
- **Enterprise-readiness audit (workflow `wf_daaef123-735`, 30 agents: 4 readers → 3 lenses → adversarial verify vs a 13-item enterprise checklist) → finance BC declared NOT deploy-ready despite an all-green unit suite.** LESSON: every blocking defect was masked by unit tests that mock the EntityManager, the event publisher and the gateway ("mock-deep green"). The classes that only fail on a real PG+RabbitMQ path: (1) identity wiring reading a request field the gateway never populates (fails closed for every caller); (2) an outbox entity missing from the ORM `entities[]` — every event-emitting write rolls back, so the feature silently does nothing; (3) outbox relay disabled + no consumer binding — events written, never delivered; (4) a message consumer using the GLOBAL EntityManager → MikroORM v6 `cannotUseGlobalContext` on the first message → straight to DLQ (a pattern that gets copy-pasted between workers — do NOT copy it). Root-cause enabler: the integration `*.tc.spec.ts` files were mock stubs with `it.todo`, and provider pacts asserted events no code emits — a test layer that exists but proves nothing is worse than none, because it reads as coverage. A deploy would have shipped a bounded context that authenticated, approved, calculated and delivered NOTHING — caught pre-deploy. **Filed: umbrella [#1617](https://github.com/initech-trading-platform/acme/issues/1617) + P0 pack #1618 (identity, cross-link #1536) / #1619 (outbox entity registration + platform fail-fast guard) / #1620 (relay+consumer) / #1621 (consumer EM-context primitive `withMessageContext`) / #1622 (real PG+RMQ integration harness = the VERIFIABLE GATE, maker≠checker).** Lower-severity findings (retry dead-ends, lifecycle edge cases, missing maker-checker, health-check inversion, FE auth-error conflation, CSV formula-injection) tracked under #1617 as a hardening backlog. **STRENGTHS SOUND (money precision, fail-closed tenant tier, typed RBAC, DB invariants, lifecycle SMs, outbox discipline) → hardening not rebuild.** Readiness artifact shared for the human go/no-go. Commission-side items coordinate w/ epic #1534 GREEN-reconcile (identity extraction=#1536, event-contract UNION, outbox) — cross-link NOT dup. **NEXT DECISION: build harness #1622 → land P0 pack #1618-1621 behind it (recommended) before ANY deploy.**
- **@acme/ui SYNCED to Claude Design** (project 00000000, all 57 comps) + `.design-sync/` config committed (`0124ba68` on feat/platform-finance). See [[platform-ui-design-sync]].
- **Open decisions closed before implementation (2026-06-25).** The one that still matters technically: OD-4 stack = **MikroORM + RabbitMQ** (the design-philosophy doc's Prisma/BullMQ references are stale — tracked in #1615). The rest were product/policy calls and are not part of this export.
- **Gotchas:** `DesignSync` (claude_design MCP) works ONLY in the main session, not subagents. `.claude` here is a **symlink → $PROJECT_ROOT/.claude**; FF-merging main needs `unlink .claude` → `git merge --ff-only` → restore symlink (see [[feedback_claude_symlink_blocks_git_reset]]). Branch was 0-ahead/13-behind; FF'd to `f395f84d` 2026-06-22. **2026-06-29 merged main (NOT a FF — 23 vs 12 commits, incl #1501 kit overhaul):** `rm .claude`(symlink only) → `git checkout HEAD -- .claude` (restores the 8 NON-skip-worktree files; the 859 skip-worktree incl `skills/deploy/SKILL.md` stay off-disk, fine) → `git merge` (SKIP_HOOKS; main's SKILL.md change merges INDEX-only via skip-worktree, never through the symlink to primary) → resolve 5 config/doc conflicts (`.design-sync/*` take-main, `docs/adr/README.md` union) → `rm -rf .claude && ln -s $PROJECT_ROOT/.claude .claude` re-symlink. KEY: `git commit` uses the INDEX, so the worktree's `D .claude/*` (8 non-skip files re-shadowed by the symlink) are NOT committed. After re-symlink, hooks work again (they live in the primary's `.claude/hooks`).
