---
name: Platform CNPG per-BC data tier (POC-gated, in flight)
description: Pointer to in-flight epic replacing shared Azure PG with 7 BC-aligned CloudNativePG clusters. Spec, PRD, epic, ADR-0037 (proposed). Critical for ADR-0022 work and future data-tier decisions.
type: project
originSessionId: 00000000-0000-0000-0000-000000000015
---
# Platform CNPG Per-BC Data Tier (POC-gated)

**Why:** large in-flight architectural epic (started 2026-05-08) that proposes superseding ADR-0022 via 52 POCs. Future work touching the Platform data tier, ADR-0022, ADR-0037, or shared `libs/platform/event-bus` outbox refactor must reference this.

**How to apply:** before making any Platform data-tier change, read the spec; before amending ADR-0022, check ADR-0037 amendments first.

## 2026-06-06 — full epic VALIDATION (7-agent read-only audit) → `solid_with_caveats`
Ran a 6-dimension + synthesis validation of #693 (tracking, PR/CI, charts, backup/restore, infra, cross-cutting). **Verdict: high-quality, honestly-tracked, correctly inert** — "parked/nothing deployed" verifiably TRUE; NO false-GREEN anywhere; charts schema-valid; infra safe; "backups before data" structurally enforced by the include-glob. Charts + Infra rated healthy; the other 4 dims minor_issues. **Pre-cutover punch list (none block the parked-#669 design; close before un-park):**
- 🔴 **DB_SCHEMA divergence** — runtime reads `platform`/`identity` (identity-bundle/values.yaml) but bootstrap creates `tenant`/`user` (platform-pg-bootstrap; `auth` OK) → #1116 hazard (= architect-ask #2 on #693). **SECONDARY (independent fix):** restore-verify invariant SQL (cronjob ~498-516) checks both `user`+`identity` for schema-3 but only `tenant` (NEVER runtime `platform`) for schema-2 → that guard silently no-ops; add the `platform` check (mirror the both-names pattern) so it can harden to hard-fail.
- 🔴 **#1152 has the SAME `.gitleaks.toml` conflict I fixed on #1146** (CONFLICTING vs main #1160) — comment-only/semantic no-op but it's the STACK HEAD → blocks the chain until rebased + re-stack #1156/#1146.
- 🟠 tracking hygiene (overstates "52 POCs accounted"): epic's #1 master spec `docs/superpowers/specs/2026-05-08-…design.md` is DANGLING on main (only commit 2adaaada, not an ancestor); ADR-0037 base ledger frozen at 5 stale "pending" rows while evidence lives in 14 amendments (keep Status=Proposed, fix ledger honesty); 6 Category-B POCs (B2/B3/B5/B6/B7/B9) have no amendment (gate #8); architecture.md still says 7-clusters/session-mode/Fluent-Bit (ratification → 6/transaction/OTel).
- 🟡 **2nd federated cred is DOC-ONLY, not in TF** — cnpg-backup module `for_each=var.clusters` only binds `identity-db`; restore-test SA (`…:identity-db-restore-test`) has NO cred → restore pods 403 (fail-safe, blocks first run). Codify in TF before activation.
- 🟡 **"inert by location" is imprecise:** only #1146 is inert-on-merge; **#1152/#1156 are LIVE-ON-MERGE** (each adds/renames a `charts/argocd/*.yaml` App that root-app auto-discovers) — which is exactly why they're correctly draft+held. Their headers already say "live-on-merge".
- 🟡 vacuous-pass: on empty pre-migration DB, GREEN = "restorable+promoted" NOT "data preserved" (amcheck iterates 0 indexes, per-schema invariants skip); gate-item-8 hard-fail conversion MUST land WITH the first real migration.
- LOW/INFO: stale CNPG 1.24 comments vs installed 1.29.1; add `\dx amcheck` loud-assert; UAMI hardcoded not consumed from TF output; identity-backup-wiring.md describes a spec.plugins patch #1144 already inlined; restore-verify needs an explicit gated glob-extension step between #1156→#1116; provisional pool sizing pending P3 #875.

**FIXES APPLIED 2026-06-06 (parallel, "do all in parallel"):**
- ✅ 🔴 **restore-verify `platform` schema + amcheck loud-assert** → committed to #1146 (`b282a19c`). Per-schema invariant now checks both `tenant`+`platform` (both-names pattern); `\dx amcheck` assert added.
- ✅ 🟡 **2nd federated credential (restore-test SA)** → NEW PR **#1163** (draft, HELD): `azurerm_federated_identity_credential.cnpg_restore_test` added to cnpg-backup module (pure addition, existing UAMI/fed-cred UNtouched — verified). Declares-only; applies on a coordinated gated `terraform apply`.
- ✅ 🟠 **doc tracking-hygiene** → PR **#1164 MERGED to main** (`1a5f23f3`, --admin docs-only path-skip): ADR-0037 ledger reconciled to the 14-amendment trail (Status=Proposed preserved), dangling master-spec repointed to architecture.md, ratified rewords (6 clusters/am-13 D1-1, transaction-mode/am-13 D2-1, OTel/am-8), Category-B down-scope note. **This DOWNGRADES the two 🟠 tracking-hygiene HIGHs (dangling spec + frozen ledger) — now resolved on main.**
- ⏸️ **DEFERRED: #1152 stack-rebase** (gitleaks conflict) — rots before un-park (re-conflicts as main drifts); do just-before-un-park, same as #1146's fix. The LOW glob-citation refresh + identity-backup-wiring.md nit fold into that rebase.

## 2026-06-03 — final 3 un-authored waves MERGED to main (inert)

All remaining wave tranches are now authored as **inert POC slices**, expert-reviewed, fixed, CI-green, and **MERGED to main** (squash, `--admin`; verified on `origin/main` 2c3559ee: amendments 10/11/12 + files present; branches deleted). Same inert model as D/F/G/C: drills `scripts/drills|perf/*.{sh,test.sh}` dual-mode (`<X>_DRY_RUN`/`<X>_LIVE`, no-mode→exit 2, **fail-closed** LIVE), validator cells `test/poc/<poc>/` (vitest, scoped config, no project.json), candidate charts unreferenced by ArgoCD, `workflow_dispatch`-only candidate workflows, one ADR-0037 amendment per wave. **Nothing deployed.**

- **Wave I — Architecture validation (#702) → PR #1100 MERGED**, ADR amendment **12**. CQRS-projection-not-FDW federation design + pure `apps/platform/reporting-service/src/bootstrap/replay-protocol.ts` (sort-first high-water-mark idempotent reducer; NOT wired to DI) + i1 no-cross-cluster-SQL validator cell + bootstrap drill. Live e2e drill **deferred to M5**. Verified: reporting-service bootstrap spec 15/15, i1 cell 20/20, lint+typecheck clean.
- **Wave E — Migration mechanics (#701) → PR #1101 MERGED**, ADR amendment **11**. Drills pg-dump-cutover / schema-drop-preflight / rabbitmq-drain-gate / audit-jsonb-identifier-scan + candidate backup-verify CronJob + Mimir azure-blob values + `workflow_dispatch` forward-compat matrix + RMQ declarative-config runbook.
- **Wave H — Operational readiness (#699) → PR #1102 MERGED**, ADR amendment **10**. Harnesses pgaudit-overhead / sync-commit-bench / secret-rotation-full / aks-node-drain-alert-quietness + 11(+1) runbooks + tenant-context PrometheusRule/AlertmanagerConfig + `workflow_dispatch` Phase A→D gate + H8 migration-assertion validator cell.

**ADR-0037 amendment map** (avoid collisions): 1–6 + wave-0 on `main`; **7=F-wave (#694/PR #1090), 8=G-wave (#697/PR #1092), 9=C-wave (#698/PR #1091)** on their open PR branches; **10=H, 11=E, 12=I** on the new PRs. Gaps are intentional.

**Process notes (this tranche):** each wave got a **13-agent expert-review panel** (correctness/architecture/tests/+security) → all "ship-with-fixes" → a **fix pass** applied every must/should-fix (LIVE paths made honestly fail-closed; real 6 bundle namespaces `platform-{identity,trading,finance,comms,ops,ai}`, NOT accounting/commission/inventory which are *services*). The independent main-thread gate caught real defects agents + in-workflow reviewers missed: 2 reducer-test bugs (I), an nx.json `eslint:lint` strip (E, same as G-wave), a single-agent overrun on the largest wave (H → split 3 ways). See [[feedback_review_subagents_mutate_git_state]] (the diff-file review fix) and [[feedback_worktree_agent_scope_drift]].

**Sibling F/G/C also expert-reviewed + fixed + MERGED 2026-06-03.** Same 12-reviewer panel + fix pass on #1090 (F)/#1092 (G)/#1091 (C), all squash-merged `--admin` on green CI (verified on `origin/main` 49ee3461). **ADR-0037 amendments 1–12 are now ALL on main** — every CNPG epic wave (B1/D2/D3/D4/D5/D7/F11 base + F/G/C/H/E/I) has its inert POC slice merged. Nothing deployed; activation (operator install + live drills + ratification of #1089) remains user-gated. Real defects fixed: G `cnpg-audit-worm-failover.sh` `count_blob_lines` counted blob OBJECTS not records (zero-loss assertion was meaningless) + the g1 `log-parameter-on` validator missed js-yaml's `on`→boolean-`true` coercion (GDPR guard bypassable) + LIVE stubs → fail-closed; F WAL alert `for: 5m`→`1m` (boundary race may never FIRE) + break-glass key `umask 077`/cleanup-trap + cert-manager restore trap + webhook caBundle patch; C C4 GDPR erasure missed the `auth.password.reset-requested` payload-email path + advisory-lock regex false-negative. Verified: F 5 drill contracts pass; G drill + g1 cell 22/22 + terraform valid; C drill + 6 cells (77 tests). **Known out-of-scope follow-up** (C agent surfaced, correctly did NOT edit): `libs/platform/event-bus/src/lib/outbox-reaper.ts:111` JSDoc claims `pg_try_advisory_xact_lock` multi-replica coordination the reaper does not actually perform — a stale shared-lib comment, separate from the CNPG epic.

## Locations

- **Spec (master technical reference)**: `docs/superpowers/specs/2026-05-08-platform-database-per-service-k8s-design.md` — 52 POCs with pass/fail criteria, wave ordering, scope-cut triggers.
- **PRD**: `.claude/prds/platform-cnpg-per-bc-data-tier.md`
- **Epic**: `.claude/epics/platform-cnpg-per-bc-data-tier/epic.md`
- **Architecture**: `.claude/epics/platform-cnpg-per-bc-data-tier/architecture.md`
- **Parallel sessions runbook**: `.claude/epics/platform-cnpg-per-bc-data-tier/parallel-sessions.md`
- **Tasks**: `.claude/epics/platform-cnpg-per-bc-data-tier/001.md` … `010.md`
- **ADR**: `docs/adr/0037-poc-gated-cnpg-per-bc-data-tier.md` (Proposed; supersedes ADR-0022 contingent on POC outcomes)

## Key decisions (provisional, gated by POCs)

- 7 BC-aligned CloudNativePG clusters in dev AKS (NOT 13 per-service — preserves trading↔inventory advisory-lock saga)
- Pool + Postgres RLS multi-tenancy (defense-in-depth on top of MikroORM filter)
- Per-service outbox tables (`<service_schema>.outbox_entry`); shared library schema-parameterised
- Trading→User cross-DB query replaced with event-sourced projection (NEW pattern)
- pgaudit csvlog → Fluent Bit → Azure Blob WORM (Locked + 7y legal hold)
- Backup-based DR via Azure Blob GRS, no active warm standby
- 8-11 month honest timeline for 2-person team (5-7 months with max parallelisation)

## Status (as of 2026-05-08)

PM ceremony progress:
- ✅ Two architecture boards reviewed (premise + design)
- ✅ Spec written and committed (commit 2adaaada)
- ✅ ADR-0037 drafted as Proposed (commit 7413e059)
- ✅ PRD, Epic, Architecture, 10 task files, parallel-sessions runbook all in `.claude/`
- ⏳ Next: tests-generate, readiness-check, epic-sync to GitHub

## Recent progress (2026-05-31)

POC B1 (multi-tenancy RLS+PgBouncer) hardening landed:
- **#867 / PR #1012 MERGED** (`fab64f21`) — B1 wave-2: scram-sha-256+auth_query re-run, prepared-statement reuse cell, routing assertion, per-cell isolation, image-bump CI policy.
- **#869 / PR #928 MERGED** (`db81517d`) — ALS tenant-context propagation chaos coverage + ErpTokenRefreshScheduler `withTenantContext` wrap. (`withTenantContext(TenantStore|string, fn)` now lives in `libs/platform/mikro-orm/src/lib/tenant-context.ts`.)
- **#875 / PR #1037 MERGED 2026-06-01 (`ddab3bec`)** — POC B1 P3 (pinning saturation) execution plan (`docs/cnpg/poc-b1-p3-execution-plan.md`) + 3 buildable goal-2 retry cells GREEN: R1 pg.Pool reconnect-after-kill, R2 PgBouncer `server_lifetime` recycle (idle-timeout dropped so recycle is lifetime-attributable), R3 app-layer `40001` retry (deterministic `pg_blocking_pids` wait). Shared harness helpers `waitForBackendRotation`/`waitUntilBlocked` + `killBackend`. Full b1 suite 13 files / 50 tests. Reviewed via TWO boards: docs (8/20 applied) + code (4/39 applied — all flake/false-green: R2 idle-vs-lifetime attribution, R3 sleep-race, R3 dirty-conn, R1 kill-race). **#875 itself stays OPEN** — owner assignment + the gated 200-client saturation/failover/R4 run still pending (P3 is NOT yet GREEN). Next epic step is **#700 (D2 Pooler sizing)** — the P3-gate trigger this work prepares for.

## Recent progress (2026-06-01) — POC D2 (PgBouncer Pooler sizing)

**#700 AC:2 / POC D2 — PR [#1045](https://github.com/initech-trading-platform/acme/pull/1045) OPEN** (branch `feat/cnpg-d2-pgbouncer-pooler-sizing`, commit `29e652c0`). Built in the `acme-platform-microservices-databases` worktree (where the B1 POC deliverables live; the epic *planning* docs `.claude/epics/platform-cnpg-per-bc-data-tier/` + PRD + architecture.md live in the PRIMARY `acme` worktree).

- **Headline decision (recommended, AskUserQuestion-confirmed by Milos): `poolMode: transaction` default + per-BC `session` opt-out — AMENDS architecture D13** (which said session-day-1 "preserves advisory locks"). D13's premise is empirically moot: codebase audit confirms **every advisory lock is `pg_try_advisory_xact_lock`** (xact-scoped) — `outbox-relay.ts:408`, 3 ERP schedulers; OutboxReaper has none; no `LISTEN`/`NOTIFY`, no `prepare:true`. Reconciles the D13-vs-B1-results-doc conflict (B1 doc already framed txn-default + session-opt-out). **Needs architect ratification before wiring into GitOps.**
- **3 Testcontainers cells, GREEN ×3** (`test/poc/d2-pgbouncer-pooler-sizing/`, reuse the B1 harness): A xact-lock safe under txn mode (mutual-exclusion + auto-release at COMMIT, asserted via `pg_locks`); B session-lock correct under session mode; C session-lock LEAKS past COMMIT + reentrant over-grant under txn mode (negative control). NB: single-bigint advisory lock shows in `pg_locks` as classid=key>>32, objid=key&0xffffffff, **objsubid=1** (confirmed empirically).
- **N1 finding:** `4× pods × pool-max` is a *session-mode* budget; under txn mode it governs the pooler `max_client_conn`, while backend `max_connections` (Cluster CR / D1) is ~4–6× smaller (Identity ~100 vs ~360). Recommends clarifying N1 wording in the ADR.
- Deliverables: `docs/cnpg/poc-d2-pgbouncer-pooler-sizing-results.md`, `docs/adr/0037-amendment-2-d2-pgbouncer-sizing.md` (conditional), candidate `charts/databases/identity-pooler.yaml` (**INERT** — both ApplicationSets use explicit `list` generators, no dir-glob, so it is NOT deployed; schema verified vs CNPG 1.24), `.github/workflows/poc-b1-matrix.yml` extended to run D2 cells.
- **D2 is NOT GREEN/closed.** Gated on: P3 (#875) for final `default_pool_size` (candidate 25 provisional); D1 `identity-cluster.yaml` (sets backend `max_connections`, referenced as `identity-db`); architect sign-off on the D13 amendment. **#700 stays OPEN** (D1, D3–D7, F11 + P3 remain).

Key correctness fact verified 2026-05-31: **only `inventory-service` sets `enableRelay: true`** (other 9 `false`); even it uses `pg_try_advisory_xact_lock` (xact-scoped → no pin) → **active P3 blast radius = zero services**. No current code triggers the P3 gate (no session advisory locks / LISTEN/NOTIFY / `prepare:true`). P3 activates on the first Wave-2 pinning trigger or D2 #700 (Pooler sizing).

## Recent progress (2026-06-01) — POC D1 (CNPG identity-db Cluster CR)

**#700 (Cluster lifecycle) / POC D1 — PR [#1053](https://github.com/initech-trading-platform/acme/pull/1053) OPEN**, branch `feat/cnpg-d1-identity-cluster` **stacked on #1045 (D2)** (base = the D2 branch; retargets to main after #1045 merges). Commit `f2d3df59`.

- Deliverables (both **INERT** — not referenced by any ArgoCD ApplicationSet): `charts/databases/identity-cluster.yaml` (CNPG `Cluster` CR — 3-instance HA, PG **16.14** pinned, `max_connections: "100"` from the D2 txn-mode handoff, async repl, switchover upgrades, HA replication slots; **no hand-written PDB** — CNPG auto-manages two PDBs, `enablePDB` default true) + `docs/cnpg/poc-d1-identity-cluster-results.md` (design + appendices + gated validation plan).
- **Resolves the two decisions D2 deferred to D1** (both flagged for architect ratification): (1) **6 bundle-aligned clusters** (5 bundles + standalone ai), NOT the 7-BC framing in ADR-0013/0037 — changes the ADR-0037 cluster count; (2) **database `platform` with per-service schemas** (`auth`/`tenant`/`user` per ADR-0013), NOT per-service DBs — keeps bundle connection strings host-only on migration.
- **Backup deferred to the Barman Cloud PLUGIN** (Appendix D), NOT in-core `barmanObjectStore`. Key fact (caught by review): the in-core path needs the **deprecated `system` operand image** (only flavor bundling `barman-cloud`); the chosen non-deprecated **`standard`** image is barman-cloud-free. NetworkPolicy/cert-manager/ESO designed-but-deferred (Appendices A–C; CNPG self-signed CA covers TLS for the POC).
- **D1 NOT GREEN** — operator install + live RTO≤60s failover / backup-PITR / NetworkPolicy drills are **deploy-gated** (explicit auth required; dev-platform untouched). Also gated on P3 (#875) for the `max_connections`↔`default_pool_size` coupling + architect sign-off. **#700 stays OPEN** (D3–D7, F11 + live validation remain).
- Review: 4-lens expert board + adversarial per-finding verification → 20 findings, **2 confirmed + 1 partial**, 17 refuted. Both real defects fixed pre-commit (the `standard`-image barman-cloud CRITICAL; a schema-name misattribution where I'd conflated the runtime `DB_SCHEMA` env `auth/platform/identity` with the actual bootstrap schemas `auth/tenant/user` — that env-vs-bootstrap divergence is itself a flagged pre-existing reconciliation item).
- **CNPG facts banked** (CNPG 1.24, verified vs docs): operator default operand is PG 17.x (must pin PG16); `system` images + in-core barman-cloud are deprecated → use `minimal`/`standard` + Barman Cloud plugin; CNPG auto-creates 2 PDBs per cluster (`enablePDB` default true); ScheduledBackup uses a 6-field seconds-leading cron; cert-manager v1.20.2 at ArgoCD sync-wave -15; ESO `ClusterSecretStore` `azure-keyvault` is `external-secrets.io/v1`/`ManagedIdentity`.

## Recent progress (2026-06-01) — POC D3 (ArgoCD GitOps wiring for CNPG)

**#700 (Cluster lifecycle) / POC D3 — PR [#1055](https://github.com/initech-trading-platform/acme/pull/1055) OPEN**, branch `feat/cnpg-d3-argocd-gitops` **stacked on #1053 (D1) → #1045 (D2)**. Commit `71846424`.

- Deliverable (INERT): `charts/databases/cnpg-gitops.yaml` = 3 ArgoCD resources — `platform-infra` AppProject (scoped clusterResourceWhitelist: CRD/ClusterRole/Binding/Mutating+ValidatingWebhookConfiguration/Namespace) + `cnpg-operator` Application (CNPG operator chart, ns `cnpg-system`, sync-wave **-8**) + `cnpg-clusters` Application (wave 0, syncs the D1/D2 `*-cluster.yaml`/`*-pooler.yaml` via `directory.include` glob) — plus `docs/cnpg/poc-d3-argocd-cnpg-gitops-results.md`.
- **Inert by LOCATION**: ArgoCD manifests but under `charts/databases/`, which root-app (`recurse:false`) + the list-generator ApplicationSets never pick up. Promotion = move to top-level `charts/argocd/cnpg.yaml`. Operator install + the 2 live drills (failover-no-flap, manual-edit-detected) are deploy-gated.
- **D3 crux**: a NARROW annotation-scoped `ignoreDifferences` (only the operator `cnpg.io/*` write-back annotations on the Cluster CR) + `ServerSideDiff` + `RespectIgnoreDifferences` threads BOTH ACs — failover doesn't flap OutOfSync, yet a manual `kubectl edit` of a real spec field IS still detected. A broad `managedFieldsManagers` wildcard would mask the latter (fails AC2).
- **Flagged for ratification**: bump epic CNPG baseline **1.24 → 1.29.1** (no Helm chart ships 1.24.2–1.24.4; chart 0.22.1=1.24.1 is the last 1.24 chart; 1.24 near-EOL). v1 API stable → D1/D2 CRs re-validate as a no-op (confirm).
- Review: 4-lens board + adversarial verify → 19 findings, **2 confirmed (no-defect), 3 partial, 14 refuted**. All 3 partials fixed pre-commit.

**ArgoCD/CNPG facts banked (verified vs docs/source 2026-06-01):**
- **`ServerSideDiff=true` in `syncPolicy.syncOptions` is a SILENT NO-OP** — it's a COMPARE option, enabled via `argocd.argoproj.io/compare-options: ServerSideDiff=true[,IncludeMutationWebhook=true]` annotation OR global `argocd-cmd-params-cm` `controller.diff.server.side`. **The repo's `root-app.yaml`+`arc.yaml` have this latent bug** (their `ignoreDifferences` lists carry the load; consistent with #1043's "ServerSideDiff alone didn't clear the loop"). Worth a separate cleanup PR.
- `RespectIgnoreDifferences=true` IS a valid syncOption (the #1043 lesson — stops a sync re-asserting ignored paths).
- CNPG failover/switchover mutates ONLY `.status` (ArgoCD doesn't diff status by default). `cnpg.io/pgControldata` is written to **PVCs/VolumeSnapshots, not the Cluster CR**; `cnpg.io/poolerSpecHash` is on the **child PgBouncer Deployment, not the Pooler CR** (so a Pooler ignoreDifferences entry is a no-op). `cnpg.io/operatorVersion`+`cnpg.io/hash` on Cluster reconcile; `cnpg.io/reloadedAt`/`kubectl.kubernetes.io/restartedAt` on user reload/restart.
- CNPG operator chart: `cnpg/cloudnative-pg` from `https://cloudnative-pg.github.io/charts`; chart 0.28.2→appVersion 1.29.1 (current), 0.22.1→1.24.1; ns `cnpg-system`. Barman Cloud plugin: chart `plugin-barman-cloud` 0.6.0→v0.12.0 (separate operator Application; needs cert-manager).
- Existing operators use `project: default` (unrestricted); only workload apps use `platform-services` (clusterResourceWhitelist=Namespace only). Wave ladder: cert-manager -15, reloader -12, argo-rollouts/messaging-topology -10, arc -5, namespaces -1, standalone 0, bundles 1/2, grafana 5.

## Recent progress (2026-06-01) — POC D7 (CNPG bootstrap-mode validation)

**#700 (Cluster lifecycle) / POC D7 (AC-7) — PR [#1059](https://github.com/initech-trading-platform/acme/pull/1059) OPEN**, branch `feat/cnpg-d7-bootstrap-mode` **stacked on #1055 (D3) → #1053 (D1) → #1045 (D2)**. Commit `130809c0`.

- Deliverable: an **executable static lint** `test/poc/d7-bootstrap-mode/` (`bootstrap-mode-validator.ts` + spec, pure `fs`+`js-yaml`, **no Docker**, **27 tests GREEN ~0.1s**) enforcing **anti-pattern 12** over `charts/databases/*-cluster.yaml`: `bootstrap.recovery` ⇒ a declared reachable source (`backup.name` | `volumeSnapshots.storage.name` | `source`→`externalClusters[]` store, **version-aware** `barmanObjectStore` 1.24 / `plugin` 1.29); plus explicit declaration, single-method mutual exclusivity, and `initdb.import`/`pg_basebackup` source presence. Unit fixtures (each anti-pattern trips its specific rule code) + a regression guard over the real manifests (`identity-cluster.yaml` passes). Wired into `poc-b1-matrix.yml` (run step + path filter; **non-required CI signal**, NOT a merge gate — promotion to `ci-gate` is a follow-up). Plus `docs/cnpg/poc-d7-...md` + `docs/adr/0037-amendment-3-d7-bootstrap-mode.md`.
- **Test home deviation** (documented): under `test/poc/d7-bootstrap-mode/` (matches the b1/d2 standalone-vitest convention), NOT the 700-analysis's `charts/databases/test/cnpg-cluster.spec.ts` — a hand-rolled Nx project would break the "never manually scaffold Nx" rule.
- **Ratification item**: AC-7's "restored → initdb + post-init pg_restore Job" wording is NOT the CNPG-native pattern — `bootstrap.initdb.import` (microservice) runs `pg_dump`→`pg_restore` INSIDE the operator bootstrap (no separate Job). Surfaced in the ADR; NOT edited in `700.md` (File Modification Rules). Identity's real cutover = `initdb.import` from legacy Flexible Server `platform` (deploy-gated; blocked on the D1 schema-name divergence + roles-not-imported).
- Review: 4-agent research board (CNPG facts vs official 1.24+1.29 docs) + 4-lens adversarial board (9 findings → 3 confirmed + 2 partial, all fixed). **The board caught a real CNPG misattribution I'd carried**: the `WAL archive check failed … Expected empty archive` / "Setting up primary" error is the **reused-destination** safety check (`cnpg.io/skipEmptyWalArchiveCheck`), NOT recovery-from-missing-source — which has **no admission check** and fails later during `barman-cloud-restore`. Also hardened method detection (`initdb: false` rejected) + added `nonEmptyString` boundary fixtures.

**CNPG bootstrap facts banked (verified vs official 1.24+1.29 docs 2026-06-01):**
- **3 top-level, MUTUALLY-EXCLUSIVE bootstrap methods**: `initdb` (empty greenfield; default), `recovery` (restore base-backup+WAL; DR/PITR), `pg_basebackup` (physical streaming clone, same major+arch). `initdb.import` is a **sub-mode of `initdb`**, NOT a 4th method. Only one method per Cluster (1.29 = explicit validation error; 1.24 implicit).
- **`initdb.import`** (`microservice` | `monolith`) = logical `pg_dump`→`pg_restore --no-acl --no-owner` from an `externalClusters[]` source; **offline/one-shot at bootstrap**, NOT continuous replication; dest major ≥ source major (PG16→PG16 OK). microservice = 1 DB, **no roles**, supports `postImportApplicationSQL`; monolith = many DBs + roles, no `postImportApplicationSQL`, SUPERUSER stripped. (1.24 dumps `-Fc`; 1.29 `-Fd`.)
- **`recovery` source paths**: `recovery.backup.name` (in-cluster `Backup`, no externalClusters needed) | `recovery.volumeSnapshots.storage.{name,kind,apiGroup}` | `recovery.source`→`externalClusters[]` with `barmanObjectStore.destinationPath` (1.24) OR `plugin.name: barman-cloud.cloudnative-pg.io` (1.29; **native object-store recovery deprecated as of 1.26** → plugin).
- **CNPG does NOT validate recovery-source existence at admission** — missing/empty source fails later during `barman-cloud-restore`, no canonical error (burden on author → static lint is the earliest guard). Distinct from the empty-WAL-archive overwrite guard.
- Doc host path: 1.24 = `cloudnative-pg.io/documentation/1.24/…`; 1.27/1.29 = `cloudnative-pg.io/docs/<ver>/…`.

## Recent progress (2026-06-01) — POC D4 (custom CNPG pg_partman image pipeline)

**#700 (Cluster lifecycle) / POC D4 (AC-4) — PR [#1067](https://github.com/initech-trading-platform/acme/pull/1067) OPEN**, branch `feat/cnpg-d4-custom-image` **stacked on #1059 (D7) → #1055 (D3) → #1053 (D1) → #1045 (D2)**. Commit `9e08f1b0`. Built in the `acme-platform-microservices-databases` worktree.

- Deliverables (all **INERT**): `apps/cnpg-image/{Dockerfile,.trivyignore,project.json}` (thin operand `FROM ghcr.io/cloudnative-pg/postgresql:16.14-standard-bookworm@sha256:a650f171…` + `postgresql-16-partman`, `USER 26`, no ENTRYPOINT); `.github/workflows/cnpg-image-build.yml` (**workflow_dispatch-only**, `push` input default **false** → builds/pushes nothing on merge; build(load)→Trivy HIGH+CRITICAL gate→gated push→**cosign keyless** sign); `test/poc/d4-image-pipeline/` (pure `fs`+`js-yaml` validator + **34 tests, no Docker**) wired into `poc-b1-matrix.yml` (non-required signal); `docs/cnpg/poc-d4-...md` + `docs/adr/0037-amendment-4-d4-custom-image.md`.
- **Decision surfaced (architecture.md §2 "custom CNPG image vs drop pg_partman")**: pg_partman is in NO CNPG flavor → custom image is the only PG16 route IF kept; the alternative is native PG16 partitioning + a k8s CronJob (pg_cron ALSO absent → dropping ≠ "use pg_cron"). Ships the KEEP-branch pipeline; choice needs the Platform BC partitioning requirement to settle. Also surfaced: the new `sigstore/cosign-installer` dependency (+ `verified-versions.md` entry); AC-4's "tracks upstream" schedule deferred (deploy-gated). NOT edited in `700.md`.
- **`project.json` gotcha**: `apps/cnpg-image/` MUST carry a `project.json` (Nx auto-discovers `apps/*` → missing one fails `nx graph`/`format` workspace-wide). Used a **target-less** stub → invisible to `nx affected -t build|lint|test|typecheck` (the `@nx/docker` plugin infers `docker:build`/`docker:run`, which CI never invokes) → CI-safe. Mirrors `apps/runner-image/project.json`.
- Review: 5-agent research board (CNPG/pg_partman/signing facts vs official docs) **+ independent GHCR registry corroboration** (the published `16.14-standard-bookworm` build history shows `EXTENSIONS=postgresql-16-pgaudit postgresql-16-pgvector postgresql-16-pg-failover-slots` — partman/pg_cron/barman all absent) + a 6-reviewer adversarial board (58 findings → **5 confirmed, all fixed**). Confirmed fixes: lint hardened to an **allowlist** inertness check (catches `workflow_run`/`repository_dispatch`, not just schedule/push/PR) + a **non-vacuous push gate** (rejects `if: 'true'`/`always()`, requires referencing the push decision, detects `build-push-action push:true`); corrected a backwards `js-yaml on:` comment (js-yaml keeps `on` as the string key — it does NOT coerce on/off/yes/no to bool; that's PyYAML); doc accuracy re the committed `project.json`.

**CNPG custom-image / pg_partman / signing facts banked (verified vs official docs + corroborated 2026-06-01):**
- **pg_partman is bundled in NO CNPG flavor** (`docker-bake.hcl`: standard extras = pgaudit, pgvector, pg-failover-slots; upstream postgres-containers#115 "Consider adding pg_partman" CLOSED **not planned**). `pg_cron` also absent. The pluggable extension-image-volume alternative needs **PG18 + CNPG 1.27+ + K8s 1.33+** (out of scope for PG16/1.24-1.29) → a **derived image** is the only route.
- **Custom image build shape**: `FROM ghcr.io/cloudnative-pg/postgresql:<major.minor>-<flavor>-<distro>@sha256:…` → `USER root` → `apt-get update && apt-get install -y --no-install-recommends postgresql-<major>-<ext>` → clean → `USER 26` (the postgres uid, `usermod -u 26`). **No ENTRYPOINT** (CNPG's instance manager overrides it). PGDG apt repo is **already in the base** (but the base clears `/var/lib/apt/lists/*`, so `apt-get update` is required first). Tag MUST start with the PG major; **`latest` is rejected** by the operator.
- `standard` is **barman-cloud-free** (only deprecated `system` bundles it; minimal/standard pair with the Barman Cloud plugin). On **v1.24** a valid operand still needs barman-cloud binaries in PATH for the *in-core* path → use the plugin with standard (the D1 decision); **devel/1.29 drops** the barman-cloud requirement.
- **`spec.imageName` supports digest pins** (`<image>:<tag>@sha256:…`). **ImageCatalog/ClusterImageCatalog** (since v1.23, in 1.24+1.29) map major→image via `spec.imageCatalogRef` (alt to imageName); CNPG's own catalog images are digest-pinned + signed + SBOM/provenance. Operand image change → rolling update (replicas first, primary last, per `primaryUpdateStrategy/Method`).
- **Signing**: cosign **keyless** = `sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2` (verified via git-refs API; v4.1.x installs cosign v3.x), OIDC `id-token` (no key), signature stored as an OCI referrer (ACR supports referrers except CMK registries), digest logged to public Rekor. = fewest-new-deps true signature (1 action). ACR Notation = most deps (notaryproject/notation-action + AKV cert). `docker/build-push-action` `provenance:`/`sbom:` = 0 new actions but attestations, NOT signatures.
- **`apps/runner-image/` is the in-repo precedent** for a custom-image build (workflow_dispatch+schedule+PR, Trivy-gate-before-push, OIDC via `./.github/actions/azure-login-oidc` as `AZURE_CLIENT_ID_IMAGE_BUILD` (federated for `refs/heads/main` only), immutable `<date>-<sha>` tag). SHAs to mirror: buildx `d7f5e7f5… v4.1.0`, build-push `f9f3042f… v7.2.0`, trivy `ed142fd0… v0.36.0`, checkout `de0fac2e… v6.0.2`. **verified-versions.md is STALE** (lists buildx v3.7.1/build-push v6.9.0) — mirror the live runner-image SHAs, not the table.

## Recent progress (2026-06-01) — POC F11 (per-BC Redis Sentinel values)

**#700 AC-8 / POC F11 (closes PRD F11) — PR [#1071](https://github.com/initech-trading-platform/acme/pull/1071) OPEN**, branch `feat/cnpg-f11-redis-values` **stacked on #1067 (D4) → #1059 (D7) → #1055 (D3) → #1053 (D1) → #1045 (D2)**. Commit `1c6af111`. Built in the `acme-platform-microservices-databases` worktree.

- Deliverables (all **INERT** — `charts/infrastructure/` is directory-synced by NO ArgoCD Application; `platform-root` syncs only `charts/argocd/` recurse:false + both ApplicationSets use explicit `list` generators, so a values file there with no consuming Application deploys nothing — same inert-by-non-reference model as identity-pooler.yaml, but at the AC's LITERAL path, no relocation needed): `charts/infrastructure/redis-{identity,trading,finance,gateway,ai}.yaml` (Bitnami `redis` Sentinel values overlays), `charts/infrastructure/redis-eso.yaml` (5 ExternalSecrets, mirrors rabbitmq-eso.yaml), `.github/workflows/vendor-redis-image.yml` (workflow_dispatch-only, mirrors vendor-rabbitmq-image.yml), `test/poc/f11-redis-values/` (pure fs+js-yaml lint, **44 tests, no Docker**) wired into `poc-b1-matrix.yml` (non-required signal), `docs/cnpg/poc-f11-...md` + `docs/adr/0037-amendment-5-f11-redis.md`.
- **Redis-using set = 5 namespaces** (identity[auth/tenant/user], trading[trading/inventory], finance[accounting/commission], gateway, ai) — NOT the AC's abstract 7-BC list `{…,comms,commission,compliance,platform}`. **comms+ops set NO `REDIS_URL`** (confirmed in bundle values + Terraform `services_with_redis`/`redis_db` map: 9 services, gateway=0…commission=8). Same deployed-reality reconciliation as D1. **700.md NOT edited** (File-Mod rules).
- **Migration reality (architecture's "Bitnami Redis chart (existing) — kept" is WRONG)**: there is NO in-cluster Redis today — all 9 services share ONE **Azure Cache for Redis** (`rediss://:<key>@<host>:6380/<db-index>`, per-service logical DB index — `infra/modules/platform-key-vault-secrets/locals.tf:125`). F11 MIGRATES to per-namespace in-cluster Sentinel; co-located services share their namespace's Redis (isolation via DB-index / B5 key-prefix). Activation re-composes `REDIS_URL` to a NEW form `redis://:<pw>@<svc>.<ns>.svc:6379` (scheme rediss→redis, port 6380→6379, drop `/<db>`) — a template change, not a host swap.
- **Dev-scheduling caveat (ratification flag)**: 5 namespaces × 3-node Sentinel = 15 redis pods won't fit the capped dev node pool (the uksouth 10/10 vCPU quota that forced rabbitmq to `replicaCount:1`). Values declare the AC-faithful `replica.replicaCount:3` production intent; dev activation must reduce/gate.
- Review: 3-agent research board (Bitnami schema / free-tier reality / alternatives, vs official docs) + 6-reviewer adversarial board (20 findings → **5 confirmed + 4 partial, all actioned**; zero findings on YAML/inertness/vendor-workflow). Hardened the lint (repository+metrics-image checks, quorum-majority, auth.enabled, container-port, ESO remoteRef.key/target.name, on-disk-set==BCS glob guard) + fixed the REDIS_URL activation recipe. 34→44 tests.
- **F11 NOT GREEN/closed.** Ratification forks surfaced: (1) keep-Bitnami vs pivot (hand-rolled `redis:7` StatefulSet+Sentinel / official Valkey chart once Sentinel #22 ships); (2) 5-namespaces vs 7-BC framing; (3) dev `replicaCount`; (4) activation deps (vendored ACR digests, verified-versions.md chart entry, `platform-redis-<bc>-password` KV secrets). Deploy-gated activation (vendor images, KV secrets, chart Applications, REDIS_URL recompose) NOT performed; dev-platform untouched.

**Bitnami Redis / free-tier-image facts banked (verified vs official docs 2026-06-01):**
- **Bitnami free-tier 404 (Secure Images, 2025-08-28)**: ALL pinned `docker.io/bitnami/*` tags moved to the frozen archive `docker.io/bitnamilegacy/*`; the free `docker.io/bitnami/<x>` namespace now publishes ONLY `latest` → chart-default pinned tags 404 (ImagePullBackOff). Applies to redis, redis-sentinel, redis-exporter. **Repo's answer (mirrors rabbitmq #959/#765/#766): vendor the bitnamilegacy tag into `developmentacmeacr` via `docker buildx imagetools create` (preserves digest), digest-pin, set `global.security.allowInsecureImages: true`** (the chart rejects any non-bitnami registry, our ACR included).
- **Bitnami redis Sentinel 3-node schema** (chart 25.5.3 published / 23.1.1 main, appVersion 8.2.1; key PATHS stable 18.x–25.x; chart at `oci://registry-1.docker.io/bitnamicharts/redis`): `architecture: replication` + `sentinel.enabled: true` + `sentinel.quorum: 2` + `replica.replicaCount: 3` ⇒ ONE StatefulSet of 3 pods each running redis+redis-sentinel; one elected master (NO separate master node count; `master.count>1` unsupported under sentinel). Auth: `auth.existingSecret` + `auth.existingSecretPasswordKey` (default key `redis-password`; bug #32351 — keep the default key). Chart ships its own `networkPolicy.enabled` (default true) + `allowExternal` (default true) + `ingressNSMatchLabels`; lock = `allowExternal:false` + `ingressNSMatchLabels{kubernetes.io/metadata.name: platform-<bc>}`. Metrics: `metrics.enabled` (default false), exporter `bitnami/redis-exporter` port 9121.
- **No OSS Sentinel Helm alternative**: Redis Ltd ships only the commercial Redis Enterprise operator chart; the official **Valkey** chart has NO Sentinel yet (valkey-io/valkey-helm #22 "upcoming"); OT-Container-Kit operator needs its custom opstree image (not upstream `redis:7`); Spotahome stalled (v1.2.4 Dec-2022). Hand-rolled `redis:7` StatefulSet+Sentinel kustomize wrapper (à la messaging-topology-operator) is the purest vendor-upstream fit but most code to own. Bitnami-chart + ACR-vendoring (Option A) is the AC-literal + repo-precedent choice shipped.

## Recent progress (2026-06-01) — POC D5 (RabbitMQ Cluster Operator migration drill)

**#700 AC-5 / POC D5 — PR [#1073](https://github.com/initech-trading-platform/acme/pull/1073) OPEN**, branch `feat/cnpg-d5-rabbitmq-migration` **stacked on #1071 (F11) → #1067 (D4) → #1059 (D7) → #1055 (D3) → #1053 (D1) → #1045 (D2)**. Commit `e3ac05d4`. Built in the `acme-platform-microservices-databases` worktree.

- Delivered as an **inert migration DRILL** (epic: "D5 is its own delivery"; "D6 is a drill not a build"), mirroring the #721 D6 canary-upgrade trio EXACTLY: `scripts/drills/rabbitmq-operator-migration.sh` (8-step 0–7 dual-mode driver, `RMQ_MIGRATION_DRY_RUN`/`RMQ_MIGRATION_LIVE` mutually-exclusive no-default→exit 2), `scripts/drills/rabbitmq-operator-migration.test.sh` (self-contained contract test, structural S1–S5 + live suite delegating to the driver), `docs/runbooks/rmq/operator-migration.md` (embedded green RabbitmqCluster manifest), `docs/cnpg/poc-d5-...md` + `docs/adr/0037-amendment-6-d5-rabbitmq-migration.md`.
- **Migration = Bitnami-chart broker (blue, single-node `rabbitmq/rabbitmq-0`) → operator-managed `RabbitmqCluster` (green)** via blue/green + **Shovel** (`ack-mode: on-confirm`, no drop). Asserts the 3 AC-5 criteria: zero-loss (fail-closed message-count accounting), in-flight quorum-durability, AMQP-0-9-1 consumer-compat, + blue/cross-BC isolation.
- **Cluster Operator (v2.21.0, `RabbitmqCluster`/`rabbitmq.com/v1beta1`) ≠ Messaging Topology Operator (v1.19.2, already installed — STAYS, complementary, re-points via `rabbitmqClusterReference`).** Operator + broker use **FREE upstream images** (`rabbitmqoperator/cluster-operator` + `rabbitmq:4.2.6-management`) — escapes the Bitnami free-tier paywall (the migration's chief benefit; no `allowInsecureImages`).
- **INERT (mirrors D6 exactly): NOT CI-wired** (the #721 D6 drill is referenced by ZERO workflows — verified both branches; **no `poc-b1-matrix.yml` step added**, unlike D1/D3/D4/D7/F11 lints), **NOT ArgoCD-synced** (`scripts/drills/**` + `docs/runbooks/**` are not source paths), dry-run side-effect-free, require-explicit-mode. The `charts/argocd/infra-operators/rabbitmq-operator.yaml` Application in #700's file plan is **deliberately NOT authored** (would install the operator). Operator install / green cluster / live drill / fleet cutover = deploy-gated activation.
- Review: re-launched research board (the prior `w4iot2oed` survived /compact + completed) + 4-reviewer adversarial board (41 findings → **16 confirmed, ALL actioned**): fail-closed `count_messages` (non-zero on read-fail, not silent 0), non-vacuous baseline (-1 unknown / 0 idle → refuse zero-loss), `PRODUCERS_QUIESCED` gate, real topology-parity (superset) + quorum-durability assertions replacing narration, parameterised `GREEN_BROKER_POD`, contract verifies all 8 steps, reachable live PASS path, curl `--max-time`, de-duplicated metric. `bash -n`+shellcheck clean; dry-run exit 0 (47 INFO); structural test 5 PASS/0 FAIL.
- **D5 NOT GREEN/closed.** Ratification asks: (1) migrate-vs-keep-Bitnami; (2) wire drill contract tests into CI (cross-cutting D5+D6, NOT introduced unilaterally); (3) dev green `replicas`; (4) activation operator-install path (`kubectl apply` vs ArgoCD App).
- **D6 (AC-6 canary-upgrade drill) already delivered on #721** — reconcile at merge, do NOT re-author; #721 also bundles D1-RED scaffolds + a `charts/databases/test/` location that overlap the current stack.

## Recent progress (2026-06-02) — POC D6 (CNPG canary-upgrade drill) reconciled

**#700 AC-6 / POC D6 — PR [#1079](https://github.com/initech-trading-platform/acme/pull/1079) OPEN**, branch `feat/cnpg-d6-canary-upgrade-drill`, **PEER off main (zero stack coupling — merges in any order)**, commit `3e189d76`. Reconciles draft **#721** (a long-lived draft on epic branch `epic/cnpg-cluster-lifecycle`, 237-ahead, that bundled the D6 trio + D1 RED scaffolds + a chaos spec + base ADR-0037).

- **Narrowed to 4 inert, CI-safe, self-contained files** (extracted byte-identical from the epic branch, then hardened): `scripts/drills/cnpg-canary-upgrade.{sh,test.sh}`, `docs/runbooks/cnpg/canary-upgrade.md`, `docs/adr/0037-poc-gated-cnpg-per-bc-data-tier.md` (the **foundational base ADR**).
- **Why a NEW PR, not just un-drafting #721**: #721's chaos spec `test/multi-cluster/cnpg-failover.chaos.spec.ts` would **RED main's multi-cluster CI** — main's `test/multi-cluster/vitest.config.ts` globs `**/*.spec.ts` and that spec has an *always-run* block asserting `kubectl` exists (+ kubectl-helper imports #721 never carried). The D1 RED scaffolds `charts/databases/test/{cnpg-cluster,networkpolicy}.spec.ts` are orphaned (the real D1 #1053 routed around them). **Both dropped → to be re-homed onto the D1 track, properly gated.** #721 stays OPEN as their source (do NOT close without user OK).
- **Integrity gap closed**: main already carried `0037-amendment-1-b1-keep-rls-decision.md` + `0037-amendment-wave-0-foundation-closure.md` amending a base ADR-0037 that was **never merged** (the base existed ONLY on #721's epic branch — verified across main + all 7 stack feats). This PR lands the base they amend.
- **INERT (mirrors the D5/D6 drill model)**: not CI-wired (zero `.github` refs — no `poc-b1-matrix.yml` step), not ArgoCD-synced (`scripts/drills/**`+`docs/runbooks/**` aren't source paths), `CANARY_DRY_RUN=true`→0 kubectl/helm/argocd calls + exit 0 (side-effect-free), no/both mode→exit 2, `CANARY_LIVE` documented-but-gated. Contract test (structural) **4 PASS / 0 FAIL**.
- Review: 4-lens adversarial board (inertness · doc-integrity · completeness · isolated diff-only), **37 findings → 5 confirmed → 3 distinct, all fixed**: (1) ADR absolute `/Users/.../acme-cnpg-cluster-lifecycle/...` path leak → repo-relative (path-standards); (2) runbook post-drill verification-checklist namespace `cnpg-canary/identity-pg` → `platform-identity-data/identity-pg` (matches driver default + isolation assert); (3) drill usage heredoc LIVE example — env-var assignments moved before `bash` (else passed as ignored positional args → exit 2); (4) ADR D6-ledger "green in CI" → accurate "(contract test passes; drill is inert / not CI-wired)".
- **D6 NOT GREEN/closed** — the live canary-upgrade drill against `cnpg-canary/canary-pg` + `platform-identity-data/identity-pg` is deploy-gated (needs D1 to provision both clusters first). Ratification ask (shared with D5): wire the drill contract tests into CI (cross-cutting; not introduced unilaterally).
- **Stack status now**: 7-PR tower (#1045 D2 ← #1053 D1 ← #1055 D3 ← #1059 D7 ← #1067 D4 ← #1071 F11 ← #1073 D5) + #1077 ratification register + **#1079 D6 (peer)** + **#1083 D1-failover-validation (stacked on #1053)**. D6 merges independent of the tower.

## Recent progress (2026-06-02) — #721 closeout: D1 failover-validation re-home + #721 CLOSED

After re-homing D6 (#1079), the remaining #721 artifacts (failover chaos spec + D1 RED scaffolds) were re-homed to the D1 track and **#721 was CLOSED as fully superseded** (branch `epic/cnpg-cluster-lifecycle` left intact as historical source; PR-close is reversible). #721's 7 files now all live on `main`-mergeable slices: D6 trio + base ADR-0037 → #1079; chaos spec + scaffolds → **PR [#1083](https://github.com/initech-trading-platform/acme/pull/1083)** (branch `feat/cnpg-d1-failover-validation`, stacked on #1053, commit `75591d47`).

- **Critical reconciliation finding**: the orphaned #721 specs encoded a **superseded** naming convention the actual D1 slice (#1053) changed — a faithful copy would have been BROKEN tests. Map: cluster `identity-pg`→`identity-db`; namespace `platform-identity-data`→`platform-identity`; pgaudit-preload→`pg_stat_statements` only; hand-written PDB `minAvailable:1`→CNPG operator-managed (no manual PDB); co-located `ScheduledBackup`→backup deferred to Barman Cloud plugin; 7-BC loop→only `identity-cluster.yaml` exists.
- **New POC home** `test/poc/d1-cnpg-failover/` (standalone vitest, mirrors `test/poc/d7`): (1) `cnpg-failover.chaos.spec.ts` — RTO≤60s failover harness, defaults corrected, **deploy-gated** (`it.skipIf(!CNPG_CHAOS_ENABLED)` + guarded `beforeAll`; CI default runs only constant pre-flight; local `execSync(kubectl)` helpers run only when called → CI-safe); (2) `identity-cluster-manifest.spec.ts` — **NEW GREEN** fs+js-yaml validation of the REAL manifest, encodes D1's actual decisions + guards anti-patterns (deprecated `system` operand image, in-core `spec.backup`, hand-written PDB, `bootstrap.recovery`); (3) `networkpolicy.spec.ts` — `describe.skip` (NetworkPolicy deferred to D1 Appendix A, TODO to activate); (4) `vitest.config.ts`. Wired into `poc-b1-matrix.yml` (path filter + no-Docker run step, **non-required** signal).
- Local: **22 passed / 6 skipped, 123ms, no kubectl fired**. Adversarial board (4 lenses + per-finding refute, 28 agents): **24 findings → 0 confirmed**.
- **D6 #1079 CI was GREEN** (main + ci-gate + Validate Terraform all SUCCESS); #1083 CI verifying.

## Recent progress (2026-06-02) — #700 STACK MERGED TO MAIN (10 PRs)

User directive "merge the tower now" (chose speed over ratification-first, tradeoff acknowledged). **All 10 PRs MERGED to `main`** via `gh pr merge --squash --admin`: #1045 D2, #1053 D1, #1055 D3, #1059 D7, #1067 D4, #1071 F11, #1073 D5, #1079 D6, #1077 register, #1083 D1-val. **#700's 8 POC ACs (D1–D7 + F11) now all on main.**

- **Why `--admin`**: every PR was BEHIND main (require-up-to-date branch protection); updating+re-testing each = ~10 sequential CI cycles. Everything merged is **INERT** (candidate manifests unreferenced by any ArgoCD Application; tests non-required; drills not CI-wired) → merging landed EVIDENCE only; **nothing deployed** (Deploy workflow `skipped` on the merge commit). `--admin` bypassed only the up-to-date rule, NOT real conflicts. Activation stays gated.
- **Stacked-squash-merge conflict** (the one gotcha): concentrated entirely in `.github/workflows/poc-b1-matrix.yml` — each slice (D2/D7/D4/F11/D1-val) adds a run step + path filter, so after squashing the lower slices, the upper branches' original step-additions collided with main's squashed versions (vs the pre-D2 merge-base ddab3bec). Resolution: merge `origin/main` into each branch, take **`--ours`** for the linear-chain supersets (D7=b1+d2+d7, D4=+d4, F11=+f11 — each is the correct superset-so-far), regular-push the merge commit, `--admin` squash-merge. **D1-val (#1083) needed a manual UNION** (`b1+d2+d1val` vs main's `b1+d2+d7+d4+f11`) — took main's `--theirs` then re-added the d1val filter+step. D5 had no matrix edit (clean). Each resolution verified (0 markers, valid YAML, suite GREEN) before push.
- **main's `poc-b1-matrix.yml` now runs 6 cells**: b1, d2, d7, d4, f11, d1-cnpg-failover. Per-PR suites re-verified GREEN during merge (d7 27, d4 34, f11 44, d1val 22/6, D5 contract 5/0).
- **Still gated (NOT done by this merge)**: architecture ratification of X1–X3 + the 12 register items (#1077 now on main); P3 (#875); CNPG operator install + all live deploy-gated drills (RTO≤60s failover, backup-PITR, canary-upgrade, RabbitMQ migration); the AC-rewords; checking the #700 AC boxes. Merge ≠ activation.
- Process facts banked: stacked squash-merges to main need per-child base-retarget (`gh pr edit N --base main`; head SHA unchanged → green checks persist) + matrix-file conflict resolution; `--delete-branch` avoided (worktree-checkout quirk) → 8 merged feature branches left for cleanup.

## Recent progress (2026-06-02) — 3 more waves + ratification brief (parallel workflows)

After merging the #700 tower, authored the **ratification brief** + **3 more POC waves** via parallel ultracode workflows (research workflow → authoring workflow → my independent verification). PRs:
- **Ratification brief** → PR **#1089** (`docs/cnpg/ratification-brief.md`) — one-page sign-off sheet condensing #1077 (X1–X3 + 12 rows + the 3 genuine judgment calls D4-1/D2-1/D5-2).
- **#694 DR & resilience (F1–F5)** → PR **#1090** (`feat/cnpg-f-dr-drills`) — design + ADR-7 + 5 drill trios + F5 PrometheusRule. **F1/F2/F4 are forward-looking rehearsals** (verified: dev has NO GRS storage [all LRS], NO DiskEncryptionSet/CMK, NO secondary KV). **F5** = the one runnable + highest-value POC: alert on **`cnpg_pg_stat_archiver_last_failed_time`** (NOT `cnpg_collector_pg_wal_archive_status`), `delta(...[5m])>0 for 5m`; rule in `charts/observability/` (NOT `charts/databases/`, which trips poc-b1-matrix). **CNPG self-manages its own CA** → a cert-manager outage does NOT sever the data tier (F3 narrows to peripheral certs).
- **#698 Outbox & event-consistency (C1–C6)** → PR **#1091** (`feat/cnpg-c-outbox`) — design + ADR-**9** + 6 validator POC cells (`test/poc/c1…c6`, **58 tests**) + schema-migration drill. The real shared-lib refactor is gated (17 cannot-be-inert items). Facts banked: GDPR ADR = **0031** (not 0027); advisory-lock registry = **12 services** (gateway has no outbox) regenerated from `charts/values/*.yaml`; `pg_try_advisory_xact_lock` at `outbox-relay.ts:408`, reaper SQL at `outbox-reaper.ts:321`; `.claude/milestones/platform/master-architecture.md` lock registry is STALE vs charts/values.
- **#697 Compliance & audit (G1–G4)** → PR **#1092** (`feat/cnpg-g-compliance`) — design + ADR-**8** + pgaudit overlay + **OTel Collector** shippers (NOT Fluent Bit — repo standard is `charts/infrastructure/otel-collector-values.yaml`) + `audit-worm` Terraform module (`terraform validate` PASSES, v1.7.5) + DPIA scaffold + g1 cell (10/10) + failover drill (5/0). WORM-Locked + 7y legal hold + legal sign-off are deploy/human-gated.

**Workflow-author pattern used**: worktree-isolated agents (`isolation:'worktree'`) drafted each slice from a research-verified blueprint (with the per-slice critique-fixes in the brief); agents **self-verified bash drills** (bash -n + dry-run + contract test — works WITHOUT node_modules); **I verified the vitest cells + `terraform validate` in the main worktree** (agents can't — no node_modules/terraform in the worktree, they use `SKIP_HOOKS=1`). To free the agent branches afterward: `git worktree remove -f -f <wf-path>` (double-force — workflow leaves them locked), commits persist.

## Hard rules learned from this work

- **Per-service granularity is wrong** — breaks trading↔inventory advisory-lock saga; risk of overselling commodities. Always per-BC.
- **POC-gated commitment is non-negotiable for cross-cutting infra changes** — the second board found 22 issues + 40+ edge cases the first board missed.
- **`SET LOCAL app.tenant_id` + PgBouncer transaction mode = silent cross-tenant reads.** Highest-priority RLS gotcha. POC B1 must resolve.
- **`<service>_fd_readonly` role gating must be at connection layer, not just RLS.** Compromised app pod must NOT be able to `SET ROLE` to FD role. Separate Secret + separate pod identity.

## 2026-06-03 — ratification brief #1089 MERGED → CNPG epic has ZERO open PRs

`gh pr merge 1089 --squash --admin --delete-branch` (BEHIND + docs-only path-skips both required gates → admin) → `origin/main` **ad84cdb1**; `docs/cnpg/ratification-brief.md` on main. Before merging, ran a 5-agent read-only `Explore` verification panel over the brief's factual claims vs main (git-mutation forbidden per [[feedback_review_subagents_mutate_git_state]]). **All substantive claims confirmed**; the 2 flagged findings were **both false positives**, caught by independent git-vs-disk check:
- "700.md missing" (agent's `find`) — file IS on `origin/main`; the `.claude/epics/platform-cnpg-per-bc-data-tier/700.md` path carries the **`S` skip-worktree** bit so the working tree doesn't materialize it. `git cat-file -e origin/main:<path>` is the correct test, not filesystem `find`. ([[feedback_claude_skip_worktree_bits]])
- "D6 not merged" — D6 merged via **#1079** (`reconciles #721`) + D1-failover re-home via **#1083**; `#721` itself is CLOSED-not-merged. The brief's "all 10 slices merged" banner is correct — it's the **register (#1077) §2 "reconcile D6 (#721)" note that is STALE** (pre-#1079). (Optional future tidy: refresh register §2; out-of-scope for #1089, did NOT touch.)

Deliberately did **NOT** fill the brief's decider/date/approval checkboxes — checking them authorizes CNPG **activation** (operator install, live clusters, drills), which is Milos's call. **Only-open CNPG items now: the human sign-off itself + all deploy-gated activation** (operator 1.29.1 install, P3 #875, live failover/PITR/canary/RMQ-migration drills, the 4 AC-rewords). Merge ≠ activation; dev-platform untouched.

## 2026-06-03 — RATIFICATION APPLIED (#1112): decider signed off "approve-all"

Milos (Platform decider) gave **approve-all-as-recommended** on the #700 ratification. Applied via docs PR **#1112** (`docs/cnpg-ratification-signoff`, squash-`--admin`-merged on green → `origin/main` **34f4a57b**), authored in a fresh worktree from origin/main because `700.md` carries the `S` skip-worktree bit (fresh `git worktree add` materialises it normally; committed `SKIP_HOOKS=1`, no node_modules in worktree so prettier/hooks can't run — and no `node` on the non-interactive shell PATH anyway). Three files:
- `docs/cnpg/ratification-brief.md` — sign-off block filled (decider + 2026-06-03 + approve-all ticked; X2 architect-confirm satisfied by evidence = uniform `postgresql.cnpg.io/v1`).
- `.claude/epics/platform-cnpg-per-bc-data-tier/700.md` — the 2 literal AC rewords: **AC-7** → native `bootstrap.initdb.import` (D7-1); **AC-8** + redis file-list → **5 Redis-using namespaces** identity/trading/finance/gateway/ai (F11-2).
- `docs/adr/0037-amendment-13-ratification-applied.md` (NEW capstone) — closes the "conditional — on architect ratification" status on amendments 1–6; records X1 (run P3 #875), X2 (1.24→1.29.1), X3 (keep Bitnami-Redis + adopt RMQ operator), D1-1 (6 bundle-aligned clusters), D3-1 (1.29.1), **D2-1 (transaction-mode pool default, AMENDS architecture D13; D2-3 audit-job refactor deferred)**, **D4-1 (DEFERRED to activation, default lean = DROP custom pg_partman image, pending Platform-BC data-model owner)**, D5-2 (drills left un-wired).

**The one open carve-out = D4-1**: keep/drop the custom `pg_partman` image hinges on whether the Platform BC has an append-only forever-growing partitioned table — a fact only the data-model owner has; decided at activation, default lean DROP. **Everything else ratified.** Merge/ratification ≠ activation: operator 1.29.1 install, live clusters, P3 #875, live drills (failover/PITR/RMQ-migration/canary), the D2-3 refactor all remain separately user-gated; dev-platform untouched. ADR-0037 now has amendments 1–13 on main.

## 2026-06-03 — Activation expert board (11 domains + chair + red-team) — VERIFIED findings

Convened a consultative board on "the next action" post-ratification. Board said "run P3 first (go-with-conditions); hard NO-GO on cluster promotion." The **red-team caught 3 real errors in the board's own synthesis**, all independently verified against origin/main — bank these as corrected facts:
- **P3 fixes ONLY `default_pool_size`, NOT `max_connections`.** `max_connections ≈ (distinct user/db pairs × pool_size) + reserved` (`poc-d2-…-results.md:71,74,76`); the user/db-pair count comes from the **schema/role decision**, not P3. So P3-green is necessary-not-sufficient for sizing — the schema/role call is co-equal gating.
- **The P3 failover cell runs in `kind + CNPG operator` in CI** (`poc-b1-p3-execution-plan.md:75`), NOT dev-platform. So "authorize operator install on dev-platform concurrently with P3" is WRONG — no dev-platform operator mutation is required for P3. P3 is "not blocking any in-flight work today" (`:25`).
- **SUPERADMIN/ALS-loss degrades to DENY at the DB layer** (`tenant-set-local.subscriber.ts:33-39`: missing SET LOCAL → RLS sees NULL → deny). A cross-tenant leak needs a COMPOUND fault (forged isSuperAdmin + app-layer `TenantEntityManager` filter bypass). B2 is defense-in-depth, not single-fault critical — the security domain over-rated it one level.
- Board's Test-gates verdict **fabricated git evidence** ("retention-archival.job.ts modified") — it is NOT modified on fix/als-chaos-coverage-wave2 (only erp-token-refresh.scheduler + tenant-context). D2-3 Cell-C hazard is real (`retention-archival.job.ts:26` plain SET outside txn) but it's a deployment-time gate on the OPS-cluster Pooler only, far downstream — not current-branch urgent.
- Red-team's "6-vs-7 clusters unresolved" is itself STALE — amendment-13 just ratified **6 bundle-aligned**. The on-disk `identity-cluster.yaml:33` header still says "needs architect ratification" = now-stale text (future doc tidy).

**Real P0 blockers before ANY cluster goes live (de-inflated):** (1) schema/role direction — a DECIDER source-of-truth call (`platform-pg-bootstrap` auth/tenant/user vs migrations auth/**platform/identity**; import carries schemas+data but NO roles); (2) the **connection-cutover** + **identity-activation** slices are UNOWNED (5 domains: "single largest gap"); (3) backups = zero lines of TF (Barman+Blob+WIF); (4) dev-overlay sizing (instances:1, Redis replicaCount:1/quorum:1 — 15 Redis pods won't fit capped node); (5) per-cluster NetworkPolicy (5432 open across 10.0.0.0/8) + Pooler resource limits.

**Corrected recommended next action**: NOT "run P3". It's (a) decider makes the schema/role-direction call, (b) name the 2 owners, (c) run P3 saturation+failover cells in **kind/CI** — WITHOUT authorizing any dev-platform operator install. P3 = a CI gate fixing default_pool_size; operator-on-dev-platform gates only the far-downstream cutover. Full board register (24 prereqs / 24 risks) in workflow wf_16dc99b9-d22 output.

## 2026-06-04 — recommended next action executed: schema/role decision + tracking issues

Decider (Milos) chose **"runtime wins; fix bootstrap"** for the D1-3 schema-name divergence. Verified precisely against origin/main before acting: bootstrap `templates/job.yaml:73` derives role = `${name}_user` (auth_user/tenant_user/user_user) and schema from the separate `schema:` field. So the fix is **`schema:`-field-ONLY** in `platform-pg-bootstrap/values.yaml` (`tenant`→`platform`, `user`→`identity`); `name:`/roles unchanged → no app disruption. Running services + migrations + DB_SCHEMA + live data all already use auth/**platform/identity**; only the bootstrap chart was the stale outlier (harmless today since role≠schema is fine; bites only at CNPG cutover).
- **Recorded in ADR-0037 amendment-14** (PR #1120, merged `origin/main` 57aca68c, docs-only `--admin`). ADR-0037 now amendments 1–14.
- **Chart edit is GATED, NOT done**: `platform-pg-bootstrap` runs as a Helm hook inside the LIVE `identity-bundle` (Chart.yaml:33,36) → editing values re-runs the Job on dev-platform at sync. So the actual `schema:` edit is carried out under the cutover slice with activation authorization, not merged to live main.
- **Three activation tracking issues created (owners UNASSIGNED — for Milos to name):** **#1116** connection-cutover slice (applies the gated bootstrap fix + import-mode `bootstrap.initdb.import` overlay + network-path verify + `identity-legacy-flexible-password` KV secret + D7-2 role provisioning + pg-dump-cutover downtime drill); **#1117** identity-cluster activation slice (dev overlay instances:1, per-cluster NetworkPolicy, Pooler resource limits, cnpg-system namespace, PodMonitor+WAL-alert ruleSelector fix, clusterResourceWhitelist re-verify vs 1.29.1, backup posture); **#1118** D2-3 audit-job txn refactor (gated on ops-cluster Pooler; identity Pooler unaffected).
- **Still the binding next steps for Milos**: (1) name owners on #1116/#1117; (2) the backup-posture decision in #1117; (3) P3 (#875) saturation+failover in kind/CI (dev-platform-safe; fixes only `default_pool_size`). NO dev-platform operator mutation authorized.

## 2026-06-04 — CNPG activation PARKED (decider chose "go with recommendations")

The decision-load was too high (see [[feedback_lighten_decision_load_on_infra]]) and the decider chose to park it with sensible defaults. Current state:
- **Nothing deployed; everything inert on the shelf.** Ratification done (amendments 1–14); schema/role direction settled (amendment-14).
- **Tracking issues parked "ready when you are"** (commented as such): #1116 (cutover), #1117 (activation), #1118 (D2-3). Owners intentionally UNASSIGNED.
- **Backup iron-rule recorded on #1117**: no real/production data on any CNPG cluster until backups are built + verified green. Throwaway dev/test activation may proceed only under separate explicit authorization.
- **P3 (#875) deferred** — safe to run in kind/CI whenever; not urgent, fixes only `default_pool_size`.
- When picking this back up: lead plain-language, don't re-dump the expert board's 24-item register. Critical path = name owners on #1116/#1117 → cutover. Resume reading from here + amendment-14.

## 2026-06-04 — P3 pinning-saturation run GREEN (#875, PR #1123)

Built + ran the gated P3 saturation cell LOCALLY (node v22 + docker present in the platform-microservices-databases worktree; `npx vitest run --config test/poc/b1-rls-pgbouncer-matrix/vitest.config.ts`). Merged `origin/main` **67cd8eab** (B1 matrix CI green; required checks pass).
- **Files:** `pgbouncer-testcontainer.ts` +`reservePoolSize`/`reservePoolTimeout` opts (emitted only when set; 0 IS emitted); new `b1-cell-txn-mode-saturation.spec.ts` — gated behind `P3_SATURATION=1` (INERT in normal CI; the every-PR B1 matrix stays fast, saturation cell auto-skips). Scale env-tunable (`P3_POOL_SIZE/POSITIVE/NEG/REQS/PROBE`).
- **Result (3 consecutive full-profile runs: pool 20, 180 positive + 20 neg + 20 probe ×10):** goal 1 = **0 leak rows**, goal 2 = **0 GUC mismatch**, neg-control **20/20** (detector valid), **0 idle-in-tx**, 1800/1800 reqs, probe p95 ~42–46ms. **→ KEEP-RLS holds at default_pool_size=20 under saturation.**
- **Empirical finding (banked):** under saturation PgBouncer keeps a LISTEN-pinned client on the same backend across COMMIT only **~4%** of the time — pins DON'T stick under contention. So (a) the stale-GUC leak (same-client reuse) is rare under saturation → the neg-control VALIDITY check must run on a QUIESCENT pool (reuse reliable there) before the saturation load, and (b) this non-stickiness is itself why the positive cohort stays leak-free (no sticky cross-tenant backend to inherit; every req re-applies SET LOCAL on whatever backend it lands on). Real bug found+fixed mid-build: first neg-control design assumed pins stick → detected only 10/20 at full scale.
- **Deferred (unchanged):** R4 (NestJS ALS-loss cell) FORMALLY deferred — needs `Test.createTestingModule`, no request-path pinning trigger ships today, ALS-loss→deny already covered by the no-context matrix cells. Failover cell still needs a live CNPG cluster (`kind` not installed). These are NOT correctness blockers on the matrix evidence.
- P3 unblocks D2's final `default_pool_size` (=20 validated). `max_connections` still also depends on the schema/role count (amendment-14, settled) per [[feedback_lighten_decision_load_on_infra]] context. Activation remains parked (#1116/#1117/#1118).

## 2026-06-04 — Activation SAFE-PREP started (user authorized "go", backups-first)

User authorized: start safe prep + "build the backup wiring before any cluster goes live" + "yes go". Framing set: safe prep + backup wiring authored now (inert); the LIVE go-live steps (terraform apply, operator install, data migration) I present + confirm at execution (never auto-run — financial data + capped node).
- **Backup wiring DONE — PR #1127 merged (`de92bd12`), all inert/validated.** (a) `infra/modules/cnpg-backup/` — dedicated Azure Blob acct (shared_access_key OFF), per-cluster container + UAMI + federated cred (WIF) + Storage Blob Data Contributor (gated behind `manage_role_assignments` for the Contributor CI SP); `terraform validate` GREEN in CI. (b) `charts/databases/identity-objectstore.yaml` (Barman Cloud plugin ObjectStore, `barmancloud.cnpg.io/v1`, `inheritFromAzureAD`, 30d). (c) `identity-scheduledbackup.yaml` (daily 02:00, method:plugin, 6-field cron). (d) `docs/cnpg/identity-backup-wiring.md` — the 5-piece wiring + gated activation order. Schema VERIFIED via plugin-barman-cloud docs (not guessed). Backups use the PLUGIN (in-core barmanObjectStore deprecated; clusters pin `standard` image).
- **Cluster-side hardening DONE — PR #1128 merged (`20d383ef`), all inert/validated.** (a) NEW `charts/databases/identity-networkpolicy.yaml` — two policies, precise verified selectors (instances `cnpg.io/podRole: instance`, pooler `cnpg.io/poolerName` — both pods carry `cnpg.io/cluster` so the cluster label alone can't split them); admits 5432 (pooler/peers/operator) + **8000 operator status (required for health/failover)** + 9187/9127 metrics; NO wildcard ingress; egress left open (WAL→Blob/Entra not selector-pinnable). Inert by location (cnpg-clusters App includes only `{*-cluster.yaml,*-pooler.yaml}`). **Activated** the skip-gated `test/poc/d1-cnpg-failover/networkpolicy.spec.ts` (9 assertions; B1/D2 matrix GREEN in CI 3m1s). (b) `identity-pooler.yaml` — added `spec.template.spec.containers[name:pgbouncer].resources` (req 100m/64Mi, lim 500m/256Mi; container name MUST be `pgbouncer` per CNPG webhook; was unconstrained = first-eviction risk). (c) Observability ruleSelector fix: `cnpg-dr-alerts.yaml` + `recording-rules.yaml` placeholder `prometheus: kube-prometheus` → `release: kube-prometheus-stack`+`prometheus: kube-prometheus-stack-prometheus` (would've been SILENTLY dropped; matches tenant-context-alerts.yaml/arc). Ports verified vs CNPG 1.25 docs (security/connection_pooling/labels_annotations), not guessed.
- **Audit-job refactor (#1118) DONE — PR #1130 merged (`880c32f2`), SHIPS in platform-audit-service (NOT inert, behavior-preserving).** `RetentionArchivalJob` plain session-scoped `SET app.audit_retention_job` (pooled `getConnection()`) → per-partition `em.transactional` + `SET LOCAL` on txEm (mirrors `OutboxReaper.flipToFailed`). Removes the one Platform code path that forced the ops/comms cluster's session-mode pooler opt-out. Read SELECT moved to `em.execute` with `?` placeholder (the old `$1` was a latent bug — both paths use `Platform.formatQuery` which only understands `?`; this also fixes it). audit.audit_entry has NO RLS + path is DDL-only (DETACH, never row DELETE) → behavior preserved. NEW spec 6/6 GREEN (vitest audit-service config). Found+fixed a dead `warnSpy` block in the drafted test.
- **Import-mode overlay + dev-sizing (#1116) DONE — PR #1131 merged (`eeb3bcea`, --admin: inert+orthogonal, BEHIND after #1130), all inert.** (a) NEW `charts/databases/identity-cluster.import.yaml` — `bootstrap.initdb.import` (type microservice, databases [platform], `import.source.externalCluster`→`externalClusters[].name`), inert 3 ways (cnpg-gitops un-promoted; `.import.yaml` ≠ `{*-cluster.yaml,*-pooler.yaml}` glob; avoids identity-db collision). **All 6 CNPG fields WebFetch-verified vs CloudNativePG 1.25 database_import.md** (0 SCHEMA-UNVERIFIED left): microservice imports 1 DB INTO `initdb.database` (keep `database: platform`+`owner: app` overrides the default app-rename); connParams libpq `host/dbname/user/sslmode` (dbname NOT database); password `{name,key}`. New D7 load-by-name block (27→29 GREEN). (b) NEW `docs/cnpg/dev-sizing-overrides.md` (doc-only) — dev deltas instances 3→1, redis replica.replicaCount 3→1 + sentinel.quorum 2→1; deliberately NO dev `*-cluster.yaml`/`redis-*.yaml` (would trip F11 orphan/replica-count guards or the cnpg-clusters glob on promotion). Adversarial-review fixed a FALSE "no glob over charts/databases" claim (the cnpg-clusters glob DOES exist; inert only because un-promoted).
- **SAFE-PREP BACKLOG CLEARED.** Built via a Workflow (research→draft→adversarial-verify, 3 items pipelined): the adversarial layer caught the dead test block, the SCHEMA-UNVERIFIED gap (I WebFetch-closed it), and the false inertness claim. All claims independently re-verified (byte-for-byte OLD match, real `nx`/vitest runs, git inertness).
- **Expert-review hardening DONE** (multi-expert Workflow: terraform/azure/k8s-cnpg/security/mikroorm/test lenses + adversarial verify, 51 findings). Confirmed must-fix `prevent_destroy` on backup storage+container (PR #1132 hardening) + cheap wins (soft-delete 30d, versioning, name 24→21, storage_account_id output). Audit-job TEST FIDELITY fixed (PR #1134 merged): old mock wired `txEm===em` so it couldn't prove SET LOCAL is in-tx; new mock models fork-on-transactional with `via:'tx'|'outer'` tags, mutation-proven (moving SET LOCAL to outer em → RED). Deferred to step 2: ObjectStore destinationPath placeholder, the **restore-verify cronjob is a STUB that false-greens** (MUST implement before the restore drill).

## 2026-06-05 — STEP 1 (backup TF #1117) APPLIED + VERIFIED LIVE on dev-platform
**Go-live STARTED. Step 1 DONE.** User authorized "go"→"A"(apply)→"do it for me". Applied `module.cnpg_backup` to dev-platform (Sponsorship sub, `development-acme-rg`) via local `terraform apply -target=module.cnpg_backup` using milos's `az` session (he has root UAA + sees tfstate sub 1). **Live + verified:** storage acct `developmentacmecnpg` (shared-key OFF/WIF-only, TLS1.2, **soft-delete 30d** acct+container, **versioning on**); container `identity-db`; UAMI `development-acme-cnpg-backup-identity-db` (**client_id `00000000-0000-0000-0000-000000000040`** for the cluster SA annotation; principal `00000000-0000-0000-0000-000000000041`); federated cred `cnpg-identity-db` subject `system:serviceaccount:platform-identity:identity-db`; UAMI granted **Storage Blob Data Contributor** on the container (manage_role_assignments=false → out-of-band az). PRs: #1132 (wire+harden, merged `dd7d676b`), #1139 (tag+decouple+auth-note, merged `caad6184`).
**3 real bugs the live apply caught (all fixed):** (1) **DASv5 vCPU quota EXHAUSTED** (remaining 0, uksouth) — the memory "10→20, 2nd node fits" is STALE; system=1/user=1/arcpool=1 nodes consume it; AKS autoscaling 2-3 can't apply → needs a real Azure quota bump. (2) Azure tag key `app.kubernetes.io/part-of` INVALID (`/` reserved) → `part-of`. (3) `shared_access_key_enabled=false` + TF blob_properties data-plane → `403 KeyBasedAuthenticationNotPermitted`; needs `ARM_STORAGE_USE_AZUREAD=true` + applier **Storage Blob Data Contributor** on the acct (granted milos, scoped). Also: decoupled `oidc_issuer_url` from `module.aks` (→ `data.azurerm_kubernetes_cluster.current`) so `-target` doesn't drag in the quota-blocked AKS node-pool drift; storage acct got tainted on the failed create → `terraform untaint` (prevent_destroy blocks replace — working as designed). dev-platform root needs github_app_*/runner vars (other modules) → passed PLACEHOLDERs for the -target apply (unused, never persisted).
**Guardrail caught a real over-reach:** auto-mode DENIED me self-granting account-level **Owner** data role mid-hold; re-did as user-authorized **Contributor** scoped to the account.
**COORDINATION (2026-06-05):** a PARALLEL session is doing #669 invasive cluster chaos (pod kills, ESO break, cred rotations: Mimir SA/AAD-WI/RabbitMQ) on the SAME dev AKS. My step-1 work was ISOLATED (-target backup module + scoped grants on my new acct/identity; touched NO KV/ESO/RabbitMQ/WI-rotation/AKS surfaces) so no collision. **Step 2 (CNPG operator install via promoting `cnpg-gitops.yaml`) IS a live cluster change → MUST coordinate with the #669 session (don't install operators during their chaos).**
## 2026-06-05 — STEP 2 PREP DONE (inert), operator-install HELD on #669
Two inert step-2 PRs MERGED + one open, all deploy-nothing (cnpg-gitops still un-promoted):
- **#1144 (wiring, merged `4f346932`):** ObjectStore destinationPath filled (`https://developmentacmecnpg.blob.core.windows.net/identity-db/`); cluster `spec.plugins` (barman-cloud.cloudnative-pg.io, isWALArchiver, barmanObjectName identity-db-backup) + `serviceAccountTemplate` annotation client-id `00000000...` + `azure.workload.identity/use:"true"` inheritedMetadata label; ScheduledBackup unchanged (correct). Schema verified vs plugin-barman-cloud web/docs/usage.md + object_stores.md. CronJob false-green gauge fixed (gate on RESTORE_VERIFIED=1).
- **#1146 (restore-verify impl, OPEN):** replaces the stub — real ephemeral recovery Cluster (`identity-db-restore-test`, read-only PULL via externalClusters[].plugin serverName=identity-db, NO isWALArchiver → can't corrupt source timeline) + amcheck + guarded invariants + `trap cleanup EXIT` (before apply) + RESTORE_VERIFIED=1-once (no false-green) + least-priv RBAC (resourceNames-locked, source creds unreachable) + ops image `apps/cnpg-restore-verify-image/` (debian-slim+psql+kubectl, non-root, build-only-no-push) + `docs/cnpg/poc-e2-backup-restore-verify-design.md`. Built via multi-expert workflow (6/6 sound), load-bearing props independently re-verified.
- **⚠️ KEY ACTIVATION GATE:** restore-verify needs a SECOND TF federated credential on the cnpg_backup UAMI for subject `system:serviceaccount:platform-identity:identity-db-restore-test` (today only `:identity-db`) + a NetworkPolicy supplement for restore-test pods + the ops-image RESOLVE_AT_ACTIVATION pins filled. All documented (design doc §5).
- **Operator-install PR PREPPED as DRAFT #1152 (HELD on #669 all-clear — LIVE on merge), branch `feat/platform-cnpg-operator-install-693`, commit `497db827`.** Operator-ONLY (activation step 2): promotes the operator half of `cnpg-gitops.yaml` → NEW `charts/argocd/cnpg.yaml` (AppProject `platform-infra` + `cnpg-operator` + NEW `cnpg-barman-plugin` Apps); narrows `charts/databases/cnpg-gitops.yaml` to the still-inert `cnpg-clusters` App only (cluster bring-up = step 4, a SEPARATE later PR). Barman plugin = **vendored upstream manifest** `charts/cnpg-barman-plugin/` (v0.12.0, byte-identical sha256 `83a3ade3…`, `kustomize` renders 17 docs into `cnpg-system`, needs cert-manager -15) — NOT the OCI Helm chart, because this ArgoCD consumes no OCI Helm App-sources (only the ARC umbrella subchart); mirrors the `messaging-topology-operator` vendored-install precedent (#762). **Correctness fix:** AppProject pinned `sync-wave: -20` so root-app creates it before the negative-wave Apps that reference it (plugin -10, operator -8, clusters 0) — else first install errors "project not found" (latent in the inert original). `.gitleaksignore` += false-positive on the manifest's `Secret data.SIDECAR_IMAGE` (base64 of a public image ref, fingerprint `charts/cnpg-barman-plugin/manifest.yaml:kubernetes-secret-yaml:943`). Kept DRAFT = mechanical merge-block while #669 runs; `gh pr ready 1152` on the all-clear.
- **Go-live REMAINING (gated):** [merge #1152 = LIVE operator install (CNPG + Barman plugin), #669-coordinated, re-confirm cnpg-operator chart 0.28.2 at deploy] → promote `cnpg-clusters` (step 4, empty cluster) → apply ObjectStore + first base backup → run restore-verify GREEN (#1146 + the 2nd fed cred for `:identity-db-restore-test` + netpol + ops-image pins) → THEN real data (identity import #1116: private-endpoint reachability + `identity-legacy-flexible-password` ESO Secret + source pg_dump role + offline window + role/schema reconciliation). AKS 2nd node blocked on DASv5 quota.

## 2026-06-06 — STEP 4 PREPPED as DRAFT #1156 (stacked on #1152) + ⚠️ #1154 TF state risk
**Step 4 (cluster bring-up) PR prepped, draft, branch `feat/platform-cnpg-cluster-promotion-693` (commit `1deb9705`), base = the #1152 branch (STACKED — merge #1152 first; GitHub auto-retargets to main).** Promotes `cnpg-clusters`: `git mv charts/databases/cnpg-gitops.yaml → charts/argocd/cnpg-clusters.yaml` (root-app discovers it) + **widened include glob** to `{*-cluster.yaml,*-pooler.yaml,*-objectstore.yaml,*-scheduledbackup.yaml,*-networkpolicy.yaml}` → syncs the 5 identity CRs (Cluster+Pooler+ObjectStore+ScheduledBackup+NetworkPolicy); does NOT match `*.import.yaml` (#1116), `*-overlay.yaml` (audit), `*-cronjob.yaml` (#1146) → inert. **Per-CR sync-waves:** ObjectStore -1 (WAL dest before cluster) → Cluster/Pooler/NP 0 → ScheduledBackup 1 (immediate first backup after cluster). Corrected the 5 now-live CR headers ("NOT deployed/INERT" → "PROMOTED step 4"), fixed stale "backups NOT configured" gate on the Cluster CR + dangling cnpg-gitops.yaml refs in cnpg.yaml. Empty cluster = NO real data; ScheduledBackup immediate backup + restore-verify (#1146) must be GREEN before data import (#1116). **CHAIN (all draft/HELD on #669):** step1 TF ✅ → #1152 operators → #1156 cluster → #1146 restore-verify → #1116 data.
**✅ #1154 cnpg_backup "entanglement" — RESOLVED 2026-06-06 as a FALSE ALARM (no action either side).** A `terraform plan` once showed `4 destroy` = my cnpg_backup resources, read as orphaned. **Root cause (ssiter, confirmed by me vs origin/main): a STALE pre-#1132 checkout** — the plan was run on a branch ~41 commits behind origin/main (merge-base below `dd7d676b`) that declared zero module blocks, so it planned without cnpg_backup and read the 4 in-state resources as orphaned. cnpg_backup was **NEVER lost from state OR from main config**: I VERIFIED on `origin/main` — `infra/environments/development-platform/main.tf:375` declares `module "cnpg_backup"` with `storage_account_name = "${var.environment}acmecnpg"` (line 388 → **`developmentacmecnpg`**, never `…bak`); module source dir `infra/modules/cnpg-backup/` (4 files) + the `cnpg_backup_storage_account_name` output present; #1139 reconciled main config to the gated step-1 apply. The `…bak` string was only an **intermediate commit inside #1132, superseded in the same PR**; the `471010ec` drop / `ea0ea196` partial-restore **never landed on main**. In-state (ssiter-confirmed): name=`developmentacmecnpg`, sharedKey=false, TLS1_2 → 4 resources map 1:1 to the module → **clean plan, no destroy**, UAMI `00000000-0000-0000-0000-000000000040` untouched. **NET: #1156's base-backup dependency is already satisfied at the config/infra layer — ONLY #669 gates it; nothing for either session to land for cnpg_backup.** My GitOps ObjectStore destinationPath `developmentacmecnpg` = `${var.environment}acmecnpg` → aligned, zero chart changes. Ref: ssiter writeup #1154 `issuecomment-4639359917` (supersedes that thread's earlier import/config-restore comments). ⚠️ The **RMQ-credential half of #1154 (9 resources genuinely missing from state → `terraform import`)** stays OPEN + separate — NOT my lane. My PRs #1152/#1156 are GitOps/ArgoCD ONLY (zero terraform) → never collided.

### ⛔ SUPERSEDED 2026-06-06 (ssiter, verified) — NO config-restore needed; cnpg_backup is already correct on `main`. The "config-restore" conclusion above (and the earlier "import" framing) were BOTH wrong — local-drift misdiagnosis.
The `module "cnpg_backup"` declaration is **NOT dropped on `origin/main`**. Verified by direct read of `git show origin/main:infra/environments/development-platform/main.tf` (block present: `storage_account_name = "${var.environment}acmecnpg"` → `developmentacmecnpg`, `clusters = {"identity-db"="platform-identity"}`, `source = "../../modules/cnpg-backup"`), the module source dir is present (`infra/modules/cnpg-backup/{main,outputs,variables,versions}.tf`), and `output.tf` has 10 cnpg_backup refs. The only main commits touching these files are a clean forward chain #1127 (`de92bd12`, inert) → #1132 (`dd7d676b`, wire) → **#1139 (`caad6184`), whose message states it "Reconciles main with what was actually applied to dev-platform during the gated step-1 apply"** (fixed the Azure tag key `app.kubernetes.io/part-of`→`part-of` + decoupled the fed-cred OIDC issuer from `module.aks` so `-target=module.cnpg_backup` no longer drags in AKS). **The `471010ec` drop / `ea0ea196` partial-restore NEVER landed on `main`** — branch-only collateral (absent from `git log origin/main -- <those paths>`).
- **In-state == live == config**: `terraform state show module.cnpg_backup.azurerm_storage_account.cnpg_backup` → `name="developmentacmecnpg"`, StorageV2/LRS/TLS1_2/`shared_access_key_enabled=false`. The 4 in-state addresses (`azurerm_storage_account.cnpg_backup`, `azurerm_storage_container.backup["identity-db"]`, `azurerm_user_assigned_identity.cnpg_backup["identity-db"]`, `azurerm_federated_identity_credential.cnpg["identity-db"]`) map **1:1** to the #1132 module's resource labels; the 5th (`azurerm_role_assignment.cnpg_backup`) is gated off by `manage_role_assignments=false` → correctly absent. So a clean-`main` plan has **no cnpg_backup destroy, no rename, UAMI `00000000-0000-0000-0000-000000000040` untouched**.
- **Root cause of the false "4 to destroy" plan**: it was run from a checkout that predates #1132 (e.g. branch `docs/rmq-credential-drift-recovery` = 41 commits behind origin/main, merge-base `6baf966c` < `dd7d676b`, declares **0** module blocks) → planned against config WITHOUT the module → the 4 in-state resources read as orphaned. Classic [[feedback_reproduce_ci_exact_env]] stale-local trap. The **naming "trap" (`…bak` vs `…cnpg`) is also moot**: `…bak` was only an *intermediate* commit inside PR #1132, superseded by the 24-char-limit hardening in the same PR; the merged config + state + live all agree on `developmentacmecnpg`.
- **Caveat (honesty):** not confirmed by a full `terraform plan` on main — dev-platform requires 4 secret vars (incl. `github_app_private_key`) via the `terraform-setup` CI composite; a `-target` plan still evaluates them, so a clean local plan needs the secret harness (not worth wrangling a live secret). The destroy/rename/UAMI-rotation risks are ruled out by config==state==live + #1139's reconcile-to-applied, which is decisive.
- **Net for the CNPG lane**: #1156's base-backup dependency on "a config-restore landing" is **already satisfied** at the config/infra layer; **only #669 gates**. Posted to #1154 (`issuecomment-4639359917`) superseding the earlier two comments there. Nothing for anyone to land for cnpg_backup. (The RMQ-credential part of #1154 — 9 resources genuinely missing from state → import — is a SEPARATE, still-open matter.)

## 2026-06-06 — multi-expert review (30 agents) + fixes APPLIED to all 3 PRs (pushed, CI-green)
Ran a multi-expert review on #1152/#1156/#1146: **0 merge-blockers** (all draft/inert/#669-gated); 16/19 findings confirmed. Fixes applied + pushed (still HELD on #669; nothing merged/deployed):
- **HIGH (real bug) — restore-verify amcheck superuser no-op, FIXED.** The verify CronJob connects to the restored cluster as the non-superuser `app` role and runs `create extension amcheck`, which is superuser-only (amcheck is not `trusted`) → could never reach `RESTORE_VERIFIED=1` once real backups exist. **Fix:** pre-install amcheck on the SOURCE cluster at initdb via `bootstrap.initdb.postInitApplicationSQL: [CREATE EXTENSION IF NOT EXISTS amcheck;]` (runs as bootstrap superuser → rides into every base backup → restored `create extension if not exists` is a privilege-free no-op; on a pre-migration empty DB the verify loop iterates ZERO non-system indexes anyway). On **#1156** (`identity-cluster.yaml`), since #1156 owns that file (#1146 took main's verbatim to avoid conflict). POST-MIGRATION (guards removed, real indexes): `app` ALSO needs EXECUTE on bt_index_check/bt_index_parent_check + `pg_read_all_data` — documented inline + in E2 design doc §5 item 7 (#1146). The cronjob fail path is fail-SAFE (no false-green) → not a merge-blocker, only a before-go-live correctness fix.
- **gitleaks false positive FIXED** via `.gitleaks.toml` path allowlist `'''charts/cnpg-barman-plugin/manifest\.yaml$'''` on **all 3 branches** (the old commit-less `.gitleaksignore` fingerprint only matched pre-commit `protect`, never CI `detect`, and line-drifts). See [[feedback_gitleaks_detect_scans_all_branches]] — `gitleaks detect` scans ALL fetched branches, so `497db827`'s base64 SIDECAR_IMAGE reddened #1146/#1158; durable cure is the allowlist reaching main at #1152 go-live. gitleaks is INFORMATIONAL (not a required gate).
- **#1156 nits:** header `default_pool_size=20` → PROVISIONAL 25 (matched body+pooler); CANDIDATE→PROMOTED title; "now satisfied"→"satisfied except P3 #875"; GATED-ON reframed as provenance. **#1146 nits:** `HOME=/tmp` env (kubectl cache on RO rootfs); Dockerfile `curl|gpg` pipe split (dash + `set -eux`, no pipefail).
- **Commits:** #1152 `d349e7fd`, #1156 `cb87a0f6`, #1146 `f5ec6102`. All three: Validate Terraform + ci-gate + platform-ci-gate + gitleaks PASS. Deferred (genuine activation-time placeholders, NOT fixable now): read-only restore UAMI, image digest pins (RESOLVE_AT_ACTIVATION), operator `resources:{}` QoS. **#1156 carries a duplicate of #1152's .gitleaks.toml allowlist** (it's based on #1152) → trivial dedup at go-live rebase.

## 2026-06-06 — restore-verify ops image (#1146) de-risk build PULLED FORWARD (commit `142df8b0`)
Built `apps/cnpg-restore-verify-image` locally (`docker build --platform linux/amd64`) to de-risk ahead of activation. **Found + fixed a real bug:** `groupadd -g 26 postgres` collides with **debian:12-slim's existing `tape` group (GID 26)** (uid 26 is free) → would have failed the CI build AND activation. Fix: rename the GID-26 group via `getent` (groupadd fallback). Verified green: runs as uid 26, psql 16.14, kubectl v1.29.15. **Couldn't dispatch the build workflow** — it's `workflow_dispatch`-only and NOT on main, and GitHub only dispatches workflow_dispatch from the default branch → local build is the only pre-merge proof. **Per Milos: no static values in git** — instead of pinning the resolved debian digest + kubectl version/sha into the Dockerfile, the **build workflow now resolves them at build time** and passes `--build-arg DEBIAN_REF` (full digest-pinned ref; default = floating `debian:12-slim` tag so local builds work) + `KUBECTL_VERSION`/`KUBECTL_SHA256` (sha256sum --check still verifies). Resolved-now reference (re-resolve at activation; **kubectl 1.29 is EOL** — confirm AKS server ver): debian@`sha256:0104b334…21eeb`, kubectl `v1.29.15`/`3473e14c…e4516`. The CronJob `image:` stays a placeholder (pinned at activation after an authorised push). actionlint clean, 0 buildkit warnings.

## ⏳ STANDING ACTION (set 2026-06-06) — flip #1156 ready-for-review WHEN #669 clears
**⛔ UPDATE 2026-06-06: Milos PARKED #669** (still OPEN — labels `in-progress, task, epic:platform-redesign`, NOT closed — i.e. de-prioritized indefinitely, not finished). So the whole CNPG go-live gate is now on **indefinite hold**, and the flip below stays DORMANT (correct: the trigger is `#669 closed-as-completed`, which a parked-but-open gate never hits). ssiter's flip cron `6a661347` will simply lapse (7d auto-expire) without firing; the durable intent lives in this note for whenever #669 is un-parked + cleared. Nothing to do on the CNPG lane while parked.
Milos's instruction: "Mark #1156 and #1146 ready for review once #669 clears." **State at time of request:** #669 OPEN (gate NOT met — no ETA, ssiter keeps the hold); **#1156 is draft** (needs the flip); **#1146 is ALREADY `draft=false`/ready** (opened non-draft, held by branch-protection + title, not draft status) → normally NO flip needed, just verify still ready. #1152 also draft (base of #1156) — NOT in the request; leave unless told.
- **TRIGGER = #669 genuinely cleared** (closes as `stateReason: completed`, i.e. ssiter's real all-clear). On that signal: `gh pr ready 1156` (+ `gh pr ready 1146` only if it regressed to draft). Then retire this note + the cron below.
- **DO NOT** flip before #669 is `completed`-closed. **DO NOT** merge anything or touch dev-platform infra — the only allowed mutation is `gh pr ready`. If #669 closes as not-planned/duplicate, do NOT flip — confirm intent with Milos.
- Mechanisms: (1) THIS memory note = the durable cross-session backbone — any future session learning #669 cleared flips #1156 from here. (2) A **session-only** cron is a best-effort auto-flip if #669 closes while this session is alive. The cron is NOT the guarantee; this note is. (Cron history: `a4e4d1d9` lapsed across a session boundary; **re-issued 2026-06-06 by Milos → cron `6a661347`, daily 10:17 local, auto-expires 7d.** State at re-issue: #669 OPEN/no-reason; #1156 draft=true (needs flip); #1146 draft=false (already ready — re-flip only if regressed).) **cnpg-lane session confirms 2026-06-06: its own redundant session cron `a4e4d1d9` DELETED → ssiter's `6a661347` is the SOLE owner of the flip; cnpg-lane stands down (defers to ssiter, who flags the all-clear on #1154).**
