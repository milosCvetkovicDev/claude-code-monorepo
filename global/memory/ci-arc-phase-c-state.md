---
name: ci-arc-phase-c-state
description: ci-arc-parallelisation Phase C (ci-v2.yml shadow) state — PR
metadata: 
  node_type: memory
  type: project
  originSessionId: 00000000-0000-0000-0000-000000000008
---

**Epic `ci-arc-parallelisation` (#835) — Phase C status, 2026-05-29:**

Phases A (#836–840, #842) + B (#841) CLOSED. **Phase C (#843) = `.github/workflows/ci-v2.yml`** — the parallelised CI graph as a NON-REQUIRED shadow on ARC, `name: CI-v2` (the locked Loki join key). Shipped as **PR #1002** (open; not yet merged — safe to merge as informational). Passed a pre-PR 5-agent review + a 6-domain-expert review; fixes applied (`ci-bundle-integrity`→`-large`, `ci-gate` metrics, integration-timing note). `epic.md` frontmatter is **stale** (still says `progress: 60%` / "Phase B not started" — actually ~70%, Phase B done; reconcile via `/pm:epic-refresh` from the PRIMARY `acme/` worktree, NOT a `.claude`-symlink worktree).

**Phase C measurement preconditions — UPDATE 2026-05-29: precondition #1 was a MISDIAGNOSIS, now FIXED:**
1. **ARC `/cache` "health" (was op ticket #1006 Fault B) — RESOLVED via PR #1010 (`be32b056`, 2026-05-29). NOT a dev-sub infra fault — a CI-code bug.** The `ci-v2` ARC jobs hard-failed at `arc-setup-workspace`'s `mountpoint -q /cache` with a misleading "Azure Files PV not bound" error. The truth (verified live on `development-acme-aks`, `arc-system`, via kubectl-cert admin context — I HAVE this access; I do NOT have ARM/`az` to the dev sub): PVC `arc-shared-cache` was **Bound** (1Ti RWX azurefile-csi-premium) the whole time, and the share **IS mounted** — on subPaths `/cache/npm` + `/cache/nx` (cifs), per `charts/arc/templates/runner-scale-set-*.yaml`. The parent `/cache` is (correctly) NOT a mountpoint, so `mountpoint -q /cache` is ALWAYS false → the composite probed the wrong path. **Fix: probe the two CHILD mountpoints** (matching `arc-smoke.yml` which already used `CACHE_MOUNT: /cache/npm`); #1010 also added a writability probe (RO/wrong-owner fsGroup footgun) + `pipefail` on the `npm ci | tee`. Proven end-to-end: on the PR, `ci-affected-shas` advanced past step 4 into `npm ci` for 13 min instead of fast-failing in seconds. **LESSON: "X is not a mountpoint" ≠ "PV unbound" when the chart uses subPath mounts — check the children.** (The prior #1002/#1004 "needs dev-sub kubectl, PVC/PV/StorageClass/CSI" diagnosis in this note's history was WRONG.) Precondition #1 CLEARED. — #1006 **Fault A is MOOT** (corrected by sister 2026-05-29): the legacy VM `development-acme-runner` was DECOMMISSIONED in #949 (verified ResourceNotFound) — there is no VM to restart. The ubuntu-latest fallback IS the only path now for `_ci.yml`/`ci.yml` until the Phase D ARC flip (#844). (Earlier "az vm start" advice was wrong — VM gone.)

**Platform image-build outage 2026-05-29 — ROOT CAUSE = per-worker OOM (NOT timeout, NOT broken code), FIXED Phase 0 #1014.** platform-pipeline `ci / ci` AND required `CI / main` both failed on accounting/inventory `:test` → build-push skipped → dev-platform crashloop on stale images (#970→#982 consumer fix undeployed). Tests PASS locally (isolation + full 37-project serial batch w/ coverage); fail only on 2 vCPU/7 GB hosted fallback. Shared vitest factory `libs/platform/service-bootstrap/src/testing/vitest-config.ts` ran `forks:{singleFork:false}` (uncapped concurrent forks) → kernel OOM-kills random workers → opaque task fail, NO assertion/summary output, different innocent services die each run (the "memory lottery" — verbatim in ci.yml/_ci.yml test-step comments). `NX_PARALLEL=1` (#1001) only serialises Nx PROJECTS, not vitest intra-project file workers. **Fix: `fileParallelism:false` (Vitest 4 top-level API — `maxForks` does NOT exist in v4, would be a silent no-op) + keep `isolate:true` (#823/#824 leak guard), in the factory unit branch AND the two libs with their OWN configs (event-bus, service-bootstrap).** PR #1014 — `CI / main` GREEN on 2-core (proven). **Must MERGE #1014 to fire build-push on next green main.** 8-expert panel reviewed the full "reliability-first then ARC scale" plan → filed Phases 1-4 = #1016 (determinism: fake timers + ESLint guard; @acme/clock port deferred — circuit-breaker uses cockatiel, erp-retry passthrough intentional), #1017 (de-monolith gate: 13 `test:unit` targets MUST exist before flipping; per-service build-push; npm ci dedup), #1018 (interim coverage capacity — conditional, measure after 0/1), #1019 (real ARC cutover — revives descoped #844; NO-GO until Azure DSv5 quota [DECLINED on sponsorship sub → needs alt SKU/sub] + budget alert + recover script + ci-v2 NX_PARALLEL parity + 2-step node apply + NEW workflow not ci-v2 rename). Dev ARC is 1.5 vCPU/2Gi/maxConcurrent=1 (Path III, ADR-0040 Amd 1) — moving CI there WITHOUT the quota+sizing is regressive vs ubuntu 2-core; faster hardware would WORSEN the OOM. epic.md frontmatter still stale (`/pm:epic-refresh` from primary worktree).
2. **#1003 — broken comparison basis (CRITICAL) → FIXED, MERGED via PR #1005 (`a1b13ce2`, 2026-05-29); #1003 CLOSED. Precondition #2 CLEARED.** `ci.yml` runs entirely on `ubuntu-latest`, where the OTel→Loki collector (ARC/self-hosted ONLY) is absent → `ci.yml` metrics never reached Loki → `ci-shadow-report.yml`'s `CI` column was permanently empty → the `CI-v2 ≤ 0.70 × CI` wall-clock gate was a silently-suppressed division-by-zero. **PR #1005 rewrites `ci-shadow-report.yml` to measure BOTH workflows at the RUN level via the GitHub Actions API** (`updated_at − run_started_at` wall-clock + `conclusion`), dropping the entire Azure/AKS/Loki chain. Validated live (14d): `ci.yml` n=370 (was 0), `ci-v2` n=14 all-failing (the /cache precondition). GOTCHA found: `gh api --slurp` is incompatible with `--jq` → pipe to a separate `jq`; `created>=` must be URL-encoded `%3E%3D`. Once #1005 merges, precondition #2 is cleared; #1 (ARC /cache health) still gates the window.

**Why:** the shadow's whole purpose (parity + wall-clock) silently produces no baseline because the legacy workflow it compares against moved off the metrics-instrumented runners. Easy to miss — the report renders a zero-row CI column rather than erroring.

**How to apply:** before declaring Phase C "running" or starting the 50-PR count, verify (1) ARC `/cache` bound and (2) #1003 resolved. Phase D (#844) gate-hardening prereqs: **#1/#2/#4 DONE — PR #1007 MERGED (`25f319dd`, 2026-05-29)** — `ci-gate` redesigned into MUST-SUCCEED (ci-affected-shas, lint, typecheck, build, test-backend, test-other, production-safety → require `success`; fork-skip/regression fails the gate) vs MUST-NOT-FAIL (frontend-shard, bundle-integrity, integration → allowlist `success|skipped`); the 3 validators moved from the deleted ARC migration-registry-check job onto ubuntu production-safety (run during ARC outages). #1007 ALSO fixes a latent bug: `ci-gate` referenced local `emit-job-metrics` composite with NO `actions/checkout` (introduced #1002) → erroring at job-prep on main since #1002; added checkout. Gate logic bash-verified across 6 scenarios. **#3 (paths-ignore docs-only required-check deadlock) NOT done** — same situation as ci.yml's required `main` today (admin-merge docs PRs); it's a branch-protection-config decision deferred to the #844 flip. Fork coverage policy (fail vs reduced hosted matrix) also a #844 decision; forks fail the gate by design now. Do NOT flip `ci-gate` to required until #1007 merges + #3 decided + the window completes. See [[dev-runner-vm-topology-and-fallback-oom]] (a separate ARC-health layer) and [[feedback_arc_rbac_supplement_missing_list]] (a third).

---

**2026-05-29 PM — Platform image outage UNBLOCKED (no runner needed).** Root cause of the deploy gate (`ci / ci`, `_ci.yml`) failing: its test step ran `nx affected -t test -- --coverage` with **NO heap cap**; coverage enforced nothing (no thresholds in the shared vitest factory `libs/platform/service-bootstrap/src/testing/vitest-config.ts`) and was never uploaded (lcov discarded) → **pure-waste v8 instrumentation = prime OOM driver**. Fix = drop `--coverage` + cap that call to `--max-old-space-size=2048`. **PR #1014 (reduced to `_ci.yml`-only) merged → `20313cd1`.** Then `gh workflow run platform-pipeline.yml --ref main -f sha=223b655c` (#982 event-bus reconnect fix) → `ci / ci` green → **11/12 service images built+pushed** (all #982 consumers: accounting/commission/inventory/document/notification + auth/gateway/trading/tenant/reporting/user). **`audit-service` build FAILED separately**: in-Docker `nx run platform-audit-service:build` produced no dist → `COPY … "/app/dist/apps/platform/audit-service": not found`; **masked by the Dockerfile's `… || true`** (a CI bug — build failures become opaque COPY errors). Event-bus #982 change was purely **additive** (new exports only) so not the cause; likely an audit-service compile error or build-OOM lottery — separate/pre-existing, NOT a #982 consumer, keeps its prior image (no regression). To unblock images you must DISPATCH at the service-affecting SHA — a workflow-only merge has empty `nx affected` (`base=SHA~1`) so `_build-push` skips all services.

**Systemic (the real reason self-hosted/shard matters):** the required `CI / main` (`ci.yml`) CANNOT run a **shared-lib-affected** full Platform suite on 2-core/7 GB — it OOM-kills the heaviest (commission+accounting, silent SIGKILL) **even at `NX_PARALLEL=1` + 2048 cap + no coverage**. So it blocks **every** shared-lib PR. The factory forward-guard (`fileParallelism:false`) was reverted from #1014 precisely because it marks all 16 services affected → trips this. `fileParallelism:false` is a near-no-op on 2-core anyway (vitest already defaults to ~1 fork there) — it's a forward-guard for multi-core ARC.

**Capacity (CORRECTED — quota raise was REQUESTED PREVIOUSLY AND REJECTED).** dev sponsorship sub **Total Regional vCPUs = 10, fully consumed** (4 system + 4 user + 2 arc-pool `D2as_v7`); **DASv5 family = 0**; the legacy 8-core runner was deleted (#949) to free that headroom. So NO 8-vCPU self-hosted runner fits in the AKS region without a raise (rejected). Durable workarounds (decision PENDING): **(A)** revive `infra/modules/github-runner/` 8-core VM in a **quota-free REGION** (Total Regional vCPUs is per-region; needs one `az vm list-usage -l <region>` check) — keeps self-hosted, no GitHub per-minute cost; **(B)** shard the Platform tests across free **standard** ubuntu-latest (keep a `main` gate job that *waits on* a matrix of shard jobs → preserves the required-check name, no branch-protection churn; each shard runs its services serially with full 7 GB) — no Azure, fast via fan-out, not a "bigger runner". I recommended B. arc sizing inputs live at `infra/environments/development/main.tf:671-680` (`node_sku`/`runner_max_count`/`quota_family`/`vcpu_per_node`, `skip_quota_check=true`).

**2026-05-29 PM — Workaround A BUILT + APPLIED (self-hosted runner in ukwest). PR #1024 (`feat/self-hosted-runner-ukwest`, open).** Quota raise was rejected; uksouth Total Regional vCPUs = 10/10 maxed → a runner can't fit beside AKS. A GH Actions runner only needs OUTBOUND, so it lives cross-region in **ukwest** (own VNet `10.40.0.0/24` + Standard public IP `203.0.113.12`, NSG denies all inbound) on **`Standard_D8as_v4`** (8 vCPU/32 GiB). **KEY LESSON: quota ≠ availability.** `az vm list-usage` showed ukwest Dasv7 = 0/10, but `D8as_v7` apply failed "not available in region" — ukwest doesn't OFFER the v7 SKUs. Cross-ref with **`az vm list-skus -l <region> --size <sku>`** (restrictions=[]): in ukwest only DASv4 is both offered AND has quota (DASv5=0, DASv6 no quota, Dasv7 quota-but-no-SKU). Both agents `acme-ukwest-01/02` registered + **online** (label `acme-ukwest`), services active. Live IDs: VM `development-acme-ukwest-runner` (RG `development-acme-rg`), runner MI `00000000-0000-0000-0000-000000000034`, OIDC SP `00000000-0000-0000-0000-000000000056`, admin key `~/.ssh/acme_ukwest_runner`. **REMAINING (manual, Owner/PIM — Milos's account + CI are only Contributor, classifier also blocks agent RBAC grants):** (1) `az role assignment create "Virtual Machine Contributor"` OIDC-SP→VM (for CI `az vm start`); (2) `az lock create CanNotDelete`; (3) the cutover — flip heavy jobs to `runs-on: [self-hosted, acme-ukwest]` behind a `main` GATE job (preserves required-check name), keep ubuntu fallback, then revert the `_ci.yml` coverage/cap workaround (32 GiB has headroom). Runbook: `docs/runbooks/cicd/self-hosted-runner-ukwest.md`. **Gotchas hit:** the `source-driven-dev.sh` hook blocks TF edits until `.claude/.source-driven-dev/terraform.fetched` exists (fetch azurerm docs first); the `launch-readiness-gate.sh` hook blocks `terraform apply`/`helm install` until `.claude/.launch-readiness-passed` exists AND is found relative to CWD (use `terraform -chdir=` from repo root, don't `cd` into the env dir or the relative `.claude/` lookup misses). Dev-env local `terraform plan/apply` needs 8 `TF_VAR_*` (erp/ghcr/entra/comms/ssh) — supply placeholders for the 7 unrelated ones + `-target` the isolated resources.

**2026-05-29 PM — audit-service build fix + full cutover drafted (the "b then a" pair).**
- **audit-service image-build failure ROOT-CAUSED + fixed → PR #1025 (`fix/platform-docker-build-mask`, `2ffa493`).** NOT a code defect — `apps/platform/audit-service` builds clean locally (nx + a cold `docker build --target build`, EXIT=0). `apps/platform/Dockerfile` **build stage** ran `RUN npm ci … && npx nx run …:build && npm cache clean --force || true`; shell precedence binds `|| true` to the WHOLE chain → a transient `npm ci`/`nx build` failure exits 0, the empty-dist layer is exported to the GHA buildx cache (`cache-to: mode=max`) and reused forever → prod-stage `COPY /app/dist/apps/platform/<svc>` → "not found". The **deps stage was already correctly parenthesized** `&& (npm cache clean --force || true)`; only the build stage lost the parens. Fix = one line: `&& (npm cache clean --force || true)` — self-busting (changing RUN text changes the layer cache key). **LESSON: a trailing `|| true` on a multi-cmd `&&` RUN masks ALL prior failures and buildx CACHES the broken layer → silent stale/empty images. Scope soft-fails in a subshell.** In-Docker nx has NO remote-cache creds (CI-task env only) so it always builds fresh → isolates the fault to the buildx GHA layer, not Nx cache.
- **Full runner cutover DRAFTED (user chose "Full cutover now", legacy pattern) → PR #1027 (`feat/ci-cutover-self-hosted-ukwest`, `b54318c`, DRAFT).** 3 files: `ci.yml` (required `CI / main`) → `start-runner` + `main`@`[self-hosted, acme-ukwest]` (NX_PARALLEL=6, heap 8 GiB, frontend inline) + `fallback-main` (ubuntu, 2-core guards) + `fallback-frontend-tests` + `integration-tests needs [main,fallback-main]`; `_ci.yml` → `ensure-runner` + `ci`@self-hosted + `ci-fallback` (ubuntu, #1014 guards) + `bootstrap-harness needs [ci,ci-fallback]`; `actionlint.yaml` registers `acme-ukwest`. Both YAML-parse + actionlint clean. **DRAFT on purpose — needs (1) #1024 merged, (2) the manual VM-Contributor grant, (3) one validated self-hosted run.** **KNOWN FRAGILITY (in PR body): VM down → `main` skips (skipped required check = PASS) while real work runs in NON-required `fallback-main` → a fallback failure wouldn't block merge.** Proper close = ALSO mark `fallback-main` required (branch-protection edit, admin) so exactly one of {main,fallback-main} is enforced each run. `_ci.yml` ci/ci-fallback have no equivalent hole (gate via workflow conclusion). Confirmed: `platform-pipeline.yml` calls `_ci.yml` with `secrets: inherit` (ensure-runner OIDC works); ukwest cloud-init installs Docker+Node+Bun (Trivy builds viable). **Gotcha: stray backticks inside `run:` echo strings trigger shell command-substitution (hit + fixed once).**

---

**2026-06-01 — ci-v2.yml shadow DISABLED (`gh workflow disable`, `disabled_manually`).** User-approved. Reason: dev ARC scale set is `max=1` (1.5 vCPU/maxConcurrent=1) → ci-v2 runs queue behind the single runner and get cancelled by newer push/PR before completing → **zero valid measurement** (ci-shadow-report has no completed CI-v2 data) WHILE leaving every PR `UNSTABLE` (the shadow's `ci-affected-shas`/`ci-v2-gate` checks never resolve) + churning arc-pool runners. ci-v2's checks are NON-required (`ci-v2-gate` ≠ the required `ci-gate` from ci.yml, per #1032) so disabling has ZERO effect on merge-ability — it only removes noise. Also cancelled the 7 lingering queued runs. **RE-ENABLE (`gh workflow enable ci-v2.yml`) only when ARC has real capacity = Phase 4 #1019 (gated on the DECLINED DSv5 quota → needs alt SKU/sub).** Until then the Phase-C wall-clock measurement cannot run regardless. Note: `ci-shadow-report.yml` now has no new CI-v2 data (dormant, won't error per #1005's empty-safe rewrite). Standing directive [[feedback_keep_dev_platform_healthy]]: verified dev-platform health unperturbed by this (12/13 services 1/1; ai-service B4/#957 known crashloop).

**2026-05-31 — Phase 1 (#1016) test-determinism shipped → PR #1036 (`fix/platform-test-determinism-1016`, worktree `~/projects/acme-det-1016`, OPEN).** Converted the 2 real-wall-clock-wait specs to vitest fake timers: circuit-breaker.service.spec.ts (4× ~150ms → `advanceTimersByTimeAsync`; cockatiel recovers via a `Date`-based elapsed check VERIFIED in node_modules/cockatiel source — NOT a scheduled timer, so the fake clock drives OPEN→HALF_OPEN; tests 11ms) + rabbitmq-connection.spec.ts (reconnect ~50ms → advance scheduleReconnect's setTimeout). **Guard = NEW `.eslintrc.platform.json` (no-restricted-syntax) that ALL 34 `apps/platform/*`+`libs/platform/*` project configs `extend`** — CANNOT live in root `.eslintrc.base.json` because ESLint resolves an extended config's `files` globs relative to each EXTENDING project dir (`src/...`), so a root path-glob (`apps/platform/**`) never matches; also the project configs' `extends` of a `root:true` base TERMINATES directory cascade, so intermediate `apps/platform/.eslintrc.json` is NOT read either → per-project extends is the only platform-scoping that works. Selector bans ONLY the unconditional `new Promise(r => setTimeout(r, ms))` sleep (concise-arrow + block-body + function-expr; first setTimeout arg = Identifier) → does NOT flag `Promise.race` watchdogs (`setTimeout(()=>reject(),ms)`, arrow arg) or lifecycle timers. esquery `[body.arguments.0.type='Identifier']` numeric-index DOES work. Carve-outs: erp backoff/throttle + mock-latency + rmq connect-backoff (clock-port inventory) + notification 0ms tick + tenant-context ALS-concurrency (per-line); jwt-guard's genuine 25ms wait → `setImmediate` (deterministic, no carve-out). **Ambient `Date.now()`/`new Date()` ban (335 reads/127 files) DEFERRED to @acme/clock port — can't be green before SystemClock.** platform-frontend (React) intentionally out of scope. Verified: determinism ×3 under `--fileParallelism`, guard fires under `nx lint` (negative test), 0 violations across 34 projects. Source-driven-dev hook for @nestjs/@mikro-orm src carve-outs needs breadcrumbs at the SESSION dir's `.claude/.source-driven-dev/` (hook CWD = session project, NOT the worktree). Phase 2 (#1017, can run parallel) + #1018/#1019 still open.

**2026-06-01 — #1036 CI OOM lesson → guard SCOPE-REDUCED to 3 apps; green+mergeable (`c2c50cf2`).** The full-34-config guard rollout made CI fail DETERMINISTICALLY: each project's `.eslintrc.json` is in its `default` test input (`{projectRoot}/**/*`), so touching all 34 marked every platform project's TEST affected → `nx affected -t test` ran all 34 build+test concurrently on the ukwest self-hosted runner (NX_PARALLEL=6, per-project `--maxWorkers=6`) → OOM-killed a RANDOM heavy suite (gateway run 1, commission run 2 — the "memory lottery"; 33/34 passed each time, opaque process-kill with NO vitest summary). **PRs are READ-ONLY on the remote nx cache (write token is main-only) → re-run re-executes all 34, no cache hits → futile.** Scoping the guard configs to 5 did NOT help: **foundational libs (event-bus, mikro-orm) propagate to all dependents via `^production`** (which INCLUDES `.eslintrc.json` — only specs/tsconfig.spec/test-setup/vitest.config are excluded), AND **the required `rabbitmq-connection.spec.ts` conversion ALONE affects 16 projects** (event-bus is depended on everywhere; a SPEC change there still propagated to 16 — apparent pre-existing nx input quirk). So ~16 is the affected FLOOR for the 2 conversions; scoping can't go lower. **Resolution (user chose "scope to changed projects"): guard kept ONLY on the 3 changed APPS (gateway/accounting/notification — apps have no dependents, don't propagate); reverted guard+carve-outs from the other 31 incl. event-bus/mikro-orm; BOTH spec conversions still ship (specs don't propagate to dependents' tests... mostly).** Affected 34→~16 → passed (half the load that barely OOM'd). **Full-platform guard rollout DEFERRED to a follow-up coupled with the CI-capacity fix: either lower NX_PARALLEL/maxWorkers (option C) OR exclude lint config from the `test` named-input (`!{projectRoot}/.eslintrc.json`) so config changes don't bust test-affected (option A).** GOTCHA when diagnosing locally: `nx affected --base=origin/main` is polluted by upstream drift after origin/main advances — use `--base=$(git merge-base HEAD origin/main)` or `--files=` to isolate YOUR change's footprint. **#1036 MERGED 2026-06-01 (squash `29cb5de2`) → #1016 CLOSED.** 2 code reviews (agent-skills + pr-review-toolkit) both APPROVED; fixed one nit (stale RED-phase header comments in the 2 converted specs). Cleared strict-mode BEHIND via local `git merge origin/main` (no conflicts; verified affected stayed 16, upstream mikro-orm edits already in CI base). Validate Terraform path-filter resolved SUCCESS (no admin merge needed). Worktree/branch cleaned up. **Net shipped: 2 fake-timer conversions (gateway circuit-breaker, event-bus rabbitmq — verified deterministic ×3) + determinism guard on 3 APPS only (gateway/accounting/notification).** **STILL OPEN: full-platform guard rollout** — blocked on the CI-capacity fix (option C: lower NX_PARALLEL/maxWorkers, OR option A: `!{projectRoot}/.eslintrc.json` excluded from `test` named-input so config changes don't bust test-affected). This OOM IS the capacity problem Phase 2 #1017 (de-monolith) + #1018 (capacity) exist to fix — do the full guard rollout AS PART OF those.

**2026-05-31 — cutover #1027 MERGED (`46281fa1`); branch protection now requires `ci-gate` (ci.yml) + `Validate Terraform` (was `main` + `Validate Terraform`).** The required `CI / main` skip-as-pass hole is closed BY the aggregator design: `ci-gate` `needs:[main, fallback-main]` and fails unless one succeeded — so requiring `ci-gate` (not `main`) is the proper fix, no separate fallback-required edit needed. **COLLISION found + fixed (PR #1032, merged `536283c`-era):** `ci-v2.yml` (shadow) ALSO had a job named `ci-gate`; GitHub matches required checks by NAME across ALL workflows, so the failing shadow `ci-gate` blocked EVERY PR once `ci-gate` became required. Fix = add `name: ci-v2-gate` to the shadow's gate (job **id stays `ci-gate`** to preserve the Phase-D mirror; only the check-run NAME changed). **Phase-D implication (#1019/#844): `ci-gate` is ALREADY the required context (from ci.yml); the shadow's check is now `ci-v2-gate`. The ARC flip must re-point branch protection / re-converge the names then — do NOT assume the shadow still surfaces as `ci-gate`.** Unblocked + merged the two stuck dependabot PRs: **#977** (github-actions group) + **#609** (terraform azurerm ~>4.71). **Self-hosted runner flake seen:** #609's first rebased run hit a transient `setup-bun` failure in `CI / main` (zero-byte log, action-setup step, no fallback because start-runner succeeded so `fallback-main` skipped) → cleared on a fresh `gh pr update-branch` run. Pattern: a transient failure in the self-hosted `main` job AFTER start-runner succeeds has NO fallback (fallback-main is gated on start-runner FAILURE) → re-run/re-update is the remedy.

**2026-06-01 — `ci-affected-shas` (the CI-v2 SHADOW job) is runner-starved / stuck-queued → leaves PRs perpetually `UNSTABLE` (it never resolves). It is NON-required. Merge Platform PRs on the required checks ONLY (`ci-gate` from ci.yml + `Validate Terraform`).** Confirmed by the dev-platform recovery session (it merged #1040–1043 on required-only) and observed on #928 / #1037 (where `ci-affected-shas` sat pending while `ci-gate` + `Validate Terraform` went green). If the shadow gate is ever meant to be required, it can't currently complete under ARC capacity — don't wait on it.

---

**2026-06-01 — Phase 2 (#1017) de-monolith FOUNDATION MERGED → PR #1046 (squash `f007e958`; `chore/platform-test-unit-gate-1017`). #1017 stays OPEN (build-push fan-out / npm-ci dedup / platform-ci-gate aggregator / branch-protection flip remain — see issue comment).** Green end-to-end on the PR (it marks all 13 services affected via project.json edits): `ci-gate`+`Validate Terraform`+`main`+`production-safety`+`ci / ci` all SUCCESS; the new gate ran build + 13× test:unit, 0 test (all 13 excluded by name). mergeState CLEAN, normal squash-merge (no admin). Shipped the **blocking prerequisite + the `ci.yml` half** of "make `test:unit` the required-gate command". (1) Added a `test:unit` Nx target (each service's existing `vitest.config.ts`, no coverage — coverage is opt-in via `--coverage`, off by default) to **all 13 Platform services** (0 existed before). **`nx show projects --with-target test:unit` = 14** — the 13 services **+ legacy-api** (legacy's `test:unit` PRE-EXISTS, jest, `testPathIgnorePatterns:["test/integration"]`, no DB; NOT literal in `apps/legacy-api/project.json` so a text-grep misses it — use `nx show projects --with-target`). (2) Restructured `ci.yml`'s required test step in BOTH jobs (`main` self-hosted 8-core parallel + `fallback-main` 2-core `NX_PARALLEL=1`) from the old 2-command `build,test:unit` + `test` into **3 sequential commands**: `build` (all affected, parallel) → `test:unit` (legacy + 13 svc, no coverage; serial on fallback for OOM) → `test` (everything else, **13 services excluded BY NAME**). `_ci.yml`/`platform-ci.yml` UNCHANGED. Verified: ran all 13 `test:unit` serial green (`NX Successfully ran target test:unit for 13 projects`), excludes resolve, actionlint+YAML clean, `nx affected` on the PR marks all 13 services (project.json edits) so the PR itself end-to-end-exercises the new path.

**CRITICAL LESSON (a context-aware code-review caught this; an early draft using `tag:scope:platform` for the exclude was a BLOCKER): `--exclude=tag:scope:platform` is TOO BROAD.** Among projects with a `test` target, `scope:platform` matches **16, not 13** — it ALSO tags **`platform-frontend`** (React, type:app, layer:presentation), **`platform-filter-presets`** (type:lib), **`@acme/migration-scripts`** (type:scripts), which have `test` but **NO `test:unit`** → excluding by tag SILENTLY DROPS them from the required gate (they run `test` today). **Fix = exclude the 13 services BY EXPLICIT NAME** (`platform-accounting-service,...,platform-gateway,...,platform-user-service`). Explicit names also **fail safe**: a future stray `scope:platform` project without `test:unit` keeps running `test` rather than being dropped (worst case = harmless double-run, never a coverage gap). Platform **libraries are tagged `scope:platform-shared` (NOT `scope:platform`)** so they correctly keep running `test` in cmd3 regardless. `nx --exclude` does exact-tag match (not substring) and accepts mixed names+`tag:`; shell GLOBS (`platform-*-service`) are fragile (zsh `nomatch`) and not an existing pattern — don't use them.

**Two stale-comment LESSONS (verify against the WORKTREE = origin/main, not an older checkout):** (a) origin/main's shared vitest factory `libs/platform/service-bootstrap/src/testing/vitest-config.ts` unit branch is **`pool:'forks', forks:{ singleFork:false, isolate:true }`** — NOT `fileParallelism:false`. The #1014 `fileParallelism:false` claim is STALE on main; the real OOM bound on the 2-core fallback is **`NX_PARALLEL=1` (cross-project)**, same containment the old `test` run used. (b) **`_ci.yml` no longer runs `--coverage`** (removed — "enforced nothing, never uploaded"); only `platform-ci.yml:105` still passes `--coverage`. So ci.yml NEVER ran Platform coverage → routing Platform `test`→`test:unit` is NOT a coverage regression (same specs, no `--coverage` either way). Reviews: adversarial (diff-only, 14 findings — most were pre-existing excludes or wrong re GitHub's default `bash -eo pipefail` / nx.json already defining `test:unit` targetDefault `cache:true`) + context-aware code-reviewer (found the real blocker; APPROVE-after-fix). **Remaining #1017 (follow-up PRs): per-service build-push fan-out in `platform-pipeline.yml`, `_build-push.yml` 12× npm-ci dedup, the `platform-ci-gate` aggregator job, then the branch-protection flip. Full-Platform determinism-guard rollout (deferred from #1036) also belongs with this capacity work.** CI-only change, health-neutral for dev-platform (no deploy-path/charts/image edits).

**2026-06-01 — Phase 2 (#1017) item "12× npm-ci dedup" MERGED → PR #1050 (squash `fb021e87`; `chore/platform-build-push-affected-dedup-1017`).** `_build-push.yml`: the build-push matrix ran a full `npm ci` + `nx show projects --affected` ONCE PER LEG (12× per run) just to decide whether to build each service. Replaced with a single **`detect-affected` pre-job** that computes the affected-app set once (`nx show projects --affected --base=$SHA~1 --head=$SHA --type=app --json`) and fans it to the 12-leg matrix via job outputs (`affected_json` + `detection_failed`); per-leg `npm ci`/`nx`/`Setup Node.js` removed, legs read the outputs via jq membership. **Identical image set built — PROVEN** (charts-only all-FALSE, event-bus shared-lib all-12-TRUE, exact-match edge `platform-auth`≠`platform-auth-service`; 0 mismatches vs the old `grep -Fx` path).

**LESSON — nx `show projects` output format is PLATFORM-DEPENDENT (nx 22.6.4): newline list on Linux, JSON array on macOS.** The pre-existing per-leg code piped raw stdout to `grep -qFx` (works on CI/Linux's newline output; would silently match NOTHING on macOS's JSON). NOT a CI bug (verified against a real run: `platform-auth-service is affected — building` came from the grep-success path) — but fragile. Hardened to **`--json` + jq** (platform-independent, immune to a future default-format flip). **Always verify nx output assumptions against an ACTUAL CI run, not local** — local macOS gave JSON and nearly led to a false "the code is broken" claim.

**LESSON — centralising a per-leg JSON parse introduces a NEW silent-skip path (a 4-lens adversarial review caught this as a BLOCKER, reproduced).** An unguarded `jq -c '.'` normalise: if nx exits 0 but stdout is empty/contaminated (npx/Nx notice, node warning) jq fails, and under `set -uo pipefail` (no `-e`) the script CONTINUES, writing `detection_failed=false` + empty `affected_json` → every leg reads empty → membership jq on empty stdin → `else` → `affected=false` → **all 12 skip → green build-push → seal uses stale main tags → silent stale-image deploy** (the worst deploy-path failure). The old `grep -Fx` (no JSON-parse intermediary) couldn't blank the whole set this way, so `--json` made it LESS robust to stdout noise. **Fix: route ANY parse/non-array/empty/contaminated output to the SAME fail-safe as an nx failure (`detection_failed=true` → build EVERY service)** via `jq -ce 'if type=="array" then . else error end'`; PLUS a leg-side `type=="array"` guard (defence-in-depth, builds fail-safe on empty/garbage). Guiding invariant: **a wrong SKIP = a stale deploy, so every anomaly fails TOWARD building, never toward skipping.** `npm ci` failure stays loud (composite hard-fails → detect job fails → build-push skipped → deploy blocked). Both fixes reproduced + verified across empty/contaminated/object/scalar inputs. Two follow-up code reviews (agent-skills five-axis + pr-review-toolkit silent-failure-hunter) both APPROVE/MERGE, 0 blockers; fixed one review nit (header said "pipeline fails" — a skipped build-push is `skipped` not `failure`, still deploy-blocking via unsatisfied `success()`). **Build-path only — no charts/argocd/deploy change, does NOT auto-deploy on merge; dev-platform health unaffected.** Build-push correctly does NOT run on the PR (run_build_push=false on PRs; OIDC federation is `:ref:refs/heads/main`-only → can't exercise build-push pre-merge; post-merge main-push is the real integration test, fail-safe regardless). **Remaining #1017: per-service build-push gating (each image gates on its own affected test, not the monolithic `ci / ci` — the architecturally HEAVY item: needs `_ci.yml` to expose per-service test outcomes), the `platform-ci-gate` aggregator + prove green ≥3 PRs, the branch-protection context flip, and the deferred full-Platform determinism-guard rollout.**

**2026-06-01 — Phase 2 (#1017) per-service build-push gating + `platform-ci-gate` MERGED → PR #1057 (squash `c3aacc5a`, `--admin`).** De-monoliths build-push: each image gates on its OWN `test:unit`, not the aggregate `ci`. `_ci.yml` (`ci`+`ci-fallback`) runs a single-job per-service `test:unit` accumulator (bootstrap-harness pattern, NOT a 13× npm-ci matrix) emitting a `{service:"success"|"failure"}` map as a `workflow_call` output; threaded via `platform-pipeline.yml needs.ci.outputs.test_results` → `_build-push.yml` input `ci_test_results`; each leg builds iff affected AND `=="success"`, else HOLDs last-good via the seal. New `platform-ci-gate` job (dispatcher) = the per-service red signal (named `platform-ci-gate` NOT `ci-gate` — #1032 collision). NOT required yet (prove green ≥3 PRs → branch-protection flip remains). Post-merge main run `26766453169` GREEN: all 13 legs ran the new gate with `ci_test_results={}` (workflow-only merge → all skip), seal validated 13 last-good tags, deploy skipped. PR-time `platform-ci-gate` also green (`resolve=success ci=success test_results={} → green`).

**CRITICAL LESSON — GitHub DROPS a reusable workflow's `workflow_call` outputs when the called workflow concludes `failure`.** An expert panel (github-actions + test-arch + devops) caught this as a design-breaker pre-implementation: the naive "accumulate the map, then `exit 1` if any service failed" makes the map VANISH at the caller on every test failure → build-push's loud-guard fires → de-monolith silently reverts to monolithic. **Fix (the pivot, and it's simpler): per-service `test:unit` failures must NOT fail the `ci` job** (the map carries the signal) — so `ci` succeeds on a partial-red run and outputs propagate; build-push keeps its NORMAL `needs: ci` success-gate (no `if: always()`, which ALSO removes the deploy-from-failed-CI hazard); the red signal moves to the separate `platform-ci-gate` job. Only HARD gates (lint/build/typecheck/Platform-lib `test`) fail `ci`, which correctly drops the output and blocks ALL builds.

**LESSON — the accumulator's affected base MUST match `_build-push.yml`'s `detect-affected` on the deploy path (silent-failure-hunter BLOCKER B1).** Accumulator used nx-set-shas (base = last GREEN run) while detect-affected pins `$SHA~1..$SHA`. On a RE-DISPATCH of an already-green SHA, nx-set-shas base==HEAD → empty map → build-push's "affected but absent → exit 1" FALSE-REDs a legit rebuild (the documented "dispatch at a service-affecting SHA" workflow). **Fix: branch the accumulator on `github.event_name` — `$SHA~1..$SHA` on push/dispatch (identical to detect-affected), nx-set-shas only on PR (where build-push doesn't run).** Safety direction of the TEST gate INVERTS #1050: a service we can't confirm passed must NOT be built (hold last-good); a genuinely-affected service ABSENT from the map loud-fails. Verified: 24-case bash harness + actionlint + nx `test:unit <svc>` colon-target resolves to `<svc>:test:unit`. Two reviews MERGE. ai-service now in the build matrix (#957) = 13 services = 13 test:unit targets, map keys 1:1 with legs. **Note for `_ci.yml` `test:unit` accumulator: exclude the 13 services BY NAME in the lib-`test` step (the #1046 `tag:scope:platform`-too-broad blocker).**

**2026-06-02 — Phase 2 (#1017) BRANCH-PROTECTION FLIP DONE. `platform-ci-gate` is now a REQUIRED check on `main`** (alongside `ci-gate` + `Validate Terraform`, all `app_id:15368`, strict preserved). **Required a PRECURSOR (PR #1078, squash `e6dcf32a`) — could NOT just flip.** The blocker: `platform-ci-gate` is produced only by `platform-pipeline.yml`, whose PR trigger was a `paths:` ALLOWLIST. **GitHub leaves a required check whose workflow is path-skipped in "Expected" and BLOCKS the PR** (docs: "Handling skipped but required checks") — so requiring it as-is would block EVERY non-Platform PR (legacy/infra/runner-image). Proven by #1075 (apps/runner-image) which merged CLEAN with NO `platform-ci-gate` report. **THE CANONICAL ACME FIX (the model `terraform-validate.yml` documents in its own header lines 3-9, and `ci.yml` via `paths-ignore`): a required check's workflow must run on EVERY PR (no `paths:` allowlist) and gate the heavy work INSIDE.** Precursor did exactly that on platform-pipeline.yml: PR trigger `paths:` → `paths-ignore: [docs/**, **/*.md, .gitignore, LICENSE]` (matches ci.yml → identical admin-merge set); `resolve` job gained a checkout + `has_platform` output (`git diff` vs `github.event.pull_request.base.sha` grep for the Platform surface, **uncertain→TRUE fail-safe** so a real Platform change is never skipped); `ci` job `if: needs.resolve.outputs.has_platform == 'true'` (skipped on non-Platform PRs); `platform-ci-gate` (`needs:[resolve,ci]`, `if: always()`) added an early `HAS_PLATFORM != 'true' → exit 0` GREEN branch (fires BEFORE the ci-result check so a real red is never masked). **SINGLE workflow → no duplicate-name race** (a companion `paths-ignore` skip-workflow is UNSAFE: a mixed Platform+non-Platform PR triggers both → its always-green skip job can mask the real gate's red; rejected). **The has_platform path list MUST be a superset of nx.json `sharedGlobals`** (`tsconfig.base.json` + `.github/ci-cache-version.txt`) + `nx.json` — a review caught that omitting them = a change re-hashing EVERY Platform project (`nx affected` marks all) would skip ci and merge gate-green; added to BOTH has_platform AND the push `paths:` allowlist (which had the same hole on the build side). **The robust long-term alternative is to drive has_platform from `nx show projects --affected` instead of a hand-maintained regex (drift risk).** Verified: actionlint+prettier; 13-case bash harness; **the precursor PR's own `platform-ci-gate` green (normal path)**; and **SKIP-PATH PROVEN END-TO-END** via throwaway probe PR #1081 (root `.txt` only → `resolve=success, ci=SKIPPED, platform-ci-gate=SUCCESS`) BEFORE flipping. 3-lens github-actions review APPROVE (verified the skipped-ci + if:always() → green-status chain + that build-push/deploy don't break when ci skipped: run_build_push/run_deploy are true only on push/dispatch where has_platform is forced true). Flip = `gh api --method PATCH branches/main/protection/required_status_checks` adding the 3rd check. before/after JSON in #1017 comment. **This was the 4th+ green-pipeline PR; #1017 now has ONLY the full-Platform determinism-guard rollout (deferred from #1036) remaining.** GOTCHA: for `pull_request`, GitHub runs the workflow from the PR HEAD (merge ref), so a workflow trigger/logic change takes effect for the PR itself — but a precursor touching `platform-pipeline.yml` is a Platform-surface file (has_platform=true), so it exercises the NORMAL path; the SKIP path can only be proven on a SEPARATE non-Platform PR after the precursor is on main.

**2026-06-02 — #844/#1019 (real ARC cutover) RE-ASSESSED → stays NO-GO, but CI is ALREADY 8-core (NOT 2-core ubuntu).** #844 is CLOSED (its ci-gate flip shipped via #1027/#1032). The "real ARC cutover" = **#1019** (OPEN, "NO-GO until prereqs"). **CRITICAL CORRECTION — verified live 2026-06-02: heavy CI ALREADY runs on the cross-region ukwest self-hosted runner (#1024 `531427b9` + #1027 `46281fa1`), NOT ubuntu.** `ci.yml` `main` + `_ci.yml` `ci` → `runs-on: [self-hosted, acme-ukwest]` (`development-acme-ukwest-runner`, Standard_D8as_v4, 8 vCPU/32GiB, NX_PARALLEL=6, 8GiB heap); `start-runner`/`ensure-runner` `az vm start` it on demand; `ubuntu-latest` is the FALLBACK only (skipped on every recent run). Verified: VM running, both `acme-ukwest-01/02` agents online, last 6 `ci.yml` runs all used `main`(self-hosted)=success / `fallback-main`=skipped. **LESSON (cost me a wrong "keep ubuntu-latest" recommendation): I read the STALE local branch `fix/platform-vitest-fileparallelism-oom` (pre-#1027, shows `runs-on: ubuntu-latest`) instead of `origin/main`. For ANY CI topology / runner / workflow-structure question, read `git show origin/main:<file>` — NOT the local checkout, which is often a stale feature branch.** **Quota reality (az, uksouth, sponsorship sub 00000000):** Total Regional **10/20** (the user's 10→20 raise); **DSv5 family = 0** (so `D8s_v5` for the AKS-ARC fleet is un-provisionable); `Dasv7` 2/10 (fits 1× `D8as_v7` 8-core if we ever resized the AKS arc-pool); **ukwest Total Regional 8/10** (VM uses 8 → only 2 free, no room for a 2nd `D8as_v4`). User ruled out a paid/PAYG sub. **So #1019 (AKS-ARC autoscaling) is a LOW-URGENCY concurrency optimization, NOT a fix for slow CI** — its only gain over the current ukwest VM is autoscaling for many-simultaneous-PRs, and that concurrency is quota-blocked BOTH ways (AKS DSv5=0; ukwest capped at 10). The single ukwest VM has 2 agents (≈2 concurrent jobs). **Recommendation (posted to #1019): don't pursue the AKS-ARC cutover unless PR queueing on the 2-agent ukwest VM becomes a measured pain; the epic's correctness wins (per-service gating, de-monolith, required `platform-ci-gate`) are all shipped.** Stale docs still instruct filing the DECLINED `DSv5`-to-50 ticket (`infra/modules/arc/main.tf:23-24` — the module has a `null_resource.quota_check` on `quota_family`×`vcpu_per_node`, bypassed by `skip_quota_check=true` at `infra/environments/development/main.tf:680`; + `arc-phase-a-deploy.md` §A1 line 29) — low-priority correction, deferred.

**2026-06-01 — #1062 ARC chart migration: PR #1072 MERGED (squash `9764e117`) + LIVE CUTOVER DONE & VERIFIED; #1062 CLOSED.** Retires the unsupported raw `AutoscalingRunnerSet` CRs + the permanent `arc-controller-rbac-supplement.yaml` fork → 3 aliased `gha-runner-scale-set` v0.10.0 subchart deps in `charts/arc/Chart.yaml`. The chart natively renders each set's `manager_role` (secrets:LIST + roles/rolebindings + cleanup-protection finalizer — the #4510 fix; maintainer ruled raw-CR install unsupported, won't fix). Single `arc` ArgoCD Application unchanged (subchart-deps, NOT per-set Apps — keeps one health view; manager-role names per-set distinct `{name}-gha-rs-manager` so no collision). **KEY GOTCHAS:** (1) `controllerServiceAccount` MUST be set explicitly (`arc-gha-rs-controller`/`arc-system`) — the chart's SA auto-discovery does a cluster `lookup` that's EMPTY under ArgoCD repo-server render → `fail`; `runner-set-guards.yaml` asserts it `== {release}-gha-rs-controller` so drift = loud render fail (the dark-fleet lynchpin). (2) `githubConfigSecret` STRING form → references seeded `arc-controller-secret` (chart mints nothing). (3) runner image moved per-alias (subcharts can't read parent values) → `runner-set-guards.yaml` enforces immutable+equal+no-priv/caps/APE/host-ns/docker.sock across containers+initContainers at RENDER time (stronger than the old CI-only unittest; helm-unittest v0.7.2 CANNOT address aliased-subchart templates — neither `templates:` nor `documentSelector` reach them, so the 3 runner-set suites were deleted + 2 global suites trimmed). (4) helm REPLACES lists → the dev overlay restates the FULL runner container per alias (only image+resources differ). (5) `Chart.lock` + `charts/arc/charts/` are gitignored; use `helm dependency UPDATE` (not build — lock is stale after adding deps). (6) `charts/arc` is NOT helm-gated in CI (only platform-base/bundles) → local helm v3.16.4 + unittest v0.7.2 is the ONLY validation. **Verified:** semantic pod-spec diff old-vs-new == identical modulo chart-added `restartPolicy:Never`; 14 unittest suites/88 green; guard negative-tested all branches; deploy-sim render clean. 3 reviews (5-axis + RBAC-security + silent-failure) all APPROVE, net RBAC unchanged/improved (no new cluster grant; only +serviceaccounts:list vs supplement); hardened the guard for the privileged-initContainer/caps/APE PoC + C2 controllerSA-drift + H3 missing-alias. **CUTOVER WATCH-ITEMS (when merging):** names identical → ArgoCD in-place CR update + create 3 manager roles + prune supplement; brief listener roll (ubuntu fallback covers, dev-only); confirm EphemeralRunner CRs drain to ~minRunners; `charts/argocd/arc.yaml` UNCHANGED, add `ignoreDifferences` only if the manager-role finalizer/ARS-annotations drift live. This is also the 3rd green-pipeline PR toward the `platform-ci-gate` flip. See [[feedback_arc_rbac_supplement_missing_list]].

**CUTOVER OUTCOME (2026-06-01 19:31–19:35Z, dev `development-acme-aks-admin`) — CLEAN, no drift, no `ignoreDifferences` needed.** Merged via `gh pr merge --squash` (NO --admin needed; `ci-gate`+`Validate Terraform` green, UNSTABLE was only the non-required `ARC Runner Image / Trivy` failing on PRE-EXISTING base-image CVEs — containerd CVE-2026-46680 + Go stdlib CVE-2026-39823/25/26 in `apps/runner-image/`, untouched by this PR; Trivy is informational). Triggered ArgoCD hard-refresh (`kubectl annotate application arc argocd.argoproj.io/refresh=hard`); arc app synced to `9764e117` within ~80s → **Synced/Healthy, 0 OutOfSync, 0 Unhealthy.** Verified live: (a) **3 manager Roles created** — `arc-linux-x64{,-large,-builder}-gha-rs-controller-manager` (NOTE the real name is `{scale-set}-gha-rs-controller-manager`, NOT `{name}-gha-rs-manager` as the plan/PR-body guessed — a monitor grepping `gha-rs-manager` gets a FALSE mgr=0; grep `gha-rs-controller-manager`); (b) RoleBinding subject == `ServiceAccount arc-gha-rs-controller/arc-system` ✅; (c) manager Role grants `secrets: create,delete,get,`**`list`**`,patch,update` + serviceaccounts(+list) + pods + roles/rolebindings — the #4510 fix is LIVE; (d) supplement `arc-gha-rs-controller-rbac-supplement` Role+Binding **pruned**; (e) 3 ARS reconciled IN-PLACE (same names, min0/max1, ServerSideApply — not delete+recreate); (f) controller pod `arc-gha-rs-controller-79db84c489-qjvs8` **0 restarts** (cutover didn't disrupt it), 3 listeners rolled (~2m); (g) **#4510 proven end-to-end**: a real CI job ran 2 EphemeralRunners THROUGH the migrated chart, controller logged `Cleaning up runner linked secrets`→`Runner-linked secrets are deleted` (needs secrets:list+delete — the exact op the raw-CR pattern broke), fleet drained back to 0, ZERO `forbidden`/`cannot list secrets`. **FAST-FOLLOW (ONE PR, review-endorsed, all dev-only/non-urgent, NOT a blocker):** (1) fix stale runbook `docs/runbooks/cicd/arc-operations.md:261-267` (still says bump `runnerImage.tag` — removed; now 3 per-alias `image:` literals in `charts/values/arc.yaml`, equal+immutable, guard-enforced); (2) add a pre-merge CI render gate for `charts/arc` (`helm dependency build charts/arc && helm template … -f charts/values/arc.yaml`, hard-fail rc≠0 — charts/arc is in NEITHER helm gate); (3) restore SA-binding/toleration/resources coverage as render guards (per-alias `serviceAccountName` builder=arc-builder-sa/others=arc-runner-sa; workload=ci toleration; non-empty resources) + correct the two misleading "strictly-stronger" comments (`runner-set-guards.yaml:11`, `anti-pattern-no-privileged-pods_test.yaml:62-65`); (4) commit `Chart.lock` (un-ignore only the lock); (5) guard-hardening bundle (ephemeralContainers in the security loop; blanket-deny ALL hostPath not just docker.sock; runAsUser:0/runAsNonRoot:false nil-safe; seccompProfile Unconfined; capabilities.drop ALL; listenerTemplate.spec; floor-check ≥1 set enabled); (6) schema typed minRunners/maxRunners; (7) drop orphaned `github.configUrl`. The expert panel (6 lenses, 56 findings/50 material/37 confirmed, adversarial per-finding verify) verdict was **MERGE-READY, zero must-fix**.

**FAST-FOLLOW SHIPPED 2026-06-02 → PR #1074 MERGED (squash `00f6e12d`).** Bundled ALL the #1062-review should-fix/consider items into ONE PR (branch `chore/arc-chart-followup-1062`, worktree `acme-arc-followup-1062`): (1) `runner-set-guards.yaml` — restored the per-alias serviceAccountName isolation (builder=arc-builder-sa/x64+large=arc-runner-sa), arc-pool toleration, and non-empty resources coverage the deleted raw-CR suites had, PLUS hardening (ephemeralContainers, blanket hostPath deny, runAsUser:0 + **container-level** runAsNonRoot:false [the review-caught gap — pod-level alone is insufficient since container SC overrides], seccomp Unconfined, required capabilities.drop:[ALL], listenerTemplate.spec coverage, ≥1-enabled floor) via a reusable `arc.runnerSetPodSecurityGuard` define applied to template.spec + listenerTemplate.spec; (2) `_ci.yml` — pre-merge `helm dependency build charts/arc && helm template … -f charts/values/arc.yaml --set <schema-valid dummies>` step in `helm-template-strict` (charts/arc is NOT in the ApplicationSet, so the guard otherwise fired only at ArgoCD sync) — **passed live as `ci / helm-template-strict`**; (3) committed `charts/arc/Chart.lock` (un-ignored ONLY the lock, tarballs still ignored — matches charts/bundles/* convention); (4) dropped orphaned `github.configUrl` + added typed minRunners/maxRunners to the schema; (5) fixed stale `runnerImage.tag` instructions in `arc-operations.md` + `arc-phase-a-deploy.md` + the misleading "strictly stronger" guard comments. **Key safety property: rendered manifests are BYTE-IDENTICAL to pre-PR main (helm template diff empty) → the merge was a NO-OP ArgoCD sync, zero fleet disruption** (guard emits no resources; configUrl was unconsumed). Verified: 14 unittest suites/88 green; 13 guard negative-tests (12 + the added container runAsNonRoot) each fail with the right message; actionlint clean; CI required checks (ci-gate + Validate Terraform) green, mergeState CLEAN. Focused 4-lens review (guard-correctness/security/ci-workflow/adversarial) + per-finding adversarial verify → MERGE-READY, the one should-fix (container runAsNonRoot:false) fixed in-PR before merge. **GOTCHAS for future charts/arc work in a fresh worktree:** (a) fresh worktrees off origin/main have NO node_modules → nx/husky can't run → commit/push with `SKIP_HOOKS=1` (sanctioned) after manually running helm-unittest/actionlint/prettier; (b) `nx format:write` is unavailable there — run prettier from the MAIN repo's node_modules against the worktree files by absolute path (plugins resolve), BUT prettier reflows pre-existing markdown tables in docs/*.md → restore the .md to origin/main and re-apply only scoped edits (docs/*.md aren't a required CI format gate); (c) `charts/**/templates/**` is in `.prettierignore` (the guard template is safe); (d) the source-driven-dev hook trips on `.gitignore` edits (extensionless → content-scanned → matches "helm"+"terraform" substrings) — needs `helm.fetched`+`terraform.fetched` breadcrumbs (false-positive for a config edit); (e) the manager-role name is `{scale-set}-gha-rs-controller-manager` NOT `{name}-gha-rs-manager`.

---

**2026-06-03 — epic #835 housekeeping closeout.** (1) **#1017 (Phase 2 de-monolith) CLOSED** — all ACs met across #1046 (13 `test:unit` targets + ci.yml gate) / #1050 (npm-ci dedup) / #1057 (per-service build-push + `platform-ci-gate`) / #1078 (branch-protection flip — `platform-ci-gate` now REQUIRED) + the full-Platform determinism-guard rollout (#1087/#1088 + follow-ups #1093/#1094/#1095, done this session). One checklist item intentionally NOT done: "remove #1001's `NX_PARALLEL=1`" — it's retained on the **ubuntu fallback** path (genuinely 2-core, still needs the OOM cap); self-hosted ukwest runs NX_PARALLEL=6. (2) **#1006 (dev CI infra op ticket) CLOSED as obsolete** — Fault A (legacy VM auto-start) MOOT (VM decommissioned #949, ResourceNotFound); Fault B (ARC /cache PVC) was a misdiagnosis RESOLVED by #1010 (subPath children `/cache/npm`+`/cache/nx`, parent never a mountpoint). No dev-sub hand-off remains. (3) **#1076 runner-image CVE-refresh deploy → PR #1104** (`chore/arc-runner-image-bump-1076`): bumps all 3 runner-set `image:` literals in `charts/values/arc.yaml` `20260521-4f5ffa3`→`20260602-0493802` (the #1075 `04938029` Trivy-refreshed image, built+pushed run 26807285974). Validated: prettier + `helm dependency build` + `helm template` (runner-set-guards + schema pass, 3× new tag) + `helm unittest` 14/88; CI `ci / helm-template-strict` (v3.16.4) GREEN on the PR. **MERGED (squash `6baf966c`, #1076 CLOSED).** 3-lens expert review — adversarial (diff-only, info-isolated) + kubernetes/helm + security — all MERGE/APPROVE, **0 blockers**; the adversarial reviewer's 2 "BLOCKERs" (image-may-not-exist; guard-unverifiable) were both REFUTED by the repo-aware lenses (tag provenance traced commit→run→tag; guard regex `^…:[0-9]{8}-[a-f0-9]{7,}$` + tri-equality `fail` confirmed enforced + passed on the PR render gate). Auto-syncs via standalone `charts/argocd/arc.yaml` → rolls dev ARC fleet (minRunners=0 ephemeral, graceful, no in-flight disruption). **2 surviving non-blocking follow-ups filed (both pre-existing, NOT introduced by the bump): #1107** (cache-warmer `preWarm.images` omits the runner-image → recycled/evicted nodes cold-pull; README says it should be pre-warmed but never was) **+ #1108** (runner sets pinned by MUTABLE tag not `@sha256` digest; guard enforces naming-convention only — mitigated by private ACR + AcrPull-only `arc-runner-sa` SA isolation + IfNotPresent, consistent with all chart images). **epic.md frontmatter REFRESHED 2026-06-03** → `progress: 90%`, `updated: 2026-06-03`, phase_status rewritten (A–D COMPLETE, C-shadow disabled/#1019-gated, E partial, + fast-CI Phases 0–4 incl. #1017 CLOSED). Shipped via PR #1109 (squash `69ba037a`) + a narrative progress comment on GitHub #835. Was stale at 60%/2026-05-24 (committed `6b14453a`, Phase A era); the transient "90%/2026-06-02" seen earlier was an UNCOMMITTED primary-worktree edit that got reverted — never canonical.
  Durable `.claude`/epic.md edit mechanics this verified are in [[feedback_pm_claude_file_edit_mechanics]] (real-vs-symlink worktrees, fresh-worktree `SKIP_HOOKS=1` path, the docs-only `**/*.md` admin-merge deadlock, the frontmatter `|`-block YAML rule, ff-pull pickup). Primary worktree confirmed to pick up epic.md via `git pull --ff-only` (clean ff 8130fbab→69ba037a). **Epic #835 KEPT OPEN (user decision 2026-06-03)** — open children: #843 (Phase C ci-v2 shadow — built/#1002 but disabled, measurement gated on #1019), #845 (Phase E prod-verify deferrals), #1018 (interim capacity, conditional), #1019 (ARC autoscaling — NO-GO on declined DSv5 quota). Original phase tasks 836-842 + 844 all CLOSED; core ARC-parallelisation goal is shipped+live (#835 stays the home for the parked/conditional capacity items until a quota/sub change).

**2026-06-03 — #1107 (pre-warm) + #1108 (digest-pin) BOTH CLOSED → PR #1110 (squash `85e15869`).** Combined runner-image hardening, worktree `acme-arc-hardening`. **Digest-pin** (#1108): runner-set image regex tightened `…[a-f0-9]{7,}$` → `…[a-f0-9]{7,}@sha256:[a-f0-9]{64}$`; all 3 overlay runner-set refs + the new `preWarm.images[0]` pinned to `20260602-0493802@sha256:6120040b…`. **Pre-warm** (#1107): cache-warmer's first `preWarm.images` entry IS the runner image (kubelet pre-pulls it onto recycled nodes); enforced by NEW `runner-set-guards.yaml` **guard (7)** — exactly one preWarm entry whose repo-prefix (DERIVED from the runner-set ref via `regexReplaceAll`, registry-agnostic — NOT hardcoded) equals the runner-set image; catches omission + drift + duplicates at render time. `charts/arc/values.yaml` base carries a shape-valid **placeholder** `19700101-deadbee@sha256:0000…0000` (never deployed standalone; overlay always overrides) so full-chart helm-unittest renders clean — kept as a documented MEDIUM residual (an un-overridden base deploys an un-pullable digest → ImagePullBackOff, loud at runtime). `runner-image-build.yml` emits the pushed digest (repo-filtered `RepoDigests`, NOT blind `[0]` — a shared-daemon decoy entry would corrupt a blind read) into the step summary + the auto-filed bump issue. New `tests/runner-set-guards_test.yaml` (5 cases: base clean, happy, drift, omission, >1). Chart 0.1.0→0.1.1; runbook rebuild section updated. **VERIFIED LIVE:** ArgoCD `arc` Synced+Healthy at `85e15869`, all 3 runner sets + cache-warmer `prewarm-0` carry the digest ref. Validated on CI toolchain (helm v3.16.4 + unittest v0.7.2): 15 suites/93 tests; overlay render exit 0 digest ×4; tag-only/omission/drift/>1 negatives each fail with distinct message; derived-prefix proven registry-agnostic (swap all 4 refs to another registry → renders clean). Built via ultracode workflow + a fresh adversarial pass (18 findings: 3 legit → fixed [repo-filtered digest, derived prefix, +`>1` test case]; 15 were info-isolation false positives, each verified against the code). **#835 now has 4 open children (was 6); the 2 hardening follow-ups are done.** Mechanics in [[feedback_pm_claude_file_edit_mechanics]].

**LESSON — `gh pr checks <pr> --required` only lists contexts that have ALREADY REPORTED a status.** Aggregator gates that run AFTER a long job (here `ci-gate`/`platform-ci-gate` run after `ci / ci`) are simply ABSENT from `--required` until they post → a monitor that declares "all green" off `--required` alone fires PREMATURELY (saw `Validate Terraform=pass` only, called it done, while the real state was `mergeStateStatus=BLOCKED`). **Gate merge-readiness on `mergeStateStatus` (BLOCKED→UNSTABLE/CLEAN), not on `--required` emptiness** — or poll the named required contexts explicitly from `statusCheckRollup`. `UNSTABLE` = all required green but a NON-required check (here `production-safety` pending) red/pending → still mergeable (a dev-only chart change needn't wait on it).

---

**2026-06-06 — EPIC #835 CLOSED (completed; user decision, reversing the 2026-06-03 keep-open).** Parallelisation + CI-reliability goals shipped & live (8-core self-hosted ukwest CI, per-service build-push gating, required `platform-ci-gate`, de-monolith, determinism Phase 1). Child disposition at close:
- **#843** (Phase C ci-v2 shadow) — **CLOSED completed**: workflow delivered via #1002; ≥2-week measurement is capacity-gated → folded into #1019.
- **#1018** (Phase 3 interim heavy-coverage capacity) — **CLOSED not-planned (superseded)**: 8-core self-hosted cutover solved capacity + `_ci.yml` dropped `--coverage` (#1014); instantly reversible.
- **#1019** (Phase 4 real ARC-on-AKS cutover) — **KEPT OPEN, parked tracker**. Live quota 2026-06-06 (uksouth sponsorship sub): Total Regional **10/20**, **`Standard DSv5 = 0/0`** (ARC node-pool SKU un-provisionable), ukwest **8/10**. CI already 8-core → cutover only adds autoscaling, quota-capped both ways. Revisit ONLY on measured PR-queue pain. Comment posted.
- **#845** (Phase E decommission + prod-verification) — **CLOSED completed 2026-06-06** (issuecomment-4639450600, AC-by-AC). Decommission DONE: `runner-auto-start.yml` gone, legacy uksouth VM decommissioned #949, #822/#823 already closed. AC:6 break-glass README **N/A — would be factually WRONG**: the `github-runner` module provisions the **LIVE** ukwest runner (`module "github_runner_ukwest"`, development/main.tf:676), not a dormant fallback (ARC never became primary). Thread B (ACs 8–19) **N/A by construction** — ARC-end-state KPIs (pod success rate, KEDA queue depth, buildkitd, run cost, 16 ARC Gherkins); ARC never cut over (#1019 parked). Parallelisation goal met via ukwest VM instead.
- **NEW #1161** — full-Platform determinism-guard rollout (3→34 projects; deferred from #1036). Blocked on a CI-input fix: Option A (exclude `.eslintrc.json` from `test` named-input) or Option C (lower NX_PARALLEL/maxWorkers). Low priority.
- ci-v2 shadow still `disabled_manually` (re-enable gated on #1019 capacity).
- **epic.md frontmatter RECONCILED** to `completed`/`100%` + **ci-pipeline-architecture.md ukwest banner** → both in **PR #1162** (`docs/ci-arch-ukwest-runner-banner`, `**/*.md`-only → path-skips required gates → admin-merge). NOTE: per-task-file `status:` frontmatter under `.claude/epics/ci-arc-parallelisation/` is STILL stale (836=in_progress, 841–845=open) despite the GH issues being closed — known CCPM drift, GH is source of truth; epic-refresh's `closed/total` formula would mis-compute ~36% off those, so frontmatter was set from GH truth, not the formula.

---

**2026-07-01 — arc-pool quota RE-VERIFIED: the "declined-quota / capacity-blocked at 1 runner" premise was a MISREAD; PR #1564 resizes to 1× D8as_v7 (min1/max3).** Live `az vm list-usage` + `az vm list-skus` (uksouth, sponsorship sub 00000000): **Standard Dasv7 Family 2/10** (arc-pool is the ONLY Dasv7 consumer), **Total Regional 14/30**, **Standard_D8as_v7 offered** (restrictions=[]). The AKS **system (Standard_E2as_v6) + user (Standard_E2ads_v6) pools are Easv6/Eadsv6 — a DIFFERENT family** — so they NEVER consumed Dasv7/DASv5; the stale `main.tf` comment ("4 system + 4 user + 2 arc = 10 Dasv7") was WRONG. arc-pool therefore has 8 vCPU of Dasv7 headroom. **#1564** (`feat/platform-844-arc-min1-max3`, `69ad5f8e`, OPEN): `node_sku D2as_v7→D8as_v7` (8-core, offered in uksouth), `runner_max_count=1` (a 2nd = 16 > 10 cap), `charts/values/arc.yaml` arc-linux-x64 `minRunners1/maxRunners3` + resources raised `req1.5/limit3` vCPU so 3 CI-grade runners + buildkitd bin-pack the one 8-core node (ukwest-VM class, kills the "2-vCPU too slow" risk). **GATES:** (1) dev TF is operator-only → needs operator apply; the `node_sku` change is a **node-pool REPLACEMENT** (AKS `vm_size` immutable → destroy+recreate `arcpool`, brief drain — ci-v2 is non-required + the required `ci.yml`/`main` runs on the ukwest VM + ubuntu fallback, neither touched); (2) promotion of `ci-gate` to REQUIRED still gated on ci-v2 shadow parity (#844). Follow-ups: overnight scale-to-0 (cost), `-large`/`-builder` sizing, ADR-0040 Amendment + `arc-phase-a-deploy.md` doc update. **CONTEXT:** the 5-deep PR queue on the 2-agent ukwest VM today = the "measured pain" that line-72's own criterion said would justify pursuing ARC autoscaling — so this is now warranted, consistent with that note (not a reversal). SUPERSEDES the line-72/line-30 "quota rejected → 1× D2as_v7 only" framing for the arc-pool SKU.
