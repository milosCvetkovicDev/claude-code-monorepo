---
name: platform-frontend-ui-adoption-epic
description: "ACTIVE epic #1383 — migrate platform-frontend's authenticated half off MUI onto @acme/ui; ALL 11 tasks code-complete, epic-review done, merged-with-main + fixes applied, PR #1431 (DRAFT) — gated on #1394 human prod-verify + acceptance sign-off."
metadata:
  node_type: memory
  type: project
  originSessionId: 00000000-0000-0000-0000-000000000030
---

**ACTIVE epic `platform-frontend-ui-adoption` = GitHub epic #1383** (synced 2026-06-22). Migrate the **authenticated half** of `apps/platform-frontend` off **MUI v5 + Emotion** onto **@acme/ui** (Tailwind v4). NB the **auth flow (13 pages) is ALREADY on the kit** (#1353) — do not re-scope it; the real work is the authenticated app (~29 files: `AppLayout` shell, dashboard tree + 6 widgets, NotificationBell, SearchOverlay, 7 table/form pages, `app.tsx` MUI root).

**Full PM ceremony done, all artifacts LOCAL under `.claude/epics/platform-frontend-ui-adoption/` + `.claude/prds/`:** prd-new → prd-parse → arch-create → **5-expert independent review (enterprise-arch/tech-lead/frontend/ux/adversarial, all APPROVE-WITH-CHANGES, folded in — see architecture.md §Review Hardening RH-1..RH-9)** → epic-decompose (11 tasks) → tests-generate (RED suite in epic `tests/` dir, NOT in live src — materialise at epic-start per `test-manifest.md`) → readiness-check (**READY**, 100% FR, 0 crit / 2 major) → epic-sync. **ADR-0056** (nested-provider transition) + **ADR-0057** (compose shell+SearchOverlay in-app) in `docs/adr/` — **tracked but UNCOMMITTED** (land via the epic branch / a docs PR).

**11 task sub-issues #1384–#1394** (local files renamed to issue#; deps renumbered):
`1384` baselines+spec-audit (test gate) · `1385` kit DataTable (footer pagination `{totalCount,hasMore,loading,onLoadMore}` + a11y) · `1386` kit icons+Toggle+Modal-a11y · `1387` root nested providers+z-index · `1388` AppLayout shell · `1389` small swaps · `1390` dashboard tree · `1391` notifications+search · `1392` table pages · `1393` form/report pages · `1394` MUI removal+prod-verify (last, depends on all). Wave order: 1384 → 1385∥1386∥1387 → 1388∥1389∥1390∥1391 → 1392∥1393 → 1394.

**Key resolved decisions (the review made these — honour them):** DataTable is **client-only** → page owns cursor+data, kit gets footer props (`columnDefinitions`-from-server is NOT a gap). `SearchOverlay→CommandPalette` was **WRONG** (static launcher vs async grouped search) → **compose in-app**. **Toggle/Switch absent** → build it (role=switch, aria-disabled mandatory). Real **kit a11y bugs** to fix in 1385/1386: `Modal` role=dialog on backdrop not panel; `Popover` hard-codes aria-haspopup=menu; DataTable sort-label/row-action-focus/contrast. **Reset-flip = task 1394 ONLY** (not at root); z-index: kit overlays render behind MUI AppBar z-1201 during coexistence → app override. **19 specs (not 21); 7 pages have ZERO specs** → 1392/1393 WRITE new specs. `@mui/x-date-pickers` UNUSED in platform-frontend.

**2 MAJOR risks before kit tasks merge:** (1) **#1370** (platform-ui:test CI flake) gates 1385/1386 — quarantine/fix or rely on local-green. (2) FR6 single-task (1394, by design). Estimates deliberately deferred (re-derive after 1384 + DataTable audit).

**STATUS: IMPLEMENTATION IN PROGRESS (epic-start, waves 1–3 DONE 2026-06-23).** Dedicated worktree `~/projects/acme-platform-frontend-ui-adoption` on branch `epic/platform-frontend-ui-adoption` (off origin/main 6ae0a1a9; `npm ci`'d; ADRs 0056/0057 committed 3e892cbf). Execution = **wave-by-wave, implement→adversarial-verify→central-gate→commit** (NOT the generic shared-branch model — per [[worktree-best-practices]]/[[feedback_subagent_git_add_race]]); wave 3 ran 4 implementers truly-parallel (disjoint files, kit frozen, overlays mocked in shell spec) → barrier → 4 verifiers (Workflow `wf_8b639213-b54`). **DONE+closed (8/11): #1384** baselines · **#1386** Icon+Toggle+Modal a11y (bf257788) · **#1385** DataTable footer+a11y (b0bc351e) · **#1387** root ThemeProvider+z-index (ba679859) · **#1388** AppLayout shell rebuild (e7f4750d) · **#1389** small swaps Forbidden/SessionExpiryModal/AuthLoading (3c23c510) · **#1390** dashboard tree→Card/Skeleton/KPITile/Modal (771ec1a4) · **#1391** NotificationBell→Popover + SearchOverlay composed in-app (d3d9d91a). All adversarial-verified PASS; wave-3 central gate (cache-skipped): platform-frontend lint 0 errors · typecheck ✓ · test 25 files/144 ✓ · build ✓ (bundle 963.76 kB gzip 264.78 — already below #1384 baseline). Parent fixed one defect the verifier surfaced: a stray `jsx-a11y/no-autofocus` disable directive in SearchOverlay (plugin not in this flat config = the sole lint error). **Wave 4 DONE (Workflow `wf_e4c46856-1f7`): #1392** table pages (fba23ced — Audit/UserAdmin/ScheduledReports → kit DataTable; window.confirm→TypeToConfirm/Modal danger; page-local useAuditLog adapter over real auditApi) · **#1393** form/report pages (57744d8d — Dashboard→PageHeader, Reports→render-time Column[] from server columnDefinitions + accumulate Load-More, TenantConfig native color/file in kit Field, NotificationPrefs kit Toggle mandatory aria-disabled; spec mock adapted to real notificationsApi.preferences() per-row In-App+Email). Both adversarial-verified PASS (21/21, 23/23). **Wave-4 central gate: lint 0 errors · typecheck ✓ · test 32 files/188 ✓ · build ✓.** **10/11 tasks closed.** **#1394 CODE-COMPLETE 2026-06-23 (bde9908d, pushed; adversarial-verified PASS):** removed MUI provider/createTheme/CssBaseline/#1976d2 + coexistence z-override from app.tsx (kit ThemeProvider now sole root); flipped the CSS reset in styles/tailwind.css (`@layer theme,base,components,utilities;` + `@import 'tailwindcss/preflight.css' layer(base)` — verified base precedes utilities); materialised mui-removal.spec (full real app render). **Three-way MUI-gone proof ✅** (source-grep empty · no MUI/emotion in built JS · no MUI chunk). **Bundle JS 700,650 B vs T0 993,899 B (~30% smaller).** Gate: lint 0 err·typecheck·test 33 files/194·build·**legacy-web:build green**. **The "Scenario-5 correction" was a NO-OP** — the PRD already scopes it right (AD-6/line 168: @mui stays in ROOT package.json for legacy-web; "zero MUI" = source+nx/bundle graph, NOT root-deps removal); just honored it. **EPIC = ALL 11 TASKS CODE-COMPLETE; #1394 is the ONLY open GH issue, gated ONLY on the human prod-verify (AC-8) + the acceptance sign-off on the Reports/Audit screens (AC-9)** — branch is MUI-free + code-merge-ready, merge/deploy gated on that. Checklist: `.claude/epics/platform-frontend-ui-adoption/prod-verification-1394.md`. NOT closeable autonomously. Local `main` (kit worktree) is 16 behind origin (ff-blocked by [[feedback_claude_symlink_blocks_git_reset]]) but its platform-frontend tree == origin. Distinct from [[platform-finance-ui-epic]]. Read before any platform-frontend-ui-adoption work.

**#1384 scope correction (apply to #1394 + PRD Scenario 5 before that wave):** MUI/Emotion are **shared root `package.json`** deps; legacy `legacy-web` uses `@mui/material`, so "no @mui in package.json deps" is impossible. Achievable success criterion = **zero @mui/@emotion in platform-frontend's import + nx/bundle graph**, NOT removed from root deps. Also: the source-driven-dev `react.fetched` breadcrumb now exists at `~/.claude/.source-driven-dev/` (an agent created it via context7 React-docs fetch during #1386). **The source-driven-dev hook resolves a CWD-relative `.claude/.source-driven-dev/<fw>.fetched`, NOT just `$HOME`** — needed it at the worktree's `.claude` (both worktree symlinks share the primary `.claude`).

**EPIC-REVIEW + MERGE-PREP DONE 2026-06-25 (PR #1431 DRAFT, gated):** holistic 5-lens adversarial epic-review (`wf_95c1418f-b70`) on the full 52-file diff (epic forked at 6ae0a1a9, was 19 commits behind a #1423-changed main). 13 CRITICAL/17 MAJOR, mostly the kit divergence. Resolution: **merged origin/main INTO the epic branch** — git auto-**unioned** the 3 diverged kit files (index.ts, DataTable.tsx, DataTable.spec) cleanly: main's density-auto-wire + enum-search + new primitives (Switch/Kbd/Segmented/Metric/MetricRail/NavItem/StatusStepper/STATUS_ICONS) AND the epic's footer-pagination + focus-a11y all coexist (main did NOT have pagination; epic's was older & lacked density/enum-search → union, not take-one-side). **Toggle CONSOLIDATED into Switch (user decision, supersedes the old "build Toggle" note):** extended kit `Switch` with `ariaDisabled` (focusable-but-locked mandatory case) + tests; migrated NotificationPrefs (aria-label+ariaDisabled) & TenantConfig (visible label, dropped dup span) off Toggle; **deleted Toggle.tsx + export + spec**. Other fixes: NotificationBell open-state-desync (local mirror of Popover state broke the lazy query on re-open) + count-in-aria-label; Icon.stories 7 glyphs; DataTable non-sortable-header title. Committed **f32644e4**, pushed. **3 review findings REFUSED (correct):** 3 KPI widgets already get chrome from DashboardGrid+KPITile (wrap=double-card); AuthUser.name is real; empty `<aside>` is a ratified AC (PRD Scenario 8 / task 005 AC-3) → human SoT call. Gate: platform-ui+platform-frontend typecheck/lint/test(194)/build ✓. **PR #1431 (DRAFT, base main) = the epic-merge step; DO NOT MERGE until the #1394 human gate (preview-deploy smoke + 48h + acceptance sign-off) clears, then `gh pr ready 1431` + merge.** **Switch API gained `ariaDisabled` → a @acme/ui design-sync re-sync is a follow-up.** Remaining ceremony: prod-verify (human) → epic-merge (un-draft + merge) → epic-close.
