---
name: platform-trading-hardening-epic
description: ACTIVE backend epic
metadata:
  node_type: memory
  type: project
  originSessionId: 00000000-0000-0000-0000-000000000017
---

**#1633 SOAK RUNNING (started 2026-07-18T06:08Z → ends 2026-07-20T06:08Z UTC).** Baseline posted on issue #1633 (comment 5010180675): AC1 topology VERIFIED (queue bound `accounting.invoice.processed.#`, 1 consumer, DLQ 0), lag 0, parked 0, restarts frozen at 10 (all pre-#1702), 0 ERROR lines. Pass criteria = lag≈0/parked-stable/DLQ-0/no-error-regression at window close. STILL TO DO inside window: AC2–6 seeded functional smokes (harness = open PR #1684; needs dev seed creds+TOTP [[platform-dev-superadmin-seed]]), AC7 synthetic parked-alert fire + unbind rollback rehearsal, AC9 sign-offs. **POSTURE CONFIRMED per Milos 2026-07-18: Platform is DEV-ONLY — "no plans for prod deploy for now."** The release sign-off that would gate any future prod step is therefore moot — nothing to gate today, consistent with [[no-platform-to-prod]]. #1633 AC9 open indefinitely; the 48h soak (ends 2026-07-20T06:08Z) continues as a DEV-health check, not a prod gate; AC2–6/AC7 smokes optional dev verification, unscheduled. Grafana money-pipeline alerts live for the window.
**SHIPPED TO DEV 2026-07-17 — ROLLOUT VERIFIED.** Deploy chain: #1692 (charts/values — only gateway+ai consume those!) + **#1698 (bundle values = the REAL deploy source: `charts/bundles/<bc>-bundle/values.yaml`; conflicts ×2 from racing deploy PRs resolved by keeping main's newer epic-containing tags)** + **#1702 fast-follow** (INV-P11 lock-and-recount closure; ADR renumber COMPLETED — 142 refs, first sweep was a zsh word-split no-op with a false-zero verification; metadata-only `connect:false` spec proving private-prop nativeUpdate mapping renders `status/deleted_at IS NULL/tenant_id/purchase_id`; **RMQ write fix**). **LIVE-FIRE LESSON: queue-to-exchange binding needs WRITE on the QUEUE + read on the exchange — trading_user lacked `trading-service\..*` in write → DLQ bind 403 → CrashLoopBackOff on dev (10 restarts) until #1702 synced; inventory_user always had own-queue write scope — mirror BOTH axes.** Verified evidence: migrations trading 005–009 (incl. `Migration_009_soft_delete_columns` on the real migrated DB) + inventory 003 applied by init containers; `InvoiceProcessedConsumer started`, 0 error lines; all 13 backend pods Running on `sha-f339d5e` (tenant `sha-b08953f`, audit `sha-f59789a` — newer, epic-containing); gateway `/health` 200, frontend 200, an unauthenticated request to a trading read route → clean 401 envelope. #1633 smoke/48h-soak + release sign-off remain before any prod exposure.
**MERGED TO MAIN 2026-07-17** — epic **#1623** (`platform-trading-hardening`): **PR #1651 squash-merged `f339d5e8`** (user ran the gated merge command themselves after the session classifier blocked `gh pr merge`; required 3/3 green; UNSTABLE-state semantics confirmed again; non-required reds = inherited Dependency Audit + legacy integration-tests env failures; gateway `<5ms` perf-assert flake in the full-auth-flow integration spec cleared on rerun — flaky wall-clock assert worth fixing on main). **Dev deploy dispatched**: `gh workflow run platform-pipeline.yml --ref main --field deploy=true --field environment=development --field sha=f339d5e8` (run 29580167559). ⚠️ release sign-off POSTPONED for the merge only → gate now at deploy/prod boundary; #1633 smoke+48h-soak still pending on dev. Remaining loose ends: PR #1634 CLOSED 2026-07-17 (superseded by #1651; content-verified zero branch-only lines before closing), gateway perf-assert flake, legacy integration-tests red on main.
Original epic description: enterprise-readiness hardening of the Trading BC backend (trading-service + inventory-service). Backend-only; FE epic [[platform-trading-frontend-epic]] (#1439/#1444) consumes the results.

**Synced 2026-07-02** via full PM ceremony (prd-new → prd-parse → arch-create → epic-decompose → tests-generate → readiness-check[READY] → epic-sync). 10 tasks, 156h, 9 parallel / 1 sequential.

**Task issues (file 00N → GH#):** 001→#1624 (RED suites), 002→#1625 (invoice write-back consumer+inbox+parked, trading), 003→#1626 (inventory consumer idempotency), 004→#1627 (stock-check serialization), 005→#1628 (optimistic locking + Idempotency-Key), 006→#1629 (soft delete + audit), 007→#1630 (list filters/derived status/batch mutation/CSV), 008→#1631 (lifecycle completeness), 009→#1632 (observability + Partner audit), 010→#1633 (prod-verify + 48h soak). Dep graph: **#1624 blocks all**; #1632 dep [#1625,#1626]; #1633 dep [#1625..#1632]. Conflicts: #1628↔#1627/#1629/#1631, #1629↔#1631, #1630↔#1631.

**5 workstreams:** (A) event-integration — consume `accounting.invoice.processed`, inbox/dedup both sides, parked messages; (B) concurrency — serialized stock checks, optimistic locking, idempotency keys; (C) governance — soft delete + audit; (D) contract fidelity — bracket filters incl. a derived list status, capped batch mutation + CSV export, missing lifecycle commands, contact endpoints; (E) observability — money-pipeline Grafana metrics/alerts + Partner wiring audit.

**3 ADRs (all Accepted, on epic branch):** ADR-0066 stock-check serialization = `PESSIMISTIC_WRITE` on PLI rows in deterministic order (amends ADR-0022; ADR-0012 saga stays M3+); **ADR-0067 optimistic locking = MikroORM native version col, `version` required in mutation DTOs, mismatch→409 `STALE_WRITE` (PLATFORM-WIDE)**; **ADR-0068 inbox `processed_event`+`idempotency_key`+`parked_message` = per-service PG tables, inbox insert txn-al with state change, DLX=transport backstop, Redis stays cache-only (PLATFORM-WIDE)**. master-architecture updated with 0067/0068 conventions. ADR-0065 skipped (reserved by in-flight [[platform-commission-epic]]). Reuses [[platform-outbox-atomicity-pattern]] (caller-EM), bracket parser `parse-list-filters` (ADR-0061), [[mikroorm-v6-filter-disable-syntax]] global-filter mechanism.

**Worktree:** `$PROJECT_ROOT-platform-trading-hardening` on branch `epic/platform-trading-hardening` (off origin/main 24e66b52; instance #2, alias dev-vth). Artifacts: `.claude/epics/platform-trading-hardening/{epic,architecture,test-manifest,readiness-report,github-mapping,execution-status}.md`, PRD `.claude/prds/platform-trading-hardening.md`.

**Progress (2026-07-03) — LOCAL commits only, NOTHING pushed, NO PRs (held for user):**

- **#1624 RED baseline** committed `12e56450` (3 ADRs) + `02410251` (27 RED test files). Then **harness-remediated `cf49ef83`** — the RED suite had 3 systematic defects found only by RUNNING it (typecheck-only missed them): `$1→?` (conn.execute needs `?`), cucumber `\/` escaping (`/api` steps aborted whole BDD suite at load — also pre-existing in merged #1471, so trading `test:bdd` was ungated in CI), seed `.confirm()`. See [[platform-tc-bdd-red-baseline-gotchas]].
- **#1625 pilot VERIFIED GREEN** (single impl agent, RED→GREEN): commits `6c82628a`+`0d1038e8`. Independently re-verified: `invoice-writeback.tc` 3/3, pact 9/9, bdd loads. No regression; remaining reds = other-task scaffolds + pre-existing #1358 tenant-isolation (empirically confirmed pre-existing at baseline).
- **Wave 1 COMPLETE — 4 tasks merged to epic branch (2026-07-03), integration-green.** Impl via background workflow (isolated worktrees) → then an INDEPENDENT verifier per task (maker≠checker) re-ran tests + audited diffs before each merge:
  - **#1627** stock serialization (PESSIMISTIC_WRITE, STOCK_EXCEEDED/409) — merge `3dbd4ebb`. Verifier mutation-proof (removing the lock breaks the oversell tc). Clean, no fix needed.
  - **#1629** soft-delete + audit — merge `dacb539c`. Real global `softDelete` filter, composes with tenant filter. Clean.
  - **#1630** list filters/derived status/capped batch mutation/CSV — fix `f80d73f4` (verifier caught 5 NEW self-typecheck errors: dead const + untyped `conn.getConnection().execute` raw rows — generic doesn't flow on Connection.execute, annotate callbacks) + merge `ac44174b`.
  - **#1626** inventory inbox idempotency — fix `6e055924` (verifier caught the [[platform-fail-closed-tenant-filter-forked-em]] ISSUE A bug, mergeRecommend=no → inbox `findOne` now `{filters:false}`) + merge `4709f597`. Same fix backported to trading #1625 inbox on epic (`a2bde5dc`).
  - Merge conflicts were only `hardening.steps.ts` import-unions (resolved). Final epic: trading + inventory both build+typecheck; sole remaining typecheck error = #1628 idempotency-store RED scaffold (Wave 2, expected). STILL local-only — nothing pushed, no PRs.
  - Cross-worktree STASH-BLEED during impl (stash stack is shared across worktrees): #1626's stash applied to vth-1629; recovered, no work lost; vth-1629 holds a harmless uncommitted older duplicate → prune at epic-merge. **Lesson: never `git stash` from a subagent working in a worktree.**
- **Wave 2 COMPLETE — all 3 merged to epic (2026-07-03, SOLO by main; user said "no raising [spend limit], continue" so NO agents).** Epic HEAD `ac633763` (typecheck+build+535 unit tests green). Merge order: **#1631 `939bd1e3` → #1628 `20ce9391` → #1632 `ac633763`.** Conflicts were all clean additive-unions (`hardening.steps.ts` × each, `app.module.ts` module-list, sale/entity files auto-merged). Two remediations applied during merge, see below. Still LOCAL — nothing pushed, no epic PR (next: PR epic→main, user decision).
  - **#1628 completion (SOLO):** added the missing prod piece — `OptimisticLockError → 409 STALE_WRITE` mapping in the SHARED `libs/platform/service-bootstrap/app-exception.filter.ts` (commit `9dcc0a45`, platform-wide per ADR-0067; `instanceof OptimisticLockError`, filter spec extended + green). Native optimistic-lock path is now production-real (concurrent write → 409, not 500/lost-update). Migrations renumbered `006/007_version/idempotency` → **`007_version_columns` + `008_idempotency_key`** (006 taken by #1631 haulage on epic). **STILL DEFERRED (FE-coupled follow-up):** the DTO-declared-version UX (AC1 "version REQUIRED on mutation DTOs" + AC2 primary "client declares v3 → 409 w/ currentVersion") — making version required breaks the live [[platform-trading-frontend-epic]] FE (doesn't send version yet). Native-path + 409 mapping ships the core lost-update safety NOW; the declared-version layer needs FE coordination. `assertExpectedVersion` remains BDD-exercised, not yet DTO-wired.
  - **Wave-2 merge remediation — shared `Then('the response is 409 {string}')` step COLLISION:** #1631 (t008Error, credit-note PATCH) and #1628 (t005IdempReuseError, idempotency reuse) each defined it. Unified to `const captured = t008Error ?? t005IdempReuseError` — safe because both vars reset per-scenario by untagged Before hooks (empirically proven: both scenarios pass in ONE cucumber run). The stale HEAD comment ("task 005 aborts at pending") no longer held once #1628 implemented it.
  - **[Pre-`ac633763` HELD-state notes retained below for history.]** All 3 committed to task branches off epic; verified by MAIN (spend limit blocked adversarial-verify agents):
  - **#1628** `20b04ae8` (optimistic lock + idempotency) — **INCOMPLETE despite all-green tests.** Idempotency = fully wired (global APP_INTERCEPTOR + store + entity + purge, idempotency-key.tc green). Optimistic-lock = version cols on 7 entities (native flush check works) BUT the spec'd DTO-version path is NOT wired to prod: mutation DTOs have NO `version` field, `assertExpectedVersion` is DEAD CODE (0 callers), and there is NO `OptimisticLockError→409 STALE_WRITE` mapping. `optimistic-locking.tc` green ONLY via a test-harness `fork.flush` wrapper; `optimistic-concurrency.feature` green ONLY because the step calls `assertExpectedVersion` inline (not through HTTP). **ROOT: the #1624 RED baseline tested the mechanism, never forced the HTTP wiring** — so implementing to green yields mechanism-without-wiring. Needs: `version` on mutation DTOs (AC1), services call the check (AC2), global exception-filter maps OptimisticLockError→409, BDD rewired through real endpoints. Migrations `006_version_columns` + `007_idempotency_key`.
  - **#1631** `25415fc8` (lifecycle) — endpoints GENUINELY WIRED (controllers→services: sale `@Post(':id/unfinalise')`, credit-note `@Patch(':id')`, deal `@Post()`+`@Get(':id/summary')`, haulage contacts + INV-H3), behavioral BDD 3/3, tc 5/5. Looks complete (not deeply behavior-verified end-to-end). Migration `006_haulage_contacts` → **COLLIDES with #1628's `006`** (renumber at merge; #1628 not merging yet so free on epic).
  - **#1632** `904984da` (observability + Partner) — metrics (6 OTel families) + alerts (synthetically proven, per #1135) + runbook DONE. **Partner audit PARTIAL:** built `RedisCachedTenantConfigProvider` + `HttpTenantConfigSource` (Redis-6.0-safe Lua GET+DEL) but `DefaultTenantConfigProvider` STILL returns the `partnerMode:false` STUB, and the real path needs a tenant-service `GET /internal/tenants/:id/config` endpoint that does NOT exist yet (cross-BC follow-up). No migrations.
  - **META-LESSON (reinforces adversarial-verify): green Wave-2 tests masked incomplete wiring (#1628 fully, #1632-Partner partially). The #1624 RED baseline under-specified HTTP integration. Always verify PROD WIRING, not just green.**
- **#1632 merged reality (better than the held-state note below):** Partner consuming-side IS wired — `tenant-config.module` factory returns the REAL `RedisCachedTenantConfigProvider` (over `HttpTenantConfigSource`) when Redis+HTTP configured, `DefaultTenantConfigProvider` stub only as dev/no-Redis fallback (correct design). ACTIVATION gated on the cross-BC tenant-service `GET /internal/tenants/:id/config` endpoint (does not exist) + `platform-internal-api-secret` provisioning ([[platform-internal-api-secret-provisioning-pending]] #1292). Observability (6 OTel families, alerts, Grafana dashboard, runbook, CI alert-rule checker) fully merged. The durable rule under it: **a config provider that falls back to a stub is safe only while nothing exercises the path** — the stub answers plausibly, so the gap is invisible until the first tenant needs the real value. Treat "no consumer yet" as a deadline, not a clearance, and make activating the real path a precondition of onboarding the first one.
- **3 SURGICAL BLOCKERS FIXED + PUSHED 2026-07-17** (HEAD `837891ed`; trading 554 tests + accounting 318 green; typecheck/lint/build clean; **every fix mutation-verified**):
  1. `11c24dfd` **Migration_009** adds `deleted_at timestamptz null` to all 20 TradingBaseEntity tables (catalog-only; no index — entity declares none, zero-drift rule). Verified all 20 tables are created by migrations 001–008 first (an ALTER on a never-created table would repeat the bug). Guard spec compares migration ⇄ **entity sources** (not a DB) since testcontainers use `createSchema()` — a 21st entity now fails CI.
  2. `ba046f3a` **accounting producer** now emits the full `InvoiceProcessedEventPayload`, **typed** (compiler-enforced) + explicit PascalCase→UPPER_SNAKE map (`toContractSourceEntityType`). ⚠️ **Drift was PRE-EXISTING on main** — the epic's correct consumer merely exposed it; **neither pact caught it** (accounting's provider pact self-mocks a THIRD shape via `POST /pact-events/...` and never verifies trading's published pact — a systemic contract-testing hole worth its own issue).
  3. `837891ed` **idempotency**: `completed_at` marker (folded into the unreleased Migration_008 — file doesn't exist on main, so amending is safe); replay only when COMPLETED; uncompleted → 409 `IDEMPOTENCY_KEY_IN_FLIGHT`; UniqueConstraintViolation → 409 not 500; `release()` on failure; **first-ever IdempotencyInterceptor tests**.
  - **RXJS LESSON (cost me a critical bug): `catchError` must sit DIRECTLY on `next.handle()`.** My first cut chained it AFTER the `complete()` switchMap → a `complete()` failure (post-commit) released the key → retry re-ran the mutation = **duplicate financial write**. I turned a false-success into a double-apply. Caught ONLY by the information-isolated adversarial-reviewer (the repo-aware nestjs lens returned junk). Correct shape: `handle().pipe(catchError(release+rethrow), switchMap(complete))`; a complete() failure must propagate → key uncompleted → retry 409. **Meta: never self-certify a fix — the maker≠checker rule applies to MY code too.**
- **SOFT-DELETE SQL PARITY FIXED + PUSHED 2026-07-17** (`206a53fd` list path + `9e371fb5` aggregation cache; trading 561 tests green): every table reference in the raw list SQL (the root row + 11 child EXISTS behind the derived status + the name subqueries, line-item guards in JOIN ON) now carries `deleted_at IS NULL`; hydration narrowed `filters:false`→`filters:{tenant:false}` (softDelete stays active = defence-in-depth; harness registers a neutered filter under the exact name `tenant`, so the narrowing is safe in both prod and tc). Review lens PROVED parity rule-by-rule against the ORM-side status deriver and traced MikroORM v6.6.14 source: default-true entity filters stay active on populated relations even when `filters:{tenant:{...}}` names only tenant (`isFilterActive: filter.default || name in options`) — detail path genuinely hides deleted children. **Review also surfaced the sibling in the aggregation path: the cache-update handler's 6 knex SUM queries (the totals rendered on that same list/CSV) ignored `deleted_at` — fixed same day** (domain rows only; reference-table JOINs deliberately NOT filtered — a soft-deleted reference row must keep resolving for historical line items or their values vanish from the totals). Confirmed-major coverage gap closed: tc behavioral tests proving a soft-deleted child stops matching each of two filters. Structural guard pattern: **count(FROM|JOIN <schema>.<table> <alias>) must equal count(<alias>.deleted_at IS NULL) with ALL filters enabled** — mutation-proof, names the offending table. Known heuristic limits (accepted): closed alias list; presence-not-effect.
- **TENANT FAIL-OPEN FIXED + PUSHED 2026-07-17** (`33f195d3`): the list/count/CSV raw SQL is now **fail-closed** — `buildWhere` calls `TenantContext.requireTenantId()` (typed `MissingTenantContextError`, '' = missing) before any SQL; predicate always pinned. It was the ONLY fail-open raw-SQL site in trading (purchase repo takes explicit `tenantId` param — the better pattern; outbox-lag/purge are cross-tenant by design). Unit spec (3 tests, Docker-free, capturing-connection stub asserts predicate+binding in BOTH count+page queries; zero SQL on throw) is mutation-proof. tc specs made production-faithful: deal-list-filters (3 wraps + no-context case) AND deal-batch-lock exportCsv wrap. **Adversarial review of my first cut confirmed 3 majors, all fixed pre-push: (1) I hand-rolled a bare `Error` duplicating the EXISTING platform primitive `TenantContext.requireTenantId()` — check libs/platform/mikro-orm exports before inventing tenant plumbing; (2) my caller-sweep missed deal-batch-lock.tc's exportCsv→findAll (grep for INDIRECT callers, not just `.findAll(`); (3) my `/no tenant context/` regex wouldn't even match the typed error's capitalized message — assert error TYPES, not message substrings.** Non-blocking noted: whitespace tenant-id passes the gate (matches ORM-filter behaviour platform-wide); no metric/log on gate trip.
- **MERGE-DAY BATCH 2026-07-17 (user: "do all remaining tasks, postpone the sign-off, merge to main, then deploy dev"). ⚠️ GOVERNANCE: the release sign-off was postponed for the MERGE only — it moves to the deploy/prod boundary (#1633 capstone still deploy-gated; merge≠deploy, Platform deploy is manual tag-bump).** Pushed `9e371fb5..96f320f8`:
  - `acef90c3` quick wins: CSV formula-injection (numeric-exempt, `detect-unsafe-regex`-safe helper), hourly `IdempotencyKeyPurgeScheduler` (mirrors OutboxLagObserver), RMQ `trading_user` +`read` on `acme.accounting` (values+helm test), trading inbox UniqueConstraint catch (dup→ack not DLX).
  - `44832915` **#1629 COMPLETE**: DELETE purchase/sale/credit-note (RBAC, DRAFT-only, INV-P11), ALL 7 hard-deletes tombstone, `trading.<entity>.deleted` events (5 typed payloads in event-contracts) → mapper `deleted→DELETED` → activity via existing recorder choke point (cancel already flowed), AC7 no-hard-delete gate spec. Perm naming: **`creditnote:` NOT `credit-note:`** (TradingPermission union).
  - `89844fbd` **review round confirmed 8 (4 crit): fixed all** — repo.remove = ONE atomic conditional `nativeUpdate` WHERE `{id, tenantId, deletedAt:null, _status:'DRAFT'}` (private prop names in native WHERE!) returning boolean; false→409 + NO event/emit; cascade-tombstone PLIs/SLIs in same txn; `creditnote:delete` seeded in BOTH user-service catalogs + ADMIN; delete emits `*StateChangedEvent` → financial cache recalc; stock query `whereNull(sli.deleted_at)`; gate catches `DELETE FROM`/`.delete()`.
  - `96f320f8` merge origin/main (23 commits): tenant-internal.controller = clean 4-endpoint union; **ADR COLLISION AGAIN (memory warned!): main took 0066-0069 (identity) → epic ADRs renumbered 0066/0067/0068 → 0070/0071/0072** (files + scoped ref sweep in trading paths only; 58 identity refs untouched; my "ADR-0067 Partner" comments were mislabeled anyway → dropped ADR ref). Main's new controller spec needed 2nd ctor arg.
  - Suites: trading 595, tenant 103+, user 234, inventory 294, accounting 299 (accounting-write-rbac file-level FLAKE in run-many, green standalone — recurring). CI on PR: **Dependency Audit fail = INHERITED main CVE state (epic touches no dep files) — informational, not required.**
- **🚨 EXPERT REVIEW 2026-07-17 (19 lenses / 108 agents / 9M tok) — PR #1651 was NOT mergeable as reviewed.** MAIN independently re-verified the top clusters by hand (do NOT trust the earlier "verified GREEN" claims — they were wrong). Recorded as classes, because the class is what recurs:
  1. **A soft-delete column mapped on a base entity with a `default:true` filter, and no migration creating it.** Every inheriting entity got a predicate for a column that existed only where the schema generator built it — so testcontainers were **structurally incapable of catching it** (green everywhere) while any migration-provisioned environment would have failed every query with an undefined-column error. [[platform-tc-bdd-red-baseline-gotchas]]
  2. **A soft-delete feature that met ~0 of its ACs — unreachable in the product.** No delete endpoints existed, the deletion-policy object had zero callers outside the BDD step file, most entities still hard-deleted, and the lifecycle event was never emitted. All three BDD scenarios passed because the steps hand-rolled the controller pipeline instead of driving it.
  3. **An event consumer left INERT by payload-contract drift.** The producer put one field on the envelope and named others differently; the handler read them off the payload, got `undefined`, and parked every real message — so the state transition it existed to perform could never happen outside tests. The same defect had shipped in the sibling PR. See [[platform-invoice-writeback-contract-drift]].
  4. **An idempotency store that LOST mutations.** It persisted and flushed the key with a null stored response BEFORE running the handler and never cleared it on failure ⇒ the retry matched the existing key and answered "replayed, nothing" → **client told success, mutation silently lost**. Also TOCTOU (find-then-insert, no unique-violation catch), and the purge job was never scheduled.
  5. **Raw SQL that was tenant FAIL-OPEN and soft-delete-blind.** The tenant predicate was added only `if (tenantId)` off an ALS store, so an absent context meant no predicate at all (violates [[platform-fail-closed-tenant-filter-forked-em]]); the same SQL never excluded deleted rows.
     Other confirmed classes: an inbox row written AFTER the apply instead of in the same transaction (violates ADR-0068); lifecycle endpoints taking no `version`, and a 409 that dropped the current version from `details` (violates ADR-0067); an inbox recorder with no unique-violation catch, so a benign redelivery went to the DLQ; CSV formula injection plus a silent 10k truncation; broker permissions never granted for the new consumer (bind 403 → crash-loop); a tenant-config path that failed OPEN with no chart supplying its URL/secret.
     **META-LESSON (hard-won): my pre-PR audit (12 auditors) passed this PR because it only audited MY delta, not the epic's substance. "Independent verifier + green tests" certified two tasks as CLEAN when neither worked outside the test harness. Scope audits to the SHIPPED BEHAVIOUR, and never let schema-generator tests stand in for migration-provisioned reality.**
- **EPIC PR OPENED — PR #1651 `epic/platform-trading-hardening → main` (2026-07-03), 167 files +12256/-675.** `mergeable=CONFLICTING` (16 commits behind main — needs `update-branch`/conflict-resolution before merge). Pushed after a pre-PR read-only audit (12 auditors + adversarial verify: 7-aggregate declared-version wiring, Partner contract, scaffold-removal, shared filter/migrations — all clean) AND pre-push-hook reproduction caught the ONLY real issue: **10 stale `// eslint-disable-next-line import/no-unresolved` RED-baseline (#1624) disables** across trading `src` specs + trading `test/testcontainers`+`test/pact` + inventory inbox spec — Platform ESLint never loads eslint-plugin-import so ESLint hard-errors "rule not found", failing the Platform lint gate (trading src-only lint HID the test/\* ones; inventory `src` one blocked the push). Fixed `2319df63` (modules now GREEN so disables removed). **MERGE HELD** (user go): a money-path merge to main needs an explicit human go + `ALLOW_PR_MERGE=1` + branch-up-to-date + required checks (`Validate Terraform`+`ci-gate`+`platform-ci-gate`). Epic HEAD `2319df63`.
- **Wave-2 follow-ups — ALL 3 GAPS CLOSED (2026-07-03, SOLO by main; "fix all gaps"). Both trading+tenant services build clean.**
  - **Gap 1 — #1628 declared-version DTO path — DONE `2d8532c6`.** `version` now required on mutation DTOs/inline @Body across all 7 aggregates (purchase/sale/haulage/overhead/customer/product/credit-note); services call `assertExpectedVersion(entity.version, dto.version)` (hardened helper in `common/optimistic-lock.ts`: non-integer→`ValidationException`, mismatch→`StaleWriteError`→409 `STALE_WRITE {currentVersion}`); response DTOs surface `version`. **Enforced at SERVICE layer, NOT ValidationPipe** — trading @Body params are inline/`import type` → metatype Object → ValidationPipe SKIPS them ([[platform-import-type-disables-validationpipe]]). purchase.service.spec (3 tests) + sale-updated-event.spec updated (mock+callers need `version:1`). `assertExpectedVersion` no longer dead code.
  - **Gap 2 — #1632 Partner tenant-service endpoint — DONE `4f67f639`.** Added `GET /internal/tenants/:id/config`→`{tenantId,settings}` on tenant-service `TenantInternalController` (behind `InternalAuthGuard`); `TenantSettings` gains optional `partnerMode`; `ConfigService.getSettings()` settings-only read (no creds/role); 404→trading maps to non-restricting (opt-in). Trading side was ALREADY fully wired (`TenantConfigModule` binds `RedisCachedTenantConfigProvider`→`HttpTenantConfigSource` when REDIS_URL+TENANT_SERVICE_URL+INTERNAL_API_SECRET set) — endpoint was the only missing piece. Loop now closed; runtime activation still needs #1292 secret provisioning (infra). 3 getSettings unit tests.
  - **Gap 3 — `{lparen}` deal-list BDD — DONE `a0a51e76` (by removal, not registration).** The `deal-http-gap-fills.feature`+`.steps.ts` were RED `notImplemented` scaffolds (even `bootApp` threw) whose HTTP-stack coverage SHIPPED instead as 6 dedicated integration specs (`deal-lock-rbac`, `child-lists-filter-dealid`, `deal-detail-derived-status`, `deal-activity-endpoint`, `customer-filter-role`, `trading-write-rbac` — real `app.inject`). `filter-presets.feature` was an orphan (no steps; feature lives+spec-tested in `platform-filter-presets` lib). Removed both — registering `{lparen}` would've turned 15 undefined→failing. **`global-search.feature` (6 undefined) RETAINED** — genuinely unbuilt future feature (no impl/migration anywhere), scaffold legitimately marks pending work. **Rule: remove redundant orphan scaffolds (coverage proven elsewhere); KEEP genuine-pending scaffolds.**
- **#1633 remaining:** prod-verify + 48h soak (deferred capstone, deploy-gated).
- **PR #1634** (trading #1625 clean-slice, off main) — **PUSHED `71a554d7` (2026-07-03), CI running; reconciliation (see [[platform-invoice-writeback-contract-drift]]) now on the PR.** Merge to main HELD for explicit user go (money-path change, zero tolerance; requires branch-up-to-date + `ALLOW_PR_MERGE=1` + required checks). NOTE full trading `test:integration` red on PRE-EXISTING tenant-isolation specs, independent of #1625.

**Gotcha carried:** today's code emits STOCK_INSUFFICIENT/422 where contract mandates STOCK_EXCEEDED/409 → task #1627 remaps (done). `gh sub-issue create` supports `--body` NOT `--body-file`. Source-driven-dev hook blocks Write/Edit to files importing `@nestjs/`/`@mikro-orm/` until breadcrumb `.claude/.source-driven-dev/<fw>.fetched` exists (per-worktree; `.claude` is a real dir per vth worktree, NOT symlinked here).
